# Requirements Archive: MARE-MCP-Toolbox v1.0 (Remote MCP Foundation)

**Archived:** 2026-04-27
**Status:** ✅ SHIPPED
**Defined:** 2026-04-08
**Core Value:** Automated malware triage and deep analysis via AI agents with full access to professional RE tooling — accessible both from inside the container and from external MCP clients.

> This file is a frozen snapshot of REQUIREMENTS.md at v1.0 milestone completion. Active requirements live in `.planning/REQUIREMENTS.md` for the next milestone.

## v1 Requirements

### IDA Pro Backend

- [x] **IDA-01**: IDA Pro installs conditionally via `INSTALL_IDA_PRO` build arg using multi-stage Docker build (license never in image layers) — ✓ Validated Phase 1 Plan 01
- [x] **IDA-02**: IDA Pro headless MCP server runs via SSE transport inside container — ✓ Validated Phase 1 Plan 03 (adjusted from stdio `ida-mcp` to SSE `idalib-mcp` from mrexodia)
- [x] **IDA-03**: IDA Pro license persists on host via bind mount to `~/.idapro/` (same pattern as Binary Ninja's `~/.binaryninja/`) — ✓ Validated Phase 1 Plan 01
- [x] **IDA-04**: `configure-agent-mcp.sh` detects IDA Pro and registers MCP server with fallback chain — ✓ Validated Phase 1 Plan 02 (adjusted: priority is IDA > BN > Ghidra, codified in script lines 67-119)
- [x] **IDA-05**: Hex-Rays decompiler functions are available via MCP when user has decompiler license — ✓ Validated Phase 1 (inherited from mrexodia/ida-pro-mcp tool surface)
- [x] **IDA-06**: Python environment isolation prevents conflicts between IDA Pro, Binary Ninja, and Ghidra APIs — ✓ Validated Phase 1 Plan 01 (multi-stage build keeps each backend's Python environment scoped)

### MCP Gateway

- [x] **GW-01**: Python FastMCP server exposes curated tool surface over Streamable HTTP transport (spec 2025-03-26) — ✓ Validated Phase 2 Plan 02
- [x] **GW-02**: Gateway exposes ~15-25 orchestrator-level tools mapping to the existing 13-artifact pipeline — ✓ Validated Phase 2 Plan 02 (21 curated tools registered)
- [x] **GW-03**: Disassembler tools route through the pinned backend (IDA > BN > Ghidra priority), exposed via a single authenticated endpoint. Backend tools pass through under their native names; clients call `get_active_backend()` to discover the active surface — ✓ Validated Phase 2 Plans 03/05 (wording corrected from earlier "BN > IDA > Ghidra"; pass-through semantics clarified per D-07)
- [x] **GW-04**: Bearer token authentication required on all remote MCP endpoints — ✓ Validated Phase 2 Plan 01
- [x] **GW-05**: Gateway binds to localhost only by default; explicit opt-in for network exposure — ✓ Validated Phase 2 Plan 01, Phase 3 Plan 01 (`MCP_GATEWAY_HOST_BIND`)
- [x] **GW-06**: File upload mechanism allows remote clients to submit samples to the container for analysis — ✓ Validated Phase 2 Plan 04 (`POST /upload` with sha256 content-addressing, 1 GB cap, path-traversal mitigations)

### External Clients

- [x] **CLI-01**: Claude Code connects to container via `.mcp.json` with `type: "http"` and bearer token header — ✓ Validated Phase 4 Plans 01/02/05 (automated e2e) + ✓ Manual UAT signoff 2026-05-11 (administrator@leongs-house.dev). Driven via real `claude` CLI: config discovery, env-var token expansion, `mcp list` Connected, `list_uploads`/`get_active_backend` tool calls, `resources/list` (13 URIs), `resources/read`, wrong-token 401 rejection.
- [x] **CLI-02**: Mastra.ai connects to container via `MCPClient` with same Streamable HTTP endpoint — ✓ Validated Phase 4 Plan 04 (runnable starter at `templates/mastra/` using `@mastra/mcp ~1.3.1`)
- [x] **CLI-03**: Pre-built config templates provided for Claude Code and mastra.ai — ✓ Validated Phase 4 Plans 01/04/06 (`templates/claude-code/.mcp.json`, `templates/mastra/`, README two-mode framing)
- [x] **CLI-04**: MCP Resources expose case artifacts as browsable resources — ✓ Validated Phase 4 Plan 03 (`mare://cases/<case>/<artifact>` URI scheme, 13-artifact coverage)

### Container Infrastructure

- [x] **INF-01**: Dual-mode entrypoint supports both local agent mode and remote MCP gateway mode — ✓ Validated Phase 3 Plan 01 (`./run_docker.sh` vs `./run_docker.sh --remote`)
- [x] **INF-02**: Docker Compose exposes gateway port with configurable mapping — ✓ Validated Phase 3 Plan 01 (`MCP_GATEWAY_HOST_BIND` / `MCP_GATEWAY_HOST_PORT`, no rebuild needed)
- [x] **INF-03**: Python 3.12+ available in container — ✓ Validated Phase 1 Plan 01
- [x] **INF-04**: `run_docker.sh` updated with IDA Pro zip detection and `IDA_USER_DIR` env var for license persistence — ✓ Validated Phase 1 Plan 01
- [x] **INF-05**: Existing local agent workflow continues working unchanged — ✓ Validated Phase 3 Plan 01 (`MCP_GATEWAY_ENABLED` Dockerfile guard ensures local mode has zero gateway leak)

## v2 Requirements (carried forward)

### Advanced Gateway

- **GW-V2-01**: MCP Prompts expose orchestrator workflow as prompt templates (full triage, deep analysis)
- **GW-V2-02**: Dynamic notifications push analysis progress to connected clients
- **GW-V2-03**: Multi-session support — multiple clients can run independent analyses concurrently
- **GW-V2-04**: Database/session lifecycle management with configurable timeouts and cleanup

### Advanced Disassemblers

- **DIS-V2-01**: Unified disassembler abstraction layer (normalize tool names/params across all three backends)
- **DIS-V2-02**: Backend comparison mode — run same analysis on multiple disassemblers and diff results

## Out of Scope

| Feature | Reason |
|---------|--------|
| Custom web UI or dashboard | Clients are Claude Code, Codex, mastra.ai — no custom frontend needed |
| Dynamic analysis orchestration | Static analysis focus; dynamic tools available but not orchestrated |
| Replacing Binary Ninja or Ghidra | IDA Pro is an addition, not a replacement |
| OAuth 2.1 authentication | Overkill for local/team container deployment; bearer token sufficient |
| Raw CLI tool passthrough | Security risk on privileged container; curated tools only |
| ARM64 IDA Pro support | IDA Pro Linux is x86_64-only; hard constraint from Hex-Rays |
| Rewriting existing orchestrator skill | Existing 13-artifact pipeline stays intact |

## Traceability — Final

| Requirement | Phase | Status |
|-------------|-------|--------|
| IDA-01 | Phase 1 | Complete |
| IDA-02 | Phase 1 | Complete (adjusted: SSE, not stdio) |
| IDA-03 | Phase 1 | Complete |
| IDA-04 | Phase 1 | Complete (adjusted: IDA > BN > Ghidra) |
| IDA-05 | Phase 1 | Complete |
| IDA-06 | Phase 1 | Complete |
| GW-01 | Phase 2 | Complete |
| GW-02 | Phase 2 | Complete |
| GW-03 | Phase 2 | Complete (adjusted: native-name pass-through) |
| GW-04 | Phase 2 | Complete |
| GW-05 | Phase 2 | Complete |
| GW-06 | Phase 2 | Complete |
| CLI-01 | Phase 4 | Complete (automated + manual UAT signoff 2026-05-11) |
| CLI-02 | Phase 4 | Complete |
| CLI-03 | Phase 4 | Complete |
| CLI-04 | Phase 4 | Complete |
| INF-01 | Phase 3 | Complete |
| INF-02 | Phase 3 | Complete |
| INF-03 | Phase 1 | Complete |
| INF-04 | Phase 1 | Complete |
| INF-05 | Phase 3 | Complete |

**Coverage:**
- v1 requirements: 21 total
- Shipped: 21
- Outstanding: 0 (CLI-01 manual UAT signoff completed 2026-05-11)

## Requirement Adjustments During Milestone

- **IDA-02** — moved from stdio `ida-mcp` to SSE `idalib-mcp` (mrexodia) during Phase 1 Plan 03 gap closure; SSE transport better fits remote MCP architecture
- **IDA-04 / GW-03** — fallback chain priority corrected from "BN > IDA > Ghidra" to "IDA > BN > Ghidra" per actual implementation (Phase 1 D-06, Phase 2 D-09, `configure-agent-mcp.sh` lines 67-119)
- **GW-03** — "unified interface" clarified to mean single authenticated endpoint + bearer token, NOT unified tool names; disassembler tools pass through under their native names per Phase 2 D-07 (`get_active_backend()` discovery)
- **GW-02** — landed at 22 gateway-native tools (target was 15-25)

---

*Archived: 2026-04-27*
