---
phase: 10
plan: 03
type: execute
wave: 1
depends_on: [10-01, 10-02]
files_modified:
  - mcp-gateway/src/mcp_gateway/extraction.py
autonomous: true
requirements: [EXTR-02, EXTR-06]

must_haves:
  truths:
    - "start_extract_monitor(job_id, extraction_dir) is an async coroutine in mcp_gateway.extraction"
    - "Monitor polls Phase 9's get_tool_job at EXTRACT_MONITOR_INTERVAL_S until job hits terminal status"
    - "Monitor measures extraction-dir bytes via _du_sb (os.walk + lstat, follow symlinks NEVER, dedup by inode for hardlinks per Pitfall 7)"
    - "On cap-exceed: monitor writes .MARE_EXTRACT_CAP_EXCEEDED marker, updates meta cap_exceeded=True status=cap_exceeded, cancels job via Phase 9 tools.jobs.cancel_tool_job, then exits"
    - "On job-terminal: monitor runs quarantine_symlinks BEFORE flipping meta status out of running (D-15 timing rule)"
    - "Monitor task references retained in a module-level set with discard-on-done callback (Pitfall: asyncio.create_task GC drop)"
    - "Wave 0 RED-stub test_extract_monitor.py now COLLECTS cleanly"
  artifacts:
    - path: "mcp-gateway/src/mcp_gateway/extraction.py"
      provides: "start_extract_monitor coroutine + _du_sb helper + _extract_monitor_tasks module-level set + _TERMINAL_JOB_STATUSES tuple + _spawn_monitor"
      contains: "async def start_extract_monitor"
  key_links:
    - from: "mcp-gateway/src/mcp_gateway/extraction.py::start_extract_monitor"
      to: "mcp_gateway.tools.jobs.get_tool_job / cancel_tool_job"
      via: "LOCAL import inside the coroutine (avoid circular)"
      pattern: "from mcp_gateway.tools import jobs"
    - from: "mcp-gateway/src/mcp_gateway/extraction.py::start_extract_monitor"
      to: "quarantine_symlinks (same module)"
      via: "post-terminal hook"
      pattern: "quarantine_symlinks"
    - from: "mcp-gateway/src/mcp_gateway/extraction.py::start_extract_monitor"
      to: "update_meta (same module)"
      via: "incremental meta updates"
      pattern: "update_meta"
---

<objective>
Wave 1 (separated for focused review): Extend `mcp-gateway/src/mcp_gateway/extraction.py` with the archive-bomb monitor — `start_extract_monitor(job_id, extraction_dir)` async coroutine, `_du_sb` byte-counter helper, `_extract_monitor_tasks` module-level set for GC-safe task retention, `_TERMINAL_JOB_STATUSES` frozenset, and the `_spawn_monitor` factory used by Plan 04.

Plan 03 stays in `extraction.py` (not a new file) — monitor is part of the primitive layer. Bundling with Plan 02 was avoided ONLY because the monitor is the most concurrency-sensitive piece and benefits from a separate focused review.

Purpose: Enforce EXTR-06 archive-bomb cap (MCP_GATEWAY_MAX_EXTRACT_MB, default 4 GB) and the D-15 symlink-quarantine timing rule (sweep BEFORE meta status flips out of `running`). Primary V12 (Files & Resources) security control.

Output: Additions to `extraction.py` (~120-180 LoC). RED-stub test_extract_monitor.py collects cleanly after this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-extraction-tier/10-CONTEXT.md
@.planning/phases/10-extraction-tier/10-RESEARCH.md
@.planning/phases/10-extraction-tier/10-02-SUMMARY.md
@mcp-gateway/src/mcp_gateway/extraction.py
@mcp-gateway/src/mcp_gateway/jobs.py
@mcp-gateway/src/mcp_gateway/tools/jobs.py

<interfaces>
Plan 03 ADDS to extraction.py. Existing Plan 02 surface is unchanged.

New public surface:
```
async def start_extract_monitor(
    job_id: str,
    extraction_dir: Path,
    *,
    interval_s: float | None = None,
    max_bytes: int | None = None,
) -> None
```

New module-level state:
```
_extract_monitor_tasks: set[asyncio.Task]
_TERMINAL_JOB_STATUSES: frozenset[str]    # mirrors Phase 9 D-06 vocabulary
```

New private helpers:
```
def _du_sb(root: Path) -> int             # os.walk + lstat, no-follow, inode dedup
def _spawn_monitor(job_id, extraction_dir) -> asyncio.Task
```

Phase 9 vocabulary (read-only — do NOT extend):
```
_TERMINAL_JOB_STATUSES = {"succeeded","failed","cancelled","killed_timeout","killed_log_cap"}
# "cap_exceeded" is a META sidecar status, NOT a Phase 9 JobStatus.
```

Imports Plan 03 ADDS to extraction.py:
- `asyncio` (module-top)
- `stat` (module-top — used by _du_sb for S_ISREG check)
- `tools.jobs` (LOCAL import inside start_extract_monitor for get_tool_job/cancel_tool_job)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add monitor coroutine, _du_sb, terminal-status set, GC-safe task retention to extraction.py</name>
  <files>mcp-gateway/src/mcp_gateway/extraction.py</files>
  <read_first>
    - mcp-gateway/src/mcp_gateway/extraction.py (Plan 02 output — confirm current state before extending)
    - mcp-gateway/src/mcp_gateway/jobs.py — grep for `TERMINAL_STATUSES` to confirm exact Phase 9 vocabulary
    - mcp-gateway/src/mcp_gateway/tools/jobs.py — `get_tool_job` and `cancel_tool_job` signatures (programmatic Python coroutine calls supported because FastMCP-decorated handlers remain awaitable)
    - .planning/phases/10-extraction-tier/10-CONTEXT.md lines 806-873 (D-17 monitor behaviour + post-terminal hook + D-18 env constants)
    - .planning/phases/10-extraction-tier/10-RESEARCH.md sections "Pattern 3: Sibling Monitor Task" and "Pitfall 7: du vs os.walk hardlink counting"
    - mcp-gateway/tests/extraction/test_extract_monitor.py (Wave 0 RED stub — three test function names: test_cap_exceeded_cancels_job, test_clean_exit_on_terminal, test_monitor_poll_count_updates_meta)
  </read_first>
  <behavior>
    Monitor flow (D-17 exactly):
    - interval = interval_s or EXTRACT_MONITOR_INTERVAL_S
    - cap = max_bytes or MAX_EXTRACT_BYTES
    - Loop:
      1. await asyncio.sleep(interval)
      2. snap = await tools_jobs.get_tool_job(job_id)  (LOCAL-imported tools.jobs)
      3. if snap is non-dict OR snap.get("error") OR snap.get("status") in _TERMINAL_JOB_STATUSES: break to post-terminal hook
      4. size = _du_sb(extraction_dir); polls += 1
      5. update_meta(extraction_dir, {"extract_bytes_total": size, "monitor_polls": polls})
      6. if size > cap and not cap_marker_written:
         - write `.MARE_EXTRACT_CAP_EXCEEDED` marker file with cap+observed+timestamp body
         - update_meta(..., {"cap_exceeded": True, "status": "cap_exceeded"})  # sticky
         - await tools_jobs.cancel_tool_job(job_id)
         - break to post-terminal hook

    Post-terminal hook (runs on every loop exit path):
    1. count, _paths = quarantine_symlinks(extraction_dir)  # D-15: BEFORE final status flip
    2. final_snap = await tools_jobs.get_tool_job(job_id)  (tolerate errors)
    3. existing_meta = read_meta(extraction_dir)
    4. final_status:
       - if existing_meta["cap_exceeded"]: "cap_exceeded" (sticky overrides)
       - elif final_snap.status in _TERMINAL_JOB_STATUSES: that status
       - else: existing_meta.status or "unknown"
    5. final_size = _du_sb(extraction_dir)
    6. update_meta with completed_at, exit_code, status, symlinks_quarantined, extract_bytes_total

    Exception safety:
    - Every external call (get_tool_job, _du_sb, update_meta, read_meta, write_text) wrapped in try/except + log.warning
    - asyncio.CancelledError re-raised inside the sleep call; otherwise fall through to post-terminal hook so quarantine still runs on shutdown
    - Monitor coroutine NEVER raises out (matches Phase 9 D-15 "tools never raise" contract)

    _du_sb (Pitfall 7 — hardlink inode dedup):
    - os.walk(root, followlinks=False) (default)
    - For each file: p.lstat() (NOT stat — no follow), check stat.S_ISREG, dedup via (st_dev, st_ino) tuple set
    - Skip symlinks, FIFOs, sockets, char/block devices
    - Returns int total bytes

    Task retention (RESEARCH Pattern 3 Pitfall):
    - Module-level `_extract_monitor_tasks: set[asyncio.Task] = set()`
    - `_spawn_monitor(job_id, extraction_dir)` factory: creates asyncio.Task, adds to set, registers done-callback that discards from set
    - Plan 04 wrappers MUST call `_spawn_monitor` (not bare asyncio.create_task) so GC retention is centralized
  </behavior>
  <action>
    Use the Edit tool to APPEND to `mcp-gateway/src/mcp_gateway/extraction.py` (file already exists from Plan 02).

    Step 1 — Add `asyncio` and `stat` to the module-top stdlib import block. Place alphabetically near the existing `datetime`, `hashlib`, `json`, `os`, `secrets` imports.

    Step 2 — After the existing JobToolSpec registrations (end of file from Plan 02), append the following section. Code is paste-ready; do not change semantics, only adjust whitespace to match the file's existing style.

    Code block to append (everything between the BEGIN/END markers):

    BEGIN APPEND
    ```
    # ----------------------------------------------------------------------------
    # Plan 03: archive-bomb monitor (D-17) + GC-safe task retention
    # ----------------------------------------------------------------------------

    _TERMINAL_JOB_STATUSES: frozenset[str] = frozenset({
        "succeeded", "failed", "cancelled", "killed_timeout", "killed_log_cap",
    })

    _extract_monitor_tasks: set[asyncio.Task] = set()


    def _du_sb(root: Path) -> int:
        """Sum regular-file sizes under root; do NOT follow symlinks; dedup hardlinks by inode.

        Pitfall 7 mitigation: track (st_dev, st_ino) to avoid double-counting hardlinked
        content. Errs slightly conservative (caps fire earlier than `du -sb` on hardlinked
        trees — the safe direction).
        """
        total = 0
        seen: set[tuple[int, int]] = set()
        root_p = Path(root)
        if not root_p.is_dir():
            return 0
        for dirpath, _dirs, files in os.walk(str(root_p), followlinks=False):
            for name in files:
                p = Path(dirpath) / name
                try:
                    st = p.lstat()
                except OSError:
                    continue
                if not stat.S_ISREG(st.st_mode):
                    continue
                key = (st.st_dev, st.st_ino)
                if key in seen:
                    continue
                seen.add(key)
                total += st.st_size
        return total


    async def start_extract_monitor(
        job_id: str,
        extraction_dir: Path,
        *,
        interval_s: float | None = None,
        max_bytes: int | None = None,
    ) -> None:
        """Sibling asyncio.Task — poll Phase 9 job status + extraction-dir size.

        Cap-exceeded path: write .MARE_EXTRACT_CAP_EXCEEDED marker, flip meta to
        cap_exceeded (sticky), cancel the Phase 9 job, run post-terminal hook.

        Normal path: poll until job terminal, then post-terminal hook (symlink
        quarantine BEFORE final meta status flip — D-15 timing rule).

        Never raises out of the coroutine.
        """
        interval = float(interval_s) if interval_s is not None else EXTRACT_MONITOR_INTERVAL_S
        cap = int(max_bytes) if max_bytes is not None else MAX_EXTRACT_BYTES
        polls = 0
        cap_marker_written = False

        from mcp_gateway.tools import jobs as tools_jobs  # LOCAL import: avoid circular

        try:
            while True:
                await asyncio.sleep(interval)

                try:
                    snap = await tools_jobs.get_tool_job(job_id)
                except Exception as e:
                    log.warning("[extract-monitor] get_tool_job(%s) raised: %s", job_id, e)
                    break

                if not isinstance(snap, dict):
                    log.warning("[extract-monitor] non-dict snapshot for %s", job_id)
                    break
                if "error" in snap:
                    log.info("[extract-monitor] job %s evicted (%s)", job_id, snap.get("error"))
                    break
                if snap.get("status") in _TERMINAL_JOB_STATUSES:
                    break

                try:
                    size = _du_sb(Path(extraction_dir))
                except Exception as e:
                    log.warning("[extract-monitor] _du_sb raised: %s", e)
                    size = 0
                polls += 1
                try:
                    update_meta(Path(extraction_dir), {
                        "extract_bytes_total": size,
                        "monitor_polls": polls,
                    })
                except Exception as e:
                    log.warning("[extract-monitor] update_meta(progress) failed: %s", e)

                if size > cap and not cap_marker_written:
                    try:
                        marker = Path(extraction_dir) / ".MARE_EXTRACT_CAP_EXCEEDED"
                        marker.write_text(
                            "Cap: " + str(cap) + " bytes; observed: " + str(size)
                            + " bytes; at: " + _utc_now_iso() + "\n",
                            encoding="utf-8",
                        )
                        cap_marker_written = True
                    except Exception as e:
                        log.warning("[extract-monitor] cap marker write failed: %s", e)
                    try:
                        update_meta(Path(extraction_dir), {
                            "cap_exceeded": True,
                            "status": "cap_exceeded",
                        })
                    except Exception as e:
                        log.warning("[extract-monitor] update_meta(cap) failed: %s", e)
                    try:
                        await tools_jobs.cancel_tool_job(job_id)
                    except Exception as e:
                        log.warning("[extract-monitor] cancel_tool_job failed: %s", e)
                    break

        except asyncio.CancelledError:
            log.info("[extract-monitor] cancelled for %s; running post-terminal hook anyway", job_id)

        # Post-terminal hook — D-15: quarantine BEFORE final status flip
        try:
            count, _paths = quarantine_symlinks(Path(extraction_dir))
        except Exception as e:
            log.warning("[extract-monitor] quarantine_symlinks failed: %s", e)
            count = 0

        try:
            final_snap = await tools_jobs.get_tool_job(job_id)
        except Exception:
            final_snap = {}

        try:
            existing = read_meta(Path(extraction_dir))
        except Exception:
            existing = {}

        if existing.get("cap_exceeded"):
            final_status = "cap_exceeded"
        elif isinstance(final_snap, dict) and final_snap.get("status") in _TERMINAL_JOB_STATUSES:
            final_status = final_snap["status"]
        else:
            final_status = existing.get("status", "unknown")

        try:
            final_size = _du_sb(Path(extraction_dir))
        except Exception:
            final_size = 0

        try:
            update_meta(Path(extraction_dir), {
                "completed_at": _utc_now_iso(),
                "exit_code": (final_snap.get("exit_code") if isinstance(final_snap, dict) else None),
                "status": final_status,
                "symlinks_quarantined": count,
                "extract_bytes_total": final_size,
            })
        except Exception as e:
            log.warning("[extract-monitor] update_meta(final) failed: %s", e)


    def _spawn_monitor(job_id: str, extraction_dir: Path) -> asyncio.Task:
        """Spawn the monitor and retain a strong ref to prevent GC drop.

        Plan 04 wrappers MUST call this (not bare asyncio.create_task).
        """
        task = asyncio.create_task(start_extract_monitor(job_id, Path(extraction_dir)))
        _extract_monitor_tasks.add(task)
        task.add_done_callback(_extract_monitor_tasks.discard)
        return task
    ```
    END APPEND

    Step 3 — Self-check after edit:
    - File still imports cleanly: `python -c "from mcp_gateway import extraction"` exits 0
    - Plan 02 contracts unbroken: `python -c "from mcp_gateway import extraction; from mcp_gateway.jobs import JOB_TOOL_REGISTRY; assert 'unblob' in JOB_TOOL_REGISTRY and 'binwalk_extract' in JOB_TOOL_REGISTRY"`
    - New surface present: `python -c "from mcp_gateway import extraction; assert callable(extraction.start_extract_monitor) and callable(extraction._spawn_monitor) and isinstance(extraction._extract_monitor_tasks, set) and isinstance(extraction._TERMINAL_JOB_STATUSES, frozenset)"`
    - `_du_sb` works: temp dir with 2 files (100 and 250 bytes) → returns 350
    - All 5 expected terminal statuses present: `"succeeded","failed","cancelled","killed_timeout","killed_log_cap"`
    - Plan 01 RED stub test_extract_monitor.py now COLLECTS cleanly (Plan 05 fills bodies)
  </action>
  <verify>
    <automated>cd mcp-gateway && python -c "from mcp_gateway import extraction; assert callable(extraction.start_extract_monitor); assert callable(extraction._spawn_monitor); assert isinstance(extraction._extract_monitor_tasks, set); assert isinstance(extraction._TERMINAL_JOB_STATUSES, frozenset); assert {'succeeded','failed','cancelled','killed_timeout','killed_log_cap'} <= extraction._TERMINAL_JOB_STATUSES; from mcp_gateway.jobs import JOB_TOOL_REGISTRY; assert 'unblob' in JOB_TOOL_REGISTRY and 'binwalk_extract' in JOB_TOOL_REGISTRY; print('OK')" && python -m pytest tests/extraction/test_extract_monitor.py --collect-only -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from mcp_gateway import extraction; assert callable(extraction.start_extract_monitor)"` exits 0
    - `python -c "from mcp_gateway import extraction; assert callable(extraction._spawn_monitor)"` exits 0
    - `python -c "from mcp_gateway import extraction; assert isinstance(extraction._extract_monitor_tasks, set)"` exits 0
    - `python -c "from mcp_gateway import extraction; assert {'succeeded','failed','cancelled','killed_timeout','killed_log_cap'} <= extraction._TERMINAL_JOB_STATUSES"` exits 0
    - `python -c "from mcp_gateway.extraction import _du_sb; import tempfile, pathlib; td=tempfile.mkdtemp(); pathlib.Path(td,'a').write_bytes(b'x'*100); pathlib.Path(td,'b').write_bytes(b'y'*250); assert _du_sb(pathlib.Path(td)) == 350"` exits 0
    - `grep -c '^import asyncio\|^import asyncio$' mcp-gateway/src/mcp_gateway/extraction.py` returns 1
    - `grep -c '^import stat\|^import stat$' mcp-gateway/src/mcp_gateway/extraction.py` returns 1
    - `grep -c 'from mcp_gateway.tools import jobs' mcp-gateway/src/mcp_gateway/extraction.py` returns 1 (LOCAL import inside start_extract_monitor)
    - `grep -c 'asyncio.create_task' mcp-gateway/src/mcp_gateway/extraction.py` returns 1 (only inside _spawn_monitor)
    - `grep -c 'add_done_callback' mcp-gateway/src/mcp_gateway/extraction.py` returns 1
    - `grep -c 'MARE_EXTRACT_CAP_EXCEEDED' mcp-gateway/src/mcp_gateway/extraction.py` returns 1
    - `grep -c '"cap_exceeded"' mcp-gateway/src/mcp_gateway/extraction.py` returns 2 (cap_exceeded boolean + status literal)
    - `grep -c 'followlinks=False' mcp-gateway/src/mcp_gateway/extraction.py` returns 2 (one from quarantine_symlinks in Plan 02, one from _du_sb here)
    - `grep -c 'quarantine_symlinks(' mcp-gateway/src/mcp_gateway/extraction.py` returns 2 (definition + post-terminal-hook invocation)
    - JOB_TOOL_REGISTRY still contains unblob + binwalk_extract (Plan 02 contracts unbroken)
    - `cd mcp-gateway && python -m pytest tests/extraction/test_extract_monitor.py --collect-only -q` exits 0
  </acceptance_criteria>
  <done>extraction.py extended with monitor + _du_sb + retention set + spawn factory; imports cleanly; Plan 02 contracts unbroken; Wave 0 RED stub for extract_monitor collects cleanly.</done>
</task>

</tasks>

<threat_model>
  <asvs_level>1</asvs_level>
  <threats>
    <threat id="T-10-03-01">
      <description>Archive bomb expands without bound, exhausting disk and causing host DoS.</description>
      <severity>high</severity>
      <mitigation>start_extract_monitor polls _du_sb every EXTRACT_MONITOR_INTERVAL_S (default 5 s); on size > MAX_EXTRACT_BYTES (default 4 GB) writes marker, flips meta to sticky cap_exceeded, cancels Phase 9 job (SIGTERM+SIGKILL via cancel_tool_job). Worst-case overshoot bounded by per-interval growth = (5 s × disk-write-rate). Acceptance criterion `grep -c 'MARE_EXTRACT_CAP_EXCEEDED' = 1` enforces marker; `grep -c '"cap_exceeded"' = 2` enforces meta sticky flip.</mitigation>
    </threat>
    <threat id="T-10-03-02">
      <description>Symlink in extraction → host file read via Phase 7 Resources walker BEFORE quarantine runs. If the monitor flips meta status to "succeeded" before quarantining, agents polling the meta sidecar would think the extraction is safe to browse via Resources while symlinks still resolve to /etc/shadow.</description>
      <severity>high</severity>
      <mitigation>D-15 timing rule: quarantine_symlinks runs in the post-terminal hook BEFORE update_meta with the final status (verified by ordering of statements in the action block — quarantine call precedes update_meta call). Acceptance criterion `grep -c 'quarantine_symlinks(' = 2` enforces both definition and post-terminal invocation. Agents that wait on `_mare_meta.json.status != "running"` get a guaranteed post-sweep view.</mitigation>
    </threat>
    <threat id="T-10-03-03">
      <description>asyncio.create_task return value is dropped by GC; monitor never runs; cap never enforced.</description>
      <severity>high</severity>
      <mitigation>_spawn_monitor stores the Task in module-level _extract_monitor_tasks set; add_done_callback(set.discard) removes it after completion. Plan 04 wrappers MUST call _spawn_monitor (documented in interfaces). Acceptance criterion `grep -c 'add_done_callback' = 1` enforces the retention idiom; `grep -c 'asyncio.create_task' = 1` ensures only _spawn_monitor spawns tasks (one spawn site only).</mitigation>
    </threat>
    <threat id="T-10-03-04">
      <description>Hardlink-based extraction (squashfs, ext-img) under-reports cap by 2-10x because each hardlink counted once each instead of once total via inode.</description>
      <severity>medium</severity>
      <mitigation>_du_sb dedups via (st_dev, st_ino) seen set — Pitfall 7 mitigation. Returns ONE size per inode regardless of hardlink count. Same direction as `du -sb`. Tests under tests/extraction/test_extract_monitor.py will assert this behaviour (Plan 05 body fill-in).</mitigation>
    </threat>
    <threat id="T-10-03-05">
      <description>Monitor follows a symlink during _du_sb walk, opens host files for stat, possibly reads /proc with cap-overshooting bytes.</description>
      <severity>medium</severity>
      <mitigation>_du_sb uses `os.walk(followlinks=False)` (explicit) and `p.lstat()` (NOT stat — does not follow). stat.S_ISREG check skips symlinks, FIFOs, sockets. Acceptance criterion `grep -c 'followlinks=False' = 2` enforces no-follow for both quarantine and du.</mitigation>
    </threat>
    <threat id="T-10-03-06">
      <description>Monitor coroutine raises an unhandled exception; the gateway log fills with stack traces; subsequent monitors fail to spawn.</description>
      <severity>medium</severity>
      <mitigation>Every external call wrapped in try/except + log.warning. asyncio.CancelledError caught and re-raised (per asyncio contract) but fall-through to post-terminal hook ensures quarantine still runs on shutdown. Monitor coroutine NEVER raises out — matches Phase 9 D-15 "tools never raise" contract. The done_callback removes the Task from the retention set whether it succeeded, raised, or was cancelled.</mitigation>
    </threat>
    <threat id="T-10-03-07">
      <description>Concurrent monitor+wrapper writes to _mare_meta.json corrupt the JSON, allowing agents to read stale or partial cap_exceeded flag.</description>
      <severity>medium</severity>
      <mitigation>Inherited from Plan 02: update_meta uses tempfile + os.rename atomic write. Single-writer guarantee in practice: wrapper writes initial meta (status=running) then returns; only the monitor mutates afterwards. Plan 04's wrapper must NOT re-touch meta after spawning the monitor (documented in Plan 04 D-13 flow).</mitigation>
    </threat>
  </threats>
</threat_model>

<verification>
- `python -c "from mcp_gateway import extraction; assert callable(extraction.start_extract_monitor) and callable(extraction._spawn_monitor)"` exits 0
- _du_sb correctly sums regular-file bytes ignoring symlinks/inode duplicates
- _TERMINAL_JOB_STATUSES is a frozenset containing the exact Phase 9 D-06 vocabulary
- `grep -c 'followlinks=False'` returns 2 (quarantine + du)
- `grep -c 'asyncio.create_task'` returns 1 (only inside _spawn_monitor — centralized spawn site)
- Plan 02 JobToolSpec registrations still present after Plan 03 edits
- test_extract_monitor.py collects cleanly with no errors
</verification>

<success_criteria>
1. extraction.py extended with start_extract_monitor coroutine + _du_sb + _TERMINAL_JOB_STATUSES + _extract_monitor_tasks set + _spawn_monitor factory
2. Module-top imports of `asyncio` and `stat` present (1 each)
3. tools.jobs imported LOCALLY inside start_extract_monitor only (no module-top tools.jobs import — avoids circular dependency)
4. All threat-model mitigations grep-verifiable in source (followlinks=False ≥ 2, MARE_EXTRACT_CAP_EXCEEDED ≥ 1, cap_exceeded literal ≥ 2, add_done_callback = 1, quarantine_symlinks( ≥ 2)
5. Plan 02 acceptance criteria still pass (JOB_TOOL_REGISTRY contains unblob + binwalk_extract; env constants intact)
6. Wave 0 test_extract_monitor.py collects with zero errors (RED state preserved — bodies are still placeholders, will be filled in Plan 05)
</success_criteria>

<output>
After completion, create `.planning/phases/10-extraction-tier/10-03-SUMMARY.md` documenting:
- LoC added to extraction.py (final total file LoC)
- Confirmation of all 6 grep-based threat-model mitigations
- _du_sb hardlink dedup verification result (test with hardlinked files showing single-count)
- Any deviations from the action block's template (with rationale per Rule 1/2/3)
</output>
