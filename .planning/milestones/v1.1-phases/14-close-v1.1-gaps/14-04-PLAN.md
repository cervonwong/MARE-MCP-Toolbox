---
phase: 14
plan: 04
type: execute
wave: 2
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - .planning/phases/07-run-shell-typed-static-wrappers-re-artifacts/07-VERIFICATION.md
  - .planning/phases/08-session-scoped-r2/08-VERIFICATION.md
  - .planning/phases/10-extraction-tier/10-VERIFICATION.md
  - .planning/phases/11-dynamic-lab-mode-env-gated/11-VERIFICATION.md
  - .planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VERIFICATION.md
  - .planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md
autonomous: false
requirements:
  - HARDEN-03

must_haves:
  truths:
    - "Container rebuilt from clean source with Plan 01 test fixes baked in; image SHA / build date recorded"
    - "All 15 outstanding human-verification items from .planning/v1.1-MILESTONE-AUDIT.md are executed in the rebuilt container and recorded with timestamps + ≥10 lines of transcript"
    - "Each phase's VERIFICATION.md (07, 08, 10, 11, 13) gains a `## Live UAT Results (Phase 14 closure)` section with the appropriate item subset"
    - "Phase 13 HARDEN-03 live r2 sandbox arm is closed: a live r2 session inside the rebuilt container reports `e cfg.sandbox` returning `true`"
    - "Final audit re-run `/gsd-audit-milestone v1.1` returns `status: passed` with `gaps: []`; full output recorded in `.planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md`"
    - "v1.1 milestone is archive-ready: 0 audit gaps; 0 pytest failures; planning artifacts in agreement with verification artifacts"
  artifacts:
    - path: ".planning/phases/07-run-shell-typed-static-wrappers-re-artifacts/07-VERIFICATION.md"
      provides: "3 Phase-7 live UAT items recorded"
      contains: "Live UAT Results (Phase 14 closure)"
    - path: ".planning/phases/08-session-scoped-r2/08-VERIFICATION.md"
      provides: "2 Phase-8 live UAT items recorded"
      contains: "Live UAT Results (Phase 14 closure)"
    - path: ".planning/phases/10-extraction-tier/10-VERIFICATION.md"
      provides: "4 Phase-10 live UAT items recorded"
      contains: "Live UAT Results (Phase 14 closure)"
    - path: ".planning/phases/11-dynamic-lab-mode-env-gated/11-VERIFICATION.md"
      provides: "5 Phase-11 live UAT items recorded"
      contains: "Live UAT Results (Phase 14 closure)"
    - path: ".planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VERIFICATION.md"
      provides: "1 Phase-13 live HARDEN-03 sandbox arm recorded"
      contains: "cfg.sandbox=true"
    - path: ".planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md"
      provides: "Verbatim output of /gsd-audit-milestone v1.1 re-run + status:passed verdict"
      contains: "status: passed"
      min_lines: 20
  key_links:
    - from: "rebuilt container image"
      to: "Plan 01 test fixes (D-01/D-02/D-04)"
      via: "Dockerfile + mcp-gateway/ COPY → container build pulls the fixed source"
      pattern: "Plan 01 commit SHA"
    - from: "5 VERIFICATION.md files"
      to: "the 15 audit-listed human-verification items (.planning/v1.1-MILESTONE-AUDIT.md human_verification_outstanding)"
      via: "Each item recorded with date, container build, command, outcome, ≥10-line transcript per D-13 format"
      pattern: "## Live UAT Results \\(Phase 14 closure\\)"
    - from: ".planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md"
      to: "/gsd-audit-milestone v1.1 output"
      via: "Captured verbatim from the audit re-run; success oracle"
      pattern: "status: passed"
---

<objective>
Execute the final two locked decisions of the phase:

1. **D-12 / D-13 — Live container UAT (15 items):** Rebuild the container with Plan 01's test fixes baked in, then execute each of the 15 outstanding human-verification items from `.planning/v1.1-MILESTONE-AUDIT.md` (`human_verification_outstanding`) inside the rebuilt container. Append a `## Live UAT Results (Phase 14 closure)` section to each affected phase's `VERIFICATION.md` with the items belonging to that phase. Recording format per D-13: each entry includes item description, command run, ISO-8601 UTC timestamp, container image SHA or build date, outcome, and ≥10-line transcript (truncated with `…` only when output exceeds 200 lines).

2. **D-14 — Audit re-run gate (success oracle):** Re-run `/gsd-audit-milestone v1.1`. The phase is NOT complete until the audit returns `status: passed` with `gaps: []`. Capture the full audit output verbatim to `.planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md`.

**Why wave 2 + autonomous: false:**
- Wave 2 because the container MUST include Plan 01's test fixes — running UAT on a pre-fix container would record stale behaviour. Plans 02 and 03 also live in wave 1 (they don't block UAT) — but listing `depends_on: [01, 02, 03]` ensures Plan 04 sees ALL state synchronisation finished before audit re-run.
- `autonomous: false` because (a) some items (e.g., MCP Resources visible to a remote MCP client, gdb MI allowlist runtime enforcement, qemu round-trip) require interactive remote MCP client roundtrips that an executor cannot fully script; (b) D-13 recording format requires copying ≥10 lines of REAL transcript output that the executor will gather interactively.

**Item subset assignment by phase (verbatim from audit):**

- **Phase 7 (3 items):** mare-shell UID + ACL revocations; 100 MB run_shell /dev/urandom slow test; MCP Resources visible to a remote MCP client.
- **Phase 8 (2 items):** r2-gated test suite in container; gateway shutdown leaves no zombie r2 processes.
- **Phase 10 (4 items):** remote-client recursive triage chain (`run_binwalk extract` → `list_extracted_files` → `promote_extracted_sample`); archive-bomb cap aborts mid-extraction; `probe_extraction_tools.sh` READY verdict; three slow extraction integration tests pass.
- **Phase 11 (5 items):** `tools/list` returns 61 under `--remote --dynamic`; `get_dynamic_capabilities` + `run_strace` end-to-end; strace/ltrace/qemu slow JOBS integration tests; gdb MI allowlist runtime enforcement; `probe_dynamic_tools.sh` READY verdict.
- **Phase 13 (1 item):** Live r2 session reports `e cfg.sandbox=true` (closes HARDEN-03 live arm).

Purpose: Close the milestone's last open gap (audit-listed human verification) and gate the milestone as archive-ready.

Output: 5 VERIFICATION.md files updated with appended UAT-results sections; 1 new audit-rerun capture file; final `/gsd-audit-milestone v1.1` output showing `status: passed`. v1.1 ready to archive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-close-v1.1-gaps/14-CONTEXT.md
@.planning/phases/14-close-v1.1-gaps/14-RESEARCH.md
@.planning/phases/14-close-v1.1-gaps/14-VALIDATION.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/07-run-shell-typed-static-wrappers-re-artifacts/07-VERIFICATION.md
@.planning/phases/08-session-scoped-r2/08-VERIFICATION.md
@.planning/phases/10-extraction-tier/10-VERIFICATION.md
@.planning/phases/11-dynamic-lab-mode-env-gated/11-VERIFICATION.md
@.planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VERIFICATION.md
@docker-compose.yml
@run_docker.sh
@mcp-gateway/probe_extraction_tools.sh
@mcp-gateway/probe_dynamic_tools.sh

<interfaces>
**Per-item recording format (D-13 verbatim from CONTEXT.md):**

```markdown
## Live UAT Results (Phase 14 closure)

### [Item description copied verbatim from audit's human_verification_outstanding list]
- **Date:** 2026-05-DDTHH:MM:SSZ
- **Container build:** mare-toolbox:sha-<image-sha-or-build-date>
- **Command:** `<exact command line invoked, including args>`
- **Outcome:** passed | failed | partial
- **Transcript (≥10 lines, truncated with `…` only when output > 200 lines):**
  ```
  <verbatim captured stdout/stderr>
  ```
- **Notes (optional):** <e.g., environmental caveat, dependency, retry observed>
```

**Container rebuild recipe (exact recipe to be confirmed against `docker-compose.yml`):**

The CONTEXT.md says "exact recipe to be confirmed against docker-compose.yml". The canonical project pattern is:
```
docker compose build --no-cache && docker compose up -d
```
Confirm by reading `docker-compose.yml` for service name (e.g., `gateway-agent`); the actual exact recipe goes into the SUMMARY for traceability.

**Audit re-run command (D-14):**

```
/gsd-audit-milestone v1.1
```

Acceptance: top-of-output `status: passed`, `gaps: []` block empty. Anything else → STOP; route back to the specific gap raised.

**Threat-relevant scrubbing reminder:**

Per the planning_context security threat note: live UAT transcripts could capture sensitive paths or tokens. Before pasting any transcript snippet into VERIFICATION.md:
1. Strip MCP_GATEWAY_TOKEN and any bearer-token-style hex strings (regex: `[a-f0-9]{32,}` ← review case-by-case).
2. Strip absolute filesystem paths revealing host home directory (`/home/<user>/...` → keep only the project-relative subpath).
3. Keep r2 / strace / gdb command output verbatim (those are the proof) but redact any user-identifying preamble.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Container rebuild (Plan 01 fixes baked in)</name>
  <what-built>
    Plan 01 landed three surgical test-fix commits (Tasks 1-3) that change `mcp-gateway/src/`. For UAT to record post-fix behaviour, the container image MUST be rebuilt from the post-Plan-01 source tree. The Phase 5 image-hash fix means the rebuild will be triggered automatically by `./run_docker.sh`, but D-12 explicitly calls for a `--no-cache` rebuild to eliminate any layer-cache drift.
  </what-built>
  <how-to-verify>
    1. Confirm Plan 01 is committed and clean: `git log --oneline -5 -- mcp-gateway/` should show the test-fix commits.
    2. Inspect `docker-compose.yml` to identify the service name (typically `gateway-agent` or similar — confirm). The canonical rebuild recipe is:
       ```
       docker compose build --no-cache
       docker compose up -d
       ```
       OR using the project's wrapper:
       ```
       ./run_docker.sh --remote
       # which internally triggers a build if DOCKERFILE_SHA changed (it has, post-Plan-01)
       ```
    3. Capture the resulting image SHA / build date:
       ```
       docker images mare-toolbox --format '{{.Repository}}:{{.Tag}} {{.ID}} {{.CreatedAt}}'
       ```
       Record this string — it becomes the `**Container build:**` field in every D-13 UAT entry.

    4. Smoke-check that the rebuilt container has the Plan 01 fixes:
       ```
       docker compose exec gateway-agent grep -c "Phase 14 D-01" /opt/mcp-gateway/src/mcp_gateway/tools/r2_sessions.py
       # Should return >= 1
       docker compose exec gateway-agent grep -c "Phase 14 D-02" /opt/mcp-gateway/src/mcp_gateway/sessions/__init__.py
       # Should return >= 1
       ```
       (Adjust path if mcp-gateway lives elsewhere in the container — see Dockerfile COPY directive.)
  </how-to-verify>
  <resume-signal>Type "container-built" with the image-SHA-or-tag string, OR describe the build failure.</resume-signal>
  <files>(host action — no source files modified by this task; container image artifact only)</files>
  <action>
    Execute the rebuild and smoke-check workflow described in <what-built> and <how-to-verify> above:

    1. Confirm Plan 01 committed: `git log --oneline -5 -- mcp-gateway/` shows the test-fix commits from Plan 01 (D-01 + D-02 + D-04 fixes landed).
    2. Run the rebuild: `docker compose build --no-cache` then `docker compose up -d` (preferred — matches D-12 explicit `--no-cache` requirement). Alternative wrapper: `./run_docker.sh --remote` (DOCKERFILE_SHA changed post-Plan-01 so it triggers rebuild automatically, but lacks the `--no-cache` guarantee).
    3. Capture the image identity: `docker images mare-toolbox --format '{{.Repository}}:{{.Tag}} {{.ID}} {{.CreatedAt}}'` — record the output string verbatim; it becomes the `**Container build:**` field for every UAT entry in Task 2.
    4. Smoke-check Plan 01 fixes are baked in via the two grep commands shown in <how-to-verify> step 4. Both grep counts MUST be >= 1.
    5. Resume-signal: report `container-built <image-sha-or-tag>` to the user so Task 2 can begin.
  </action>
  <verify>
    <automated>docker images mare-toolbox --format '{{.ID}} {{.CreatedAt}}' | head -1 | awk 'NF>0{exit 0}{exit 1}'</automated>
  </verify>
  <done>
    Container rebuilt with Plan 01 fixes baked in; image SHA / tag / CreatedAt captured; smoke-grep proves D-01 + D-02 markers present inside the container; resume-signal posted with image identity for Task 2 to consume.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute 15 live UAT items + record in VERIFICATION.md files (D-12, D-13)</name>
  <what-built>
    Container is freshly built and running. The user (executor) is now positioned to drive the 15 audit-listed items against the live container — some via `docker compose exec` (in-container), some via a remote MCP client (Claude Code on host, mastra.ai). Each item gets recorded as a structured entry under a new `## Live UAT Results (Phase 14 closure)` section in the appropriate phase's `VERIFICATION.md`.
  </what-built>
  <how-to-verify>
    **Plan the work by phase. Iterate through the 15 items in this order (recommended dependency-respecting order):**

    **A. Phase 7 (3 items) → append to `.planning/phases/07-run-shell-typed-static-wrappers-re-artifacts/07-VERIFICATION.md`:**

    1. **mare-shell UID + ACL revocations**
       - Command: `docker compose exec gateway-agent id mare-shell && docker compose exec gateway-agent getfacl /agent/uploads | head -20`
       - Expected: `uid=700(mare-shell) gid=700(mare-shell)` and `user:mare-shell:r-x` on /agent/uploads
    2. **100 MB run_shell /dev/urandom slow test**
       - Command (from remote MCP client OR `docker compose exec`): trigger `run_shell` against `dd if=/dev/urandom of=/dev/null bs=1M count=100` with a 100 MB output cap; OR run the slow pytest:
         ```
         docker compose exec gateway-agent bash -lc 'cd /opt/mcp-gateway && uv run pytest -m slow tests/test_run_shell.py -k urandom -q'
         ```
       - Expected: completes within timeout; bounded RSS; head-truncated output preview; `tool-logs/` captures the full transcript.
    3. **MCP Resources visible to a remote MCP client**
       - Command: from host's Claude Code or mastra.ai client connected to the container's MCP endpoint, list resources matching `mare://cases/<case>/tool-logs/<file>`.
       - Expected: at least one resource URI visible; resource content fetchable.

    **B. Phase 8 (2 items) → append to `.planning/phases/08-session-scoped-r2/08-VERIFICATION.md`:**

    4. **Container r2-gated test suite**
       - Command: `docker compose exec gateway-agent bash -lc 'cd /opt/mcp-gateway && uv run pytest -m r2_required -q'`
       - Expected: the previously-skipped r2-gated tests in `test_r2_sessions.py` + `test_sessions.py` flip from SKIP → PASS.
    5. **Gateway shutdown leaves no zombie r2 processes**
       - Command: start gateway, open 2 r2 sessions via MCP client, then SIGTERM the gateway, then:
         ```
         docker compose exec gateway-agent ps -ef | grep -c radare2
         ```
       - Expected: 0 r2 processes alive post-shutdown.

    **C. Phase 10 (4 items) → append to `.planning/phases/10-extraction-tier/10-VERIFICATION.md`:**

    6. **Remote-client recursive triage chain**
       - Drive via remote MCP client: `run_binwalk(mode=extract)` → `list_extracted_files` → `promote_extracted_sample` on a known archive (e.g., a sample tar.gz with embedded files).
       - Expected: child extracted; promoted to a new case dir with new sha256 content addressing.
    7. **Archive-bomb cap aborts mid-extraction**
       - Inside container: feed a crafted bomb (e.g., `dd if=/dev/zero bs=1M count=8192 | gzip` × N nested) to `run_unblob` or `run_binwalk`; verify `MCP_GATEWAY_MAX_EXTRACT_MB` (default 4 GB) abort path fires.
       - Expected: job status `killed_log_cap` or `killed_extract_cap`; transcript shows the cap log line.
    8. **probe_extraction_tools.sh READY verdict**
       - Command: `docker compose exec gateway-agent bash /opt/mcp-gateway/probe_extraction_tools.sh`
       - Expected: ends with `READY` (binwalk3 + unblob + upx present).
    9. **Three slow extraction integration tests**
       - Command: `docker compose exec gateway-agent bash -lc 'cd /opt/mcp-gateway && uv run pytest -m slow tests/test_extraction_*.py -q'`
       - Expected: 3 slow tests pass.

    **D. Phase 11 (5 items) → append to `.planning/phases/11-dynamic-lab-mode-env-gated/11-VERIFICATION.md`:**

    10. **tools/list returns 61 under --remote --dynamic**
        - Command: start container with `./run_docker.sh --remote --dynamic`; from remote MCP client call `tools/list` (or via curl + bearer token).
        - Expected: tools count = 61.
    11. **get_dynamic_capabilities + run_strace end-to-end**
        - Drive via remote MCP client: call `get_dynamic_capabilities()` then `run_strace(case_dir, sample, profile="file_network_process")`.
        - Expected: capabilities report includes ptrace_scope + binfmt + qemu archs; strace output captured to `case_dir/dynamic/`.
    12. **strace/ltrace/qemu slow JOBS integration tests**
        - Command: `docker compose exec gateway-agent bash -lc 'MCP_GATEWAY_DYNAMIC_TOOLS=1 cd /opt/mcp-gateway && uv run pytest -m slow tests/test_dynamic_jobs.py -q'` (path may vary — locate via `find tests/ -name '*dynamic*'`).
        - Expected: pass.
    13. **gdb MI allowlist runtime enforcement**
        - Open gdb MI session (`open_gdb_session`); attempt a non-allowlisted command (e.g., `python print(1)`); expect rejection.
        - Expected: ValueError-shaped tool error dict; session NOT crashed.
    14. **probe_dynamic_tools.sh READY verdict**
        - Command: `docker compose exec gateway-agent bash /opt/mcp-gateway/probe_dynamic_tools.sh`
        - Expected: ends with `READY` (strace + ltrace + qemu-user-static + gdb-multiarch present + ptrace_scope check).

    **E. Phase 13 (1 item) → append to `.planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VERIFICATION.md`:**

    15. **HARDEN-03 live r2 sandbox arm**
        - Inside container: open an r2 session via `open_r2_session` (or `docker compose exec gateway-agent bash -lc 'cd /opt/mcp-gateway && uv run python -c "import asyncio; ..."'`); execute `e cfg.sandbox`.
        - Expected: output line `true`.
        - Closes the audit-recorded `HARDEN-03 / partial / argv-order unit test passes, but live r2 cfg.sandbox positive control is skipped on host` gap.

    **Recording protocol — for EACH of the 15 items:**

    a. Append the new section header `## Live UAT Results (Phase 14 closure)` to the target VERIFICATION.md (one header per file; multiple items go under it as `### <item description>` sub-headings).

    b. Use the D-13 exact format (see `<interfaces>` block above) for each item. Required fields: Date (ISO-8601 UTC), Container build (image SHA / tag from Task 1 resume-signal), Command (exact line), Outcome, Transcript (≥10 lines, no host-path leakage).

    c. Scrub the transcript per the security mitigation in `<interfaces>`: redact bearer-token-style hex strings, replace `/home/<user>/...` paths with project-relative paths.

    d. If ANY item fails (outcome=failed), STOP and route back to the implementing phase's plan (e.g., a Phase 11 dynamic-mode failure → consider whether the gap is environmental or a real regression).

    e. After all 15 items recorded, run a verification grep:
       ```
       grep -c "## Live UAT Results (Phase 14 closure)" .planning/phases/{07,08,10,11,13}-*/0[0-9]-VERIFICATION.md
       # Expected: 5 (one per file)
       grep -c "^### " .planning/phases/{07,08,10,11,13}-*/0[0-9]-VERIFICATION.md | awk -F: '{s+=$2}END{print s}'
       # Expected: ≥ 15 (one heading per item; note some files have pre-existing ### headings, so the actual sum will be larger)
       ```
  </how-to-verify>
  <resume-signal>Type "uat-complete" with a one-line summary (e.g., "15/15 passed, 0 failed"), OR describe which items failed and the diagnostic next step.</resume-signal>
  <files>
    .planning/phases/07-run-shell-typed-static-wrappers-re-artifacts/07-VERIFICATION.md
    .planning/phases/08-session-scoped-r2/08-VERIFICATION.md
    .planning/phases/10-extraction-tier/10-VERIFICATION.md
    .planning/phases/11-dynamic-lab-mode-env-gated/11-VERIFICATION.md
    .planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VERIFICATION.md
  </files>
  <action>
    Execute and record all 15 UAT items as detailed in <what-built> and <how-to-verify> above. The five phase blocks (A through E) drive the work in the order Phase 7 (3 items) → Phase 8 (2) → Phase 10 (4) → Phase 11 (5) → Phase 13 (1). For each item:

    1. Run the exact command shown in the matching sub-bullet of <how-to-verify>.
    2. Capture ISO-8601 UTC timestamp, container build (from Task 1 resume-signal), exact command, outcome (passed/failed/partial), and ≥10 lines of transcript.
    3. Scrub transcript per the security mitigation in <interfaces>: redact bearer-token-style hex `[a-f0-9]{32,}` matches and replace `/home/<user>/...` with project-relative paths. Keep r2/strace/gdb command output verbatim.
    4. Append the entry under a `## Live UAT Results (Phase 14 closure)` section in the appropriate VERIFICATION.md file (5 files total).
    5. Phase 13 item 15 (HARDEN-03 live arm): the transcript MUST contain the literal sequence `e cfg.sandbox` then `true` on consecutive lines (the live proof the sandbox latched at argv-eval time).
    6. If any item fails, halt and route back to the responsible plan; do NOT mass-commit partial UAT results.
    7. After all 15 pass, commit the 5 VERIFICATION.md files together with message: `Record Phase 14 closure: 15 live UAT items across phases 7/8/10/11/13`.
  </action>
  <verify>
    <automated>bash -c "test $(grep -l '## Live UAT Results (Phase 14 closure)' .planning/phases/{07,08,10,11,13}-*/0[0-9]-VERIFICATION.md | wc -l) -eq 5 && grep -c 'cfg.sandbox' .planning/phases/13-*/13-VERIFICATION.md | awk -F: '{s+=\$2}END{exit (s>=1)?0:1}'"</automated>
  </verify>
  <done>
    Five VERIFICATION.md files each carry a `## Live UAT Results (Phase 14 closure)` section; all 15 audit items recorded with D-13-compliant entries; HARDEN-03 live arm transcript contains `e cfg.sandbox` then `true`; transcripts scrubbed; commit landed scoped to exactly the 5 VERIFICATION.md files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Audit re-run gate (D-14 success oracle)</name>
  <read_first>
    - .planning/v1.1-MILESTONE-AUDIT.md (the audit that surfaced the 15-item gap — its gap blocks must all be closed by now)
    - .planning/phases/14-close-v1.1-gaps/14-CONTEXT.md (D-14 success oracle locked decision)
    - .planning/REQUIREMENTS.md (post-Plan-02 state)
    - .planning/ROADMAP.md (post-Plan-03 state)
    - .planning/STATE.md (post-Plan-03 state)
    - All 4 VALIDATION.md files (post-Plan-03 state)
    - All 5 VERIFICATION.md files (post-Task-2 state with Live UAT Results)
  </read_first>
  <files>.planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md</files>
  <action>
    1. Run the audit re-run command:
       ```
       /gsd-audit-milestone v1.1
       ```
       (This is a Claude Code slash command; the executor invokes it from the project root.)

    2. Capture the FULL audit output verbatim to `.planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md`. Structure the file as:
       ```markdown
       # Phase 14 — Audit Re-Run Capture (D-14 Success Oracle)

       **Date:** 2026-05-DDTHH:MM:SSZ
       **Command:** `/gsd-audit-milestone v1.1`
       **Status:** passed
       **Gaps:** []

       ## Full Audit Output (verbatim)

       ```yaml
       <paste the full audit YAML/markdown output verbatim>
       ```

       ## Verdict

       v1.1 is archive-ready. All 9 implementation phases verified; all 61 requirements traced; 0 gaps remaining; full non-slow pytest suite green; 15 live UAT items recorded across phases 7/8/10/11/13.
       ```

    3. The acceptance gate is BINARY:
       - **PASS:** `status: passed` AND `gaps: []` in the audit output → milestone closure ready.
       - **FAIL:** any other status, or non-empty `gaps:` block → STOP. Identify the gap from the audit output, route back to the implementing plan (e.g., a missed traceability row → Plan 02; a missed UAT item → Task 2 of this plan), close the gap, and re-run the audit.

    4. If PASS: commit `.planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md` with message: `Close Phase 14: audit re-run status passed; v1.1 archive-ready`.

    5. If FAIL: do NOT commit a "passed" audit-rerun file. Record the actual failed output to a draft file (e.g., `14-AUDIT-RERUN.draft.md`) and surface the gap for the next iteration.
  </action>
  <verify>
    <automated>bash -c "test -f .planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md && grep -c 'status: passed' .planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md | awk '{exit (\$1>=1)?0:1}'"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md` exists with at least 20 lines
    - `grep -c "^status: passed" .planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md` returns at least 1
    - `grep -c "^gaps: \[\]" .planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md` returns at least 1 (or `gaps:` followed by a literally empty list block)
    - `grep -c "## Live UAT Results (Phase 14 closure)" .planning/phases/{07,08,10,11,13}-*/0[0-9]-VERIFICATION.md` returns 5 (one per phase file)
    - `grep -cE "cfg\.sandbox.*true" .planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VERIFICATION.md` returns at least 1 (HARDEN-03 live arm closed)
    - `git log --oneline -1 -- .planning/phases/14-close-v1.1-gaps/14-AUDIT-RERUN.md` shows the closure commit
    - Final regression sanity: `cd mcp-gateway && uv run pytest -m 'not slow' 2>&1 | tail -1 | grep "0 failed"` still passes (Plan 01's invariant holds end-to-end)
  </acceptance_criteria>
  <done>
    `/gsd-audit-milestone v1.1` returns `status: passed` with `gaps: []`; full output captured to 14-AUDIT-RERUN.md; commit landed; v1.1 milestone archive-ready.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container ↔ host filesystem | UAT transcripts cross this boundary — captured output may contain host-side paths or environment variables |
| MCP bearer token ↔ planning artifact | Live MCP client calls during UAT use the bearer token; transcripts must not capture/leak it |
| audit-output ↔ planning truth | The audit re-run is the success oracle — recording false success would invalidate the entire phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04-01 | Information Disclosure (token / path leak in transcripts) | VERIFICATION.md Live UAT Results sections | medium | mitigate | Task 2 step c explicit transcript-scrubbing protocol: redact bearer-token-style hex (regex `[a-f0-9]{32,}`), replace `/home/<user>/...` with project-relative paths, keep r2/strace/gdb output verbatim (those are the proof). Pre-commit grep can defensively check: `grep -E "[a-f0-9]{32,}" .planning/phases/{07,08,10,11,13}-*/0[0-9]-VERIFICATION.md` should return 0 after scrubbing. |
| T-14-04-02 | Repudiation (false-positive audit pass) | 14-AUDIT-RERUN.md status field | high | mitigate | Task 3 binary gate: file is committed ONLY if `status: passed` is verbatim from `/gsd-audit-milestone v1.1` output. Step 5 prescribes a draft suffix for failed runs to prevent accidental committed false-positive. Reviewer (human) sees the verbatim audit output and can detect tampering. |
| T-14-04-03 | DoS (container rebuild failure mid-UAT) | Task 1 container rebuild | low | accept | Standard `docker compose build --no-cache` — if it fails, Task 1's resume-signal flow surfaces the failure and the user re-tries. No security impact. |
| T-14-04-04 | Tampering (UAT item skipped / mis-recorded) | VERIFICATION.md item entries | medium | mitigate | Task 2 step e final grep enforces ≥ 15 `### ` sub-headings (one per item); Task 3 acceptance grep enforces 5 `## Live UAT Results (Phase 14 closure)` headers. Audit re-run is the second-layer check — a missed/mis-recorded item that affects the audit's verdict would surface as a gap in the re-run output. |
</threat_model>

<verification>
Plan-04 verification chain:

1. **Container rebuild evidence:** Task 1 resume-signal contains an image-SHA-or-tag string; smoke greps confirm Plan 01 fixes are baked in.
2. **15 UAT items recorded:** 5 VERIFICATION.md files each have a `## Live UAT Results (Phase 14 closure)` section; total of ≥ 15 sub-item headings; transcripts scrubbed of bearer tokens and host paths.
3. **HARDEN-03 live arm:** 13-VERIFICATION.md UAT section contains `cfg.sandbox` literal `true` in a recorded transcript.
4. **Audit re-run:** `/gsd-audit-milestone v1.1` output captured to 14-AUDIT-RERUN.md; contains `status: passed` and `gaps: []`.
5. **Full-suite invariant preserved:** Final `cd mcp-gateway && uv run pytest -m 'not slow'` still shows `0 failed`.
6. **Per-commit scope:** UAT recording commits and audit-rerun commit are separate; no commit mixes UAT capture with audit output.
</verification>

<success_criteria>
- Container rebuilt --no-cache from post-Plan-01 source; image SHA/tag recorded
- 15 UAT items executed and recorded across 5 VERIFICATION.md files with D-13 format
- Transcripts scrubbed: no bearer tokens, no host home-directory paths
- HARDEN-03 live r2 sandbox arm closed (transcript shows `e cfg.sandbox` → `true`)
- `/gsd-audit-milestone v1.1` returns `status: passed` with `gaps: []`
- 14-AUDIT-RERUN.md committed with verbatim audit output
- Final pytest gate `cd mcp-gateway && uv run pytest -m 'not slow'` still exits 0 with `0 failed`
- v1.1 milestone archive-ready
</success_criteria>

<output>
After completion, create `.planning/phases/14-close-v1.1-gaps/14-04-SUMMARY.md` documenting:
- Container image SHA/tag rebuilt at start
- One bullet per UAT item: phase + item description + outcome (passed/failed)
- Transcript scrub summary (e.g., "redacted 3 host-path references; no bearer tokens captured")
- HARDEN-03 live transcript line `e cfg.sandbox\ntrue` quoted verbatim (post-scrub)
- Audit re-run status + gaps list (should be `passed` + `[]`)
- Any deviation from the plan with Rule-1/2/3 markers
- Final v1.1 milestone-archive-ready statement
</output>
