---
phase: 14
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/05-f-1-image-hash-fix/05-VALIDATION.md
  - .planning/phases/06-retoolrunner-artifacts-io-foundation/06-VALIDATION.md
  - .planning/phases/12-orchestrator-skill-update/12-VALIDATION.md
  - .planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VALIDATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "ROADMAP.md Progress table rows for Phases 5, 6, 7, 8, 9 show Status=Complete and a real completion date sourced from each phase's VERIFICATION.md (not 2026-05-21 placeholder)"
    - "ROADMAP.md top-of-section v1.1 phase list shows [x] for Phases 5, 6, 7, 8, 9 (currently [ ])"
    - "STATE.md body 'Current Position' block matches frontmatter — Progress line shows 9/9 complete (not 0/8); Status line agrees with frontmatter status; 'Current focus' line either removed or updated to reference Phase 14 (the current closure phase)"
    - "VALIDATION.md frontmatter for phases 5, 6, 12, 13 each shows nyquist_compliant: true (precondition: each phase's VERIFICATION.md status is passed/PASS)"
    - "No new VALIDATION.md flag flips for phases whose VERIFICATION.md does NOT confirm passed (per D-11 explicit guard)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated progress table + v1.1 phase list checkboxes"
      contains: "Phase 5"
    - path: ".planning/STATE.md"
      provides: "Body text consistent with frontmatter (9/9 phases complete)"
      contains: "9/9"
    - path: ".planning/phases/05-f-1-image-hash-fix/05-VALIDATION.md"
      provides: "nyquist_compliant: true"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/06-retoolrunner-artifacts-io-foundation/06-VALIDATION.md"
      provides: "nyquist_compliant: true"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/12-orchestrator-skill-update/12-VALIDATION.md"
      provides: "nyquist_compliant: true"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VALIDATION.md"
      provides: "nyquist_compliant: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "ROADMAP.md Progress table dates"
      to: ".planning/phases/{05,06,07,08,09}-*/0X-VERIFICATION.md frontmatter `verified:` field"
      via: "Each date column value extracted (date-only, no time) from the matching VERIFICATION.md verified timestamp"
      pattern: "Phase [56789].*Complete.*2026-"
    - from: ".planning/STATE.md body progress line"
      to: ".planning/STATE.md frontmatter progress block"
      via: "Both must read 9/9; body must NOT contradict the frontmatter authoritative count"
      pattern: "9/9 phases complete"
    - from: ".planning/phases/{05,06,12,13}-*/0X-VALIDATION.md frontmatter"
      to: ".planning/phases/{05,06,12,13}-*/0X-VERIFICATION.md status"
      via: "Flip nyquist_compliant: true only when matching VERIFICATION.md status is passed/PASS"
      pattern: "nyquist_compliant: true"
---

<objective>
Sync three classes of planning-state drift identified by the v1.1 audit (`.planning/v1.1-MILESTONE-AUDIT.md` section "Planning state drift"):

1. **D-09 — ROADMAP.md progress sync:** Mark Phases 5, 6, 7, 8, 9 as `Complete` in the progress table with their REAL completion dates extracted from each phase's `VERIFICATION.md` frontmatter `verified:` field. Also flip the top-of-section v1.1 checklist checkboxes for Phases 5-9 from `[ ]` to `[x]` (currently four show `[ ]` even though their plans are all complete).

2. **D-10 — STATE.md body sync:** Update body text to match frontmatter. The frontmatter already declares `completed_phases: 9` and `percent: 100`, but the body's "Current Position" block (around lines 27-35) still shows `Progress: [          ] 0% (0/8 phases complete)` and `Current focus: Phase 13 — harden-concurrency-caps-and-r2-sandboxing`. Update body to reflect the current reality (9 phases complete, Phase 14 in progress as the closure phase).

3. **D-11 — VALIDATION.md frontmatter sync:** Flip `nyquist_compliant: false` → `nyquist_compliant: true` in the frontmatter of `05-VALIDATION.md`, `06-VALIDATION.md`, `12-VALIDATION.md`, `13-VALIDATION.md`. Per D-11 explicit guard: each flip requires the corresponding `*-VERIFICATION.md` status to be `passed` or `PASS`. Source-of-truth checks (confirmed during research):
   - `05-VERIFICATION.md` status: `passed` (4/4 must-haves)
   - `06-VERIFICATION.md` status: `passed` (5/5 success criteria)
   - `12-VERIFICATION.md` status: `passed` (5/5 must-haves)
   - `13-VERIFICATION.md` status: `PASS` (9/9 must-haves)
   All four pass the precondition — proceed with all four flips.

Purpose: Bring planning state into agreement with verification evidence. Plan 03 is wave-1 parallel with Plans 01 and 02 (no file overlap: Plan 01 only touches `mcp-gateway/`; Plan 02 only touches `REQUIREMENTS.md`; Plan 03 only touches `ROADMAP.md` + `STATE.md` + 4 VALIDATION.md files).

Output: 6 files modified — 1 ROADMAP, 1 STATE, 4 VALIDATION. No new content created (only field flips + table-cell updates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-close-v1.1-gaps/14-CONTEXT.md
@.planning/phases/14-close-v1.1-gaps/14-RESEARCH.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-f-1-image-hash-fix/05-VALIDATION.md
@.planning/phases/06-retoolrunner-artifacts-io-foundation/06-VALIDATION.md
@.planning/phases/12-orchestrator-skill-update/12-VALIDATION.md
@.planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VALIDATION.md
@.planning/phases/05-f-1-image-hash-fix/05-VERIFICATION.md
@.planning/phases/06-retoolrunner-artifacts-io-foundation/06-VERIFICATION.md
@.planning/phases/07-run-shell-typed-static-wrappers-re-artifacts/07-VERIFICATION.md
@.planning/phases/08-session-scoped-r2/08-VERIFICATION.md
@.planning/phases/09-background-job-system/09-VERIFICATION.md
@.planning/phases/12-orchestrator-skill-update/12-VERIFICATION.md
@.planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VERIFICATION.md

<interfaces>
**Current ROADMAP.md state (relevant lines):**

```
Lines 24-32 — v1.1 top phase list (currently all [ ] for 5-9):
- [ ] **Phase 5: F-1 Image-Hash Fix** ...
- [ ] **Phase 6: ReToolRunner + artifacts_io Foundation** ...
- [ ] **Phase 7: run_shell + Typed Static Wrappers + re_artifacts** ...
- [ ] **Phase 8: Session-Scoped r2** ...
- [ ] **Phase 9: Background Job System** ...
- [x] **Phase 10: Extraction Tier** — ... (completed 2026-05-19)
- [x] **Phase 11: Dynamic Lab Mode** ... (completed 2026-05-20)
- [x] **Phase 12: Orchestrator Skill Update** ... (completed 2026-05-20)
- [x] **Phase 13: Harden ...** (completed 2026-05-20)
- [ ] **Phase 14: Close v1.1 Milestone Gaps** ...

Lines 178-193 — Progress table (currently 5-9 say Not started; 10-13 say Complete):
| 5. F-1 Image-Hash Fix          | v1.1      | 0/3   | Not started | -          |
| 6. ReToolRunner Foundation     | v1.1      | 0/3   | Not started | -          |
| 7. run_shell + Static Wrappers | v1.1      | 0/?   | Not started | -          |
| 8. Session-Scoped r2           | v1.1      | 0/5   | Not started | -          |
| 9. Background Job System       | v1.1      | 0/?   | Not started | -          |
| 10. Extraction Tier            | v1.1      | 5/5 | Complete    | 2026-05-19 |
| 11. Dynamic Lab Mode           | v1.1      | 6/6 | Complete    | 2026-05-20 |
| 12. Orchestrator Skill Update  | v1.1      | 5/5 | Complete    | 2026-05-20 |
| 13. Hardening + r2 Sandboxing  | v1.1      | 4/4 | Complete    | 2026-05-20 |
| 14. Close v1.1 Gaps            | v1.1      | 0/? | Not started | -          |
```

**Verified dates extracted from each phase's VERIFICATION.md `verified:` frontmatter field (research-confirmed):**

```
Phase 5 → verified: 2026-05-12T14:30:00Z → 2026-05-12
Phase 6 → verified: 2026-05-13T00:00:00Z → 2026-05-13
Phase 7 → verified: 2026-05-13T05:04:48Z → 2026-05-13
Phase 8 → verified: 2026-05-18T00:00:00Z → 2026-05-18
Phase 9 → verified: 2026-05-19T03:26:38Z → 2026-05-19
```

**Current STATE.md body drift (lines 27-35):**

```
27: ## Current Position
28: 
29: Milestone: v1.1 Remote RE Tool Expansion
30: Phase: 13 (harden-concurrency-caps-and-r2-sandboxing) — EXECUTING
31: Plan: 4 of 4
32: Status: Phase complete — ready for verification
33: Last activity: 2026-05-21 - Completed quick task 260521-d6l: Phase 13 hot-fix r2 sandbox latching via stdin instead of argv
34: 
35: Progress: [          ] 0% (0/8 phases complete)
```

Line 35 contradicts frontmatter at lines 9-14: `completed_phases: 9, percent: 100`. Line 25 says `Current focus: Phase 13` — also stale.

**VALIDATION.md frontmatter shape (all 4 target files — keys and values, frontmatter fences omitted to avoid parser confusion):**

- `phase`: 5 (or 6, 12, 13)
- `slug`: f-1-image-hash-fix (or matching slug)
- `status`: draft
- `nyquist_compliant`: false  ← **FLIP THIS to true**
- `wave_0_complete`: false  ← Do NOT flip this (separate signal)
- `created`: 2026-05-XX

**VERIFICATION.md status sources (all four pass the D-11 precondition):**

```
05-VERIFICATION.md: status: passed                      ✅
06-VERIFICATION.md: status: passed                      ✅
12-VERIFICATION.md: status: passed                      ✅
13-VERIFICATION.md: status: PASS                        ✅
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync ROADMAP.md — v1.1 checkboxes + progress table dates (D-09)</name>
  <read_first>
    - .planning/ROADMAP.md (read lines 22-35 for v1.1 phase list AND lines 175-195 for progress table)
    - .planning/phases/05-f-1-image-hash-fix/05-VERIFICATION.md (verified date)
    - .planning/phases/06-retoolrunner-artifacts-io-foundation/06-VERIFICATION.md (verified date)
    - .planning/phases/07-run-shell-typed-static-wrappers-re-artifacts/07-VERIFICATION.md (verified date)
    - .planning/phases/08-session-scoped-r2/08-VERIFICATION.md (verified date)
    - .planning/phases/09-background-job-system/09-VERIFICATION.md (verified date)
    - .planning/phases/14-close-v1.1-gaps/14-CONTEXT.md (D-09 locked: real dates, NOT 2026-05-21 placeholder)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    **A. Re-confirm completion dates by direct read (defence against research drift):**

    For each of Phases 5-9, run:
    ```
    grep "^verified:" .planning/phases/0{5,6,7,8,9}-*/0[5-9]-VERIFICATION.md
    ```
    Extract the date portion (YYYY-MM-DD) from each `verified:` field. Per research, the dates are:
    - Phase 5: 2026-05-12
    - Phase 6: 2026-05-13
    - Phase 7: 2026-05-13
    - Phase 8: 2026-05-18
    - Phase 9: 2026-05-19

    If any direct grep returns a date that DISAGREES with this list, use the actual date from the grep, NOT the research-recorded value.

    **B. Flip top-of-section v1.1 checklist checkboxes (lines 24-32):**

    Replace these 5 lines exactly (preserve all wording after the checkbox):

    ```
    - [ ] **Phase 5: F-1 Image-Hash Fix** — ...
    - [ ] **Phase 6: ReToolRunner + artifacts_io Foundation** — ...
    - [ ] **Phase 7: run_shell + Typed Static Wrappers + re_artifacts** — ...
    - [ ] **Phase 8: Session-Scoped r2** — ...
    - [ ] **Phase 9: Background Job System** — ...
    ```
    With (only the `[ ]` becomes `[x]`; the rest of each line is preserved verbatim — re-read the line and substitute only the checkbox):

    ```
    - [x] **Phase 5: F-1 Image-Hash Fix** — ...
    - [x] **Phase 6: ReToolRunner + artifacts_io Foundation** — ...
    - [x] **Phase 7: run_shell + Typed Static Wrappers + re_artifacts** — ...
    - [x] **Phase 8: Session-Scoped r2** — ...
    - [x] **Phase 9: Background Job System** — ...
    ```

    **C. Update Progress table rows (lines 184-188):**

    Replace these 5 rows:
    ```
    | 5. F-1 Image-Hash Fix          | v1.1      | 0/3   | Not started | -          |
    | 6. ReToolRunner Foundation     | v1.1      | 0/3   | Not started | -          |
    | 7. run_shell + Static Wrappers | v1.1      | 0/?   | Not started | -          |
    | 8. Session-Scoped r2           | v1.1      | 0/5   | Not started | -          |
    | 9. Background Job System       | v1.1      | 0/?   | Not started | -          |
    ```
    With (substitute the Plans cell with the real plan count, Status → Complete, Completed → real date from step A):
    ```
    | 5. F-1 Image-Hash Fix          | v1.1      | 3/3   | Complete    | 2026-05-12 |
    | 6. ReToolRunner Foundation     | v1.1      | 3/3   | Complete    | 2026-05-13 |
    | 7. run_shell + Static Wrappers | v1.1      | 8/8   | Complete    | 2026-05-13 |
    | 8. Session-Scoped r2           | v1.1      | 5/5   | Complete    | 2026-05-18 |
    | 9. Background Job System       | v1.1      | 5/5   | Complete    | 2026-05-19 |
    ```
    Plan counts (Phase 7 = 8, Phase 9 = 5) come from ROADMAP.md Phase Details sections (Phase 7 Plans: 8 plans; Phase 9 Plans: 5 plans (4 original + 1 gap-closure)).

    Do NOT touch the Phase 10-14 rows — those are correct or out of scope.

    **D. Do NOT modify** the Phase 14 row (still `0/?` and `Not started` — Plan 04 / Plan 05 will close that out at end-of-phase).
  </action>
  <verify>
    <automated>grep -cE "Phase [5-9].*Complete.*2026-05-(12|13|18|19)" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*Phase 5:" .planning/ROADMAP.md` returns at least 1
    - `grep -c "^- \[x\] \*\*Phase 6:" .planning/ROADMAP.md` returns at least 1
    - `grep -c "^- \[x\] \*\*Phase 7:" .planning/ROADMAP.md` returns at least 1
    - `grep -c "^- \[x\] \*\*Phase 8:" .planning/ROADMAP.md` returns at least 1
    - `grep -c "^- \[x\] \*\*Phase 9:" .planning/ROADMAP.md` returns at least 1
    - `grep -c "^- \[ \] \*\*Phase [5-9]:" .planning/ROADMAP.md` returns 0 (no unchecked Phase 5-9 entries in top list)
    - `grep -cE "\| 5\. F-1.*Complete.*2026-05-12" .planning/ROADMAP.md` returns 1
    - `grep -cE "\| 6\. ReToolRunner.*Complete.*2026-05-13" .planning/ROADMAP.md` returns 1
    - `grep -cE "\| 7\. run_shell.*Complete.*2026-05-13" .planning/ROADMAP.md` returns 1
    - `grep -cE "\| 8\. Session-Scoped.*Complete.*2026-05-18" .planning/ROADMAP.md` returns 1
    - `grep -cE "\| 9\. Background Job.*Complete.*2026-05-19" .planning/ROADMAP.md` returns 1
    - `grep -cE "Phase [5-9].*Not started" .planning/ROADMAP.md` returns 0 (no Phase 5-9 rows still claim Not started in the progress table)
    - `grep -c "2026-05-21" .planning/ROADMAP.md | awk '$1==0{exit 0}{exit 1}'` — no 2026-05-21 placeholder dates introduced for Phases 5-9 (the audit specifically forbade this placeholder)
  </acceptance_criteria>
  <done>
    Top-of-section v1.1 phase list has [x] for Phases 5-9; progress table rows for Phases 5-9 show Complete with the real verified-date (per VERIFICATION.md frontmatter); no 2026-05-21 placeholder used; Phase 14 row untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync STATE.md body to match frontmatter (D-10)</name>
  <read_first>
    - .planning/STATE.md (read full file — frontmatter at lines 1-16 is authoritative; body at lines 27-35 is stale)
    - .planning/phases/14-close-v1.1-gaps/14-CONTEXT.md (D-10 locked decision)
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
    1. DO NOT modify the frontmatter. The frontmatter is the source of truth: `milestone: v1.1`, `status: verifying`, `completed_phases: 9`, `total_phases: 9`, `percent: 100`. Leave it intact.

    2. Locate the "Current Position" section. Replace lines 27-35 with the synchronised version:
    ```markdown
    ## Current Position

    Milestone: v1.1 Remote RE Tool Expansion
    Phase: 14 (close-v1.1-gaps) — EXECUTING (audit-closure phase)
    Plan: Phase 14 plans in flight (gap closure)
    Status: 9/9 implementation phases complete; verifying milestone via Phase 14 closure
    Last activity: 2026-05-21 - Phase 14 planning + execution underway to close audit gaps

    Progress: [##########] 100% (9/9 implementation phases complete; Phase 14 closure in progress)
    ```

    Specifically: the `Progress: [          ] 0% (0/8 phases complete)` line MUST become `Progress: [##########] 100% (9/9 implementation phases complete; Phase 14 closure in progress)` so it agrees with frontmatter `completed_phases: 9, percent: 100`.

    3. The block in the file may have additional context lines below line 35 — leave those untouched. Only the 8-line block at 27-35 changes.

    4. Search for any other body text that contradicts frontmatter. Specifically check:
    - Line 25 `**Current focus:** Phase 13` — update to `**Current focus:** Phase 14 (audit closure)`
    - Anywhere else in the body that mentions "Phase 13" as current, or "8 phases", or "0/8" — bring into agreement with frontmatter 9/9 + Phase 14 in flight.

    5. Do NOT delete the "Performance Metrics", "Accumulated Context", "Pending Todos", "Blockers/Concerns", "Quick Tasks Completed" sections — those are historical record. Their content is out of scope for D-10 (only the "Current Position" block and the one-line "Current focus" pointer are in scope).

    6. The "Session Continuity" block at the bottom (around lines 256-260) is auto-managed by gsd-tools — do NOT modify it manually.
  </action>
  <verify>
    <automated>bash -c "! grep -E '0/8 phases complete|^Progress: \[          \] 0%' .planning/STATE.md && grep -E '9/9' .planning/STATE.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "0/8 phases complete" .planning/STATE.md` returns 0 (stale text removed)
    - `grep -c "9/9 implementation phases complete" .planning/STATE.md` returns at least 1
    - `grep -c "Progress: \[##########\] 100%" .planning/STATE.md` returns at least 1
    - `grep -cE "^Phase: 14" .planning/STATE.md` returns at least 1 (Current Position now points at Phase 14)
    - `grep -c "Current focus.*Phase 14" .planning/STATE.md` returns at least 1
    - `grep -c "Current focus.*Phase 13" .planning/STATE.md` returns 0 (stale pointer removed)
    - Frontmatter `completed_phases: 9` is preserved: `grep -c "^completed_phases: 9$" .planning/STATE.md` returns 1
    - Frontmatter `percent: 100` is preserved: `grep -c "^  percent: 100$" .planning/STATE.md` returns 1
  </acceptance_criteria>
  <done>
    STATE.md body "Current Position" block matches frontmatter (9/9 phases complete; Phase 14 in flight); "Current focus" pointer updated; frontmatter untouched; Session Continuity and history sections preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip nyquist_compliant for VALIDATION.md frontmatter of phases 5, 6, 12, 13 (D-11)</name>
  <read_first>
    - .planning/phases/05-f-1-image-hash-fix/05-VERIFICATION.md (status: passed — precondition satisfied)
    - .planning/phases/06-retoolrunner-artifacts-io-foundation/06-VERIFICATION.md (status: passed)
    - .planning/phases/12-orchestrator-skill-update/12-VERIFICATION.md (status: passed)
    - .planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VERIFICATION.md (status: PASS)
    - .planning/phases/05-f-1-image-hash-fix/05-VALIDATION.md (frontmatter to flip)
    - .planning/phases/06-retoolrunner-artifacts-io-foundation/06-VALIDATION.md
    - .planning/phases/12-orchestrator-skill-update/12-VALIDATION.md
    - .planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VALIDATION.md
    - .planning/phases/14-close-v1.1-gaps/14-CONTEXT.md (D-11 + its explicit precondition guard)
  </read_first>
  <files>
    .planning/phases/05-f-1-image-hash-fix/05-VALIDATION.md
    .planning/phases/06-retoolrunner-artifacts-io-foundation/06-VALIDATION.md
    .planning/phases/12-orchestrator-skill-update/12-VALIDATION.md
    .planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VALIDATION.md
  </files>
  <action>
    **Per-file precondition check + flip. For EACH of the 4 phases (05, 06, 12, 13):**

    1. Run the precondition check against the matching VERIFICATION.md:
    ```
    grep -E "^status:\s*(passed|PASS)" .planning/phases/<phase>-*/0<NN>-VERIFICATION.md
    ```
    If this returns at least one matching line → precondition satisfied → proceed to step 2. If returns 0 lines → STOP for this file; record the discrepancy in SUMMARY; leave the file's frontmatter `nyquist_compliant: false`. Do NOT force the flip.

    Per CONTEXT.md interfaces (and a direct VERIFICATION.md grep at planning time):
    - 05 → `status: passed` ✅
    - 06 → `status: passed` ✅
    - 12 → `status: passed` ✅
    - 13 → `status: PASS` ✅

    All four pass — proceed with all four flips.

    2. In each VALIDATION.md, replace the single line `nyquist_compliant: false` with `nyquist_compliant: true`. The frontmatter is YAML; the line is one of the 6 frontmatter keys (between the two `---` fences at the top of the file). Use a targeted single-line substitution — do NOT rewrite the rest of the frontmatter or any post-frontmatter content.

    3. Do NOT modify `wave_0_complete` — that is a separate execution-time flag, also `false` in all 4 files. Flipping it requires actual Wave-0 work that didn't happen for these phases; per D-11 only `nyquist_compliant` is in scope.

    4. Do NOT modify `status: draft` in any VALIDATION.md — promoting status from `draft` to `validated`/`approved` is a separate concern owned by `/gsd-validate-phase`, not by this planning-state sync.

    5. After all 4 flips, run a single confirmation grep across all 4 files:
    ```
    grep -E "^nyquist_compliant: true" .planning/phases/{05,06,12,13}-*/0{5,6,12,13}-VALIDATION.md
    ```
    MUST return 4 lines (one per file).
  </action>
  <verify>
    <automated>bash -c "test $(grep -lE '^nyquist_compliant: true' .planning/phases/{05-f-1-image-hash-fix,06-retoolrunner-artifacts-io-foundation,12-orchestrator-skill-update,13-harden-concurrency-caps-and-r2-sandboxing}/*-VALIDATION.md | wc -l) -eq 4"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "^nyquist_compliant: true" .planning/phases/05-f-1-image-hash-fix/05-VALIDATION.md` returns the file path (flip landed in 05)
    - `grep -l "^nyquist_compliant: true" .planning/phases/06-retoolrunner-artifacts-io-foundation/06-VALIDATION.md` returns the file path
    - `grep -l "^nyquist_compliant: true" .planning/phases/12-orchestrator-skill-update/12-VALIDATION.md` returns the file path
    - `grep -l "^nyquist_compliant: true" .planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VALIDATION.md` returns the file path
    - For each of the 4 files, `grep -c "^nyquist_compliant: false" {file}` returns 0 (no remaining false flag)
    - For each of the 4 files, `grep -c "^wave_0_complete: false" {file}` returns 1 (NOT flipped — D-11 scope guard)
    - For each of the 4 files, `grep -c "^status: draft" {file}` returns 1 (NOT promoted — D-11 scope guard)
    - Precondition trail recorded: SUMMARY contains the verbatim grep output proving each phase's VERIFICATION.md status is passed/PASS before the flip
  </acceptance_criteria>
  <done>
    Phases 5, 6, 12, 13 VALIDATION.md frontmatter all show `nyquist_compliant: true`. `wave_0_complete` and `status` fields are unchanged (D-11 scope guard). Precondition trail recorded in SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 4: Cross-file consistency check + commit</name>
  <read_first>
    - .planning/ROADMAP.md (final state after Task 1)
    - .planning/STATE.md (final state after Task 2)
    - All four VALIDATION.md files (final state after Task 3)
  </read_first>
  <files>(no source edits — verification + commit only)</files>
  <action>
    1. **Cross-file agreement test:** Confirm ROADMAP, STATE, and VALIDATION all tell the same story about 9 completed implementation phases:
    ```
    # ROADMAP says Phases 5-13 are Complete (9 of them)
    grep -cE "Phase [5-9].*Complete|Phase 1[0-3].*Complete" .planning/ROADMAP.md
    # Should be ≥ 9

    # STATE body says 9/9 complete
    grep -c "9/9" .planning/STATE.md
    # Should be ≥ 1

    # 4 VALIDATION.md files have nyquist_compliant: true
    grep -l "^nyquist_compliant: true" .planning/phases/{05,06,07,08,09,10,11,12,13}-*/0[0-9]-VALIDATION.md | wc -l
    # Should be ≥ 4 (phases 5, 6, 12, 13 — phases 7, 8, 9, 10, 11 already true from prior plans)
    ```

    2. **Sanity grep for placeholder dates** (D-09 explicitly forbade 2026-05-21 placeholder):
    ```
    grep -E "Phase [5-9].*2026-05-21" .planning/ROADMAP.md
    ```
    Should return 0 lines.

    3. **Commit the 6 files together** with message: `Sync planning state — ROADMAP progress + STATE body + VALIDATION nyquist flags`. This is a per-workstream commit (CONTEXT.md discretion allows this); the test fix (Plan 01) and REQUIREMENTS sync (Plan 02) are intentionally separate commits.

    4. **Validation pass** — run `git status` to confirm exactly 6 files modified (no stray edits):
    ```
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/05-f-1-image-hash-fix/05-VALIDATION.md
    .planning/phases/06-retoolrunner-artifacts-io-foundation/06-VALIDATION.md
    .planning/phases/12-orchestrator-skill-update/12-VALIDATION.md
    .planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VALIDATION.md
    ```
  </action>
  <verify>
    <automated>bash -c "test $(grep -cE 'Phase [5-9].*Complete' .planning/ROADMAP.md) -ge 5 && test $(grep -c '9/9' .planning/STATE.md) -ge 1 && test $(grep -lE '^nyquist_compliant: true' .planning/phases/{05,06,12,13}-*/0[0-9]-VALIDATION.md | wc -l) -eq 4"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "Phase [5-9].*Complete" .planning/ROADMAP.md` returns at least 5 (Phases 5, 6, 7, 8, 9 all Complete in progress table)
    - `grep -c "9/9" .planning/STATE.md` returns at least 1
    - `grep -lE "^nyquist_compliant: true" .planning/phases/{05,06,12,13}-*/0[0-9]-VALIDATION.md | wc -l` returns exactly 4
    - `grep -cE "Phase [5-9].*2026-05-21" .planning/ROADMAP.md` returns 0 (no placeholder dates)
    - `git log --oneline -1 -- .planning/ROADMAP.md .planning/STATE.md` shows the closure commit
    - `git diff HEAD~1 --stat | head -10` shows exactly 6 files in this commit (or includes only the expected 6 in a per-workstream commit)
  </acceptance_criteria>
  <done>
    All 6 files synced; ROADMAP, STATE, and VALIDATION agree about 9/9 implementation phases complete; no placeholder dates; commit landed scoped to exactly the 6 files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact ↔ truth | The audit reads ROADMAP, STATE, VALIDATION as the source of truth for milestone phase state. False claims here would mislead future audits and human reviewers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03-01 | Repudiation (stale/false completion claim) | ROADMAP.md progress table dates + VALIDATION.md nyquist flags | medium | mitigate | All dates sourced from each phase's `VERIFICATION.md` `verified:` field via direct grep (Task 1 step A); no 2026-05-21 placeholder permitted (Task 1 acceptance criterion). VALIDATION.md flips gated by per-file VERIFICATION.md `status: passed/PASS` precondition check (Task 3 step 1); files whose precondition fails are NOT flipped — discrepancy recorded in SUMMARY instead. |
| T-14-03-02 | Tampering (unintended frontmatter corruption) | STATE.md / VALIDATION.md frontmatter | low | mitigate | Single-key YAML substitutions only (`nyquist_compliant: false` → `true`). Task 4 git-diff stat acceptance criterion bounds the commit to exactly 6 files. The `wave_0_complete` and `status` keys are explicitly out of scope per D-11 (negative grep on `wave_0_complete: false` and `status: draft` proves they were not touched). |
| T-14-03-03 | Information Disclosure (none) | n/a | n/a | accept | No PII or secrets in planning artifacts. |
</threat_model>

<verification>
Plan-03 verification chain:

1. **ROADMAP progress table:** All 5 rows for Phases 5-9 show Complete + correct date (`grep -cE "Phase [5-9].*Complete.*2026-05-(12|13|18|19)" .planning/ROADMAP.md` >= 5).
2. **ROADMAP top checklist:** All 5 checkboxes for Phases 5-9 show `[x]` (5 grep hits).
3. **STATE body sync:** No `0/8` text; `9/9` present at least once; `Phase 14` is the current focus.
4. **VALIDATION nyquist flips:** 4 files show `nyquist_compliant: true`; none show `false` in those 4 files; precondition grep recorded in SUMMARY.
5. **No placeholder dates:** No `2026-05-21` on Phase 5-9 rows in ROADMAP.
6. **Single per-workstream commit:** 6 files changed in one commit.
</verification>

<success_criteria>
- ROADMAP.md: Phases 5-9 checkboxes show `[x]`; progress table rows show Complete with real dates (2026-05-12, 2026-05-13, 2026-05-13, 2026-05-18, 2026-05-19)
- STATE.md: body Progress line agrees with frontmatter (9/9, 100%); Current focus updated to Phase 14
- 4 VALIDATION.md files: `nyquist_compliant: true` in frontmatter; `wave_0_complete: false` and `status: draft` NOT modified
- No `2026-05-21` placeholder date used for Phase 5-9 in ROADMAP
- Commit scoped to exactly 6 files
- SUMMARY records precondition grep output for D-11 per-file gating
</success_criteria>

<output>
After completion, create `.planning/phases/14-close-v1.1-gaps/14-03-SUMMARY.md` documenting:
- Verbatim output of the date-extraction greps for D-09 (Phases 5-9 verified dates from each VERIFICATION.md)
- Verbatim output of the precondition grep for D-11 (4 phases × `status: passed`/`PASS` confirmation)
- List of 6 files modified
- Any deviation from the plan (e.g., a VERIFICATION.md status precondition failing for one of the 4 nyquist flips) with Rule-1/2/3 markers
</output>
