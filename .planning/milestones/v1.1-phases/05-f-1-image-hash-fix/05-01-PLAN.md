---
phase: 05-f-1-image-hash-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - run_docker.sh
autonomous: true
requirements:
  - FOUND-01
requirements_addressed:
  - FOUND-01

must_haves:
  truths:
    - "Both `sort` invocations in run_docker.sh's image-hash subshell prefix the command with LC_ALL=C, making file ordering byte-deterministic across host locales."
    - "run_docker.sh still parses --help and exits 0 after the patch (no syntax breakage)."
    - "The variable names DOCKERFILE_SHA, SHORT_SHA, HASH_IMAGE remain present and unchanged in run_docker.sh's scope."
  artifacts:
    - path: "run_docker.sh"
      provides: "Hardened inline content-hash subshell with locale-stable sort"
      contains: "LC_ALL=C sort"
  key_links:
    - from: "run_docker.sh:215"
      to: "GNU sort"
      via: "pipe with LC_ALL=C prefix"
      pattern: "LC_ALL=C sort"
    - from: "run_docker.sh:220"
      to: "GNU sort"
      via: "pipe with LC_ALL=C prefix"
      pattern: "LC_ALL=C sort"
---

<objective>
Add `LC_ALL=C` to BOTH `sort` invocations inside the existing inline content-hash subshell at `run_docker.sh:212-229`, so the same source tree produces the same hash regardless of the developer's locale. This is the smallest possible diff and ships first because it is independently valuable and lower risk than the helper extraction in Plan 05-02.

Purpose: Eliminate locale-dependent flap of `DOCKERFILE_SHA` (en_US.UTF-8 vs C vs zh_CN.UTF-8 currently produce different sort orders for non-ASCII filenames). Closes D-02 from CONTEXT.md.

Output: Modified `run_docker.sh` with two `LC_ALL=C sort` invocations replacing the two bare `sort` invocations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-f-1-image-hash-fix/05-CONTEXT.md
@.planning/phases/05-f-1-image-hash-fix/05-RESEARCH.md
@.planning/phases/05-f-1-image-hash-fix/05-VALIDATION.md
@CLAUDE.md
@run_docker.sh

<interfaces>
<!-- Existing variable names in run_docker.sh that MUST be preserved (D-01). -->
<!-- These are downstream consumers of the hash, untouched by this plan. -->

From run_docker.sh (existing scope):
  DOCKERFILE_SHA  # 64-char hex from inline subshell
  SHORT_SHA       # first 12 chars of DOCKERFILE_SHA
  HASH_IMAGE      # "${IMAGE_REPO}:${SHORT_SHA}" → used by docker tag commands
  IMAGE_REPO      # "kali-re-tools" (line 206)

Existing inline subshell (run_docker.sh:212-229) — exactly two `sort` lines need editing:
  Line 215: `find "$SCRIPT_DIR/docker-bin" -type f -print | sort | xargs sha256sum`
  Line 220: `... -o -type f -print | sort | xargs sha256sum`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LC_ALL=C to both sort invocations in run_docker.sh image-hash subshell</name>
  <read_first>
    - run_docker.sh (read lines 200-235 to confirm current state; the two `sort` lines are at 215 and 220 per RESEARCH.md §"Critical detail (D-02)")
    - .planning/phases/05-f-1-image-hash-fix/05-RESEARCH.md §"Critical detail (D-02)" and §"Pattern 3: Find + Prune + Sort + sha256sum"
    - .planning/phases/05-f-1-image-hash-fix/05-CONTEXT.md §"D-02" (locked decision)
  </read_first>
  <files>run_docker.sh</files>
  <action>
    Make the smallest possible edits to run_docker.sh:

    1. Line 215 — change:
       `    find "$SCRIPT_DIR/docker-bin" -type f -print | sort | xargs sha256sum`
       to:
       `    find "$SCRIPT_DIR/docker-bin" -type f -print | LC_ALL=C sort | xargs sha256sum`

    2. Line 220 — change:
       `      -o -type f -print | sort | xargs sha256sum`
       to:
       `      -o -type f -print | LC_ALL=C sort | xargs sha256sum`

    Do NOT touch any other line. Do NOT alter the prune list (D-03), the Binja/IDA conditional blocks (CLAUDE.md licensing constraint), the `set -euo pipefail` discipline, the F-1 fix comment, the variable names DOCKERFILE_SHA / SHORT_SHA / HASH_IMAGE (D-01), or the surrounding image-tag mechanism. Do NOT export LC_ALL globally — prefix the `sort` invocation only, per RESEARCH.md §"Placement of LC_ALL=C" rationale (keeps intent obvious to future readers).

    Use the Edit tool (not Write) to preserve every other byte of the file.
  </action>
  <verify>
    <automated>grep -nE "LC_ALL=C sort" run_docker.sh | wc -l | grep -q '^2$' &amp;&amp; bash run_docker.sh --help >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "LC_ALL=C sort" run_docker.sh` returns exactly `2`
    - Line containing `find "$SCRIPT_DIR/docker-bin"` ends with `... -print | LC_ALL=C sort | xargs sha256sum`
    - The second `sort` in the mcp-gateway find pipeline is prefixed `LC_ALL=C sort`
    - `grep -nE '^DOCKERFILE_SHA=' run_docker.sh` still finds the variable assignment
    - `grep -nE '^SHORT_SHA=' run_docker.sh` still finds the variable assignment
    - `grep -nE '^HASH_IMAGE=' run_docker.sh` still finds the variable assignment
    - `bash run_docker.sh --help` exits 0
    - `bash -n run_docker.sh` exits 0 (syntax OK)
    - The prune list at run_docker.sh:217-219 is byte-identical (still contains `__pycache__`, `.pytest_cache`, `.ruff_cache`, `.venv`, `*.egg-info`, `htmlcov`, `node_modules`, `dist`)
    - Lines 221-228 (Binja/IDA conditional blocks) are unchanged
  </acceptance_criteria>
  <done>
    Two `LC_ALL=C sort` patterns appear in run_docker.sh; `./run_docker.sh --help` exits 0; no other diff against the previous file.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → run_docker.sh | The developer invokes the script locally; `set -euo pipefail` already gates failure propagation. No new boundary introduced by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | run_docker.sh inline subshell | accept | LOW — change is a one-token prefix (`LC_ALL=C`) to an existing locked-down pipeline. `set -euo pipefail` (line 2) already in force. No new inputs, no new files, no new privilege boundary. |
| T-05-01-02 | Denial of Service | sort with LC_ALL=C | accept | INFO — LC_ALL=C disables locale collation, which is faster than locale-aware sort, not slower. No DoS surface. |
| T-05-01-03 | Information Disclosure | hash output | accept | INFO — the hash is already non-secret (it's literally the image tag). No PII or secret material is touched. |

All threats are LOW/INFO. No HIGH or MEDIUM threats. No block-on-severity gates triggered.
</threat_model>

<verification>
After the task runs, manually confirm:

```bash
# Both LC_ALL=C sort patterns present (exactly 2)
grep -cE "LC_ALL=C sort" run_docker.sh    # expects: 2

# Script still parses and --help still works
bash -n run_docker.sh                      # syntax check
./run_docker.sh --help                     # functional smoke; expects exit 0

# Variables preserved (D-01)
grep -E "^(DOCKERFILE_SHA|SHORT_SHA|HASH_IMAGE)=" run_docker.sh  # expects: 3 lines

# Prune list preserved (D-03)
grep -E "__pycache__.*\.pytest_cache.*\.ruff_cache" run_docker.sh   # expects: 1 match (on the prune line)
```
</verification>

<success_criteria>
- run_docker.sh contains exactly two `LC_ALL=C sort` occurrences (one per find pipeline)
- run_docker.sh has zero remaining bare `| sort |` occurrences inside the hash subshell at lines 212-229
- `./run_docker.sh --help` exits 0
- No other lines of run_docker.sh have changed (D-01 honored — variable names, prune list, Binja/IDA blocks all intact)
- This plan does NOT introduce `scripts/compute_image_hash.sh` or any new test (those are Plans 05-02 and 05-03)
</success_criteria>

<output>
After completion, create `.planning/phases/05-f-1-image-hash-fix/05-01-SUMMARY.md`.
</output>
