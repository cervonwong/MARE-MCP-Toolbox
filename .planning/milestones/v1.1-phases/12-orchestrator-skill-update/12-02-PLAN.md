---
phase: 12
plan: 02
type: execute
wave: 1
depends_on: [12-01]
files_modified:
  - workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-1-packed-binary-triage.md
  - workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-2-elf-deep-dive.md
  - workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-3-pe-deep-dive.md
  - workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-4-rop-gadget-hunt.md
  - workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-5-dynamic-api-trace.md
  - workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-6-firmware-unpack.md
  - workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-7-cross-arch-iot.md
  - workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md
autonomous: true
requirements: [SKILL-02, SKILL-03]
requirements_addressed: [SKILL-02, SKILL-03]
tags: [skill-content, workflows, w-1-through-w-7]
must_haves:
  truths:
    - "Exactly 7 W-N files exist under `references/workflows/` named `W-N-<slug>.md` (kebab-case)"
    - "Each W-N file references at least one v1.1 wrapper appropriate to its workflow (per WN_WRAPPER_PATTERNS in test_skill_md_dual_mode.py)"
    - "Every `mcp__mare-toolbox__*` reference in every W-N file has a fallback (`scripts/`, `fallback`, or `else`) within ±3 lines"
    - "No W-N file contains the abbreviated `mcp__mare__` prefix (must be full `mcp__mare-toolbox__`)"
    - "Each W-N file documents per-step: gateway-mode call, scripts-mode fallback, expected artifact path (D-05)"
    - "`references/deep-re-workflows.md` exists, lists all seven W-N workflows with one-line summaries + entry conditions (D-04)"
    - "W-5/W-6/W-7 (dynamic-mode workflows) document the placeholder-artifact pattern `<case_dir>/dynamic/<step>-skipped.md` AND the INDEX.md `skipped steps` subsection (D-18)"
    - "Test `test_workflow_count_locked` flips GREEN"
    - "Test `test_workflow_index_present` flips GREEN"
    - "Test `test_wn_files_reference_v1_1_wrappers` flips GREEN for all 7 parametrized cases"
  artifacts:
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-1-packed-binary-triage.md"
      provides: "Packer ID → UPX/commercial/unknown branch → recurse checklist (FEATURES.md §W-1)"
      contains: "run_die"
      min_lines: 40
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-2-elf-deep-dive.md"
      provides: "ELF static via rabin2 JSON + readelf modes + nm + r2 session (FEATURES.md §W-2)"
      contains: "run_rabin2"
      min_lines: 50
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-3-pe-deep-dive.md"
      provides: "PE flavor; rabin2 JSON + xxd PE header + capstone + r2 (FEATURES.md §W-3)"
      contains: "run_rabin2"
      min_lines: 45
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-4-rop-gadget-hunt.md"
      provides: "NX/PIE/RELRO context + ropper structured gadgets (FEATURES.md §W-4)"
      contains: "run_ropper"
      min_lines: 30
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-5-dynamic-api-trace.md"
      provides: "strace/ltrace via jobs + gdb session follow-up (FEATURES.md §W-5)"
      contains: "start_tool_job"
      min_lines: 45
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-6-firmware-unpack.md"
      provides: "binwalk → unblob job → promote children → recurse (FEATURES.md §W-6)"
      contains: "run_unblob"
      min_lines: 40
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-7-cross-arch-iot.md"
      provides: "rabin2 → r2 disasm + qemu-user job for behavior (FEATURES.md §W-7)"
      contains: "run_qemu_user"
      min_lines: 30
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md"
      provides: "Index file (D-04) listing all 7 workflows with summaries + entry conditions"
      contains: "W-1"
      min_lines: 35
  key_links:
    - from: "Each W-N file"
      to: "v1.1 wrappers (run_die, run_rabin2, run_readelf, run_ropper, start_tool_job, run_strace, run_unblob, run_qemu_user, etc.)"
      via: "explicit `mcp__mare-toolbox__<wrapper>(...)` invocation per step"
      pattern: "mcp__mare-toolbox__\\w+"
    - from: "Each W-N file"
      to: "scripts/<helper>.sh fallback (when one exists) or `run_shell <tool>` (when no script)"
      via: "scripts-mode line per step"
      pattern: "scripts/|run_shell"
    - from: "deep-re-workflows.md"
      to: "all 7 W-N files"
      via: "entry-condition table with relative-path links"
      pattern: "W-[1-7]"
    - from: "W-5, W-6, W-7"
      to: "<case_dir>/dynamic/<step>-skipped.md placeholder pattern"
      via: "dynamic-mode-off skip rule (D-18)"
      pattern: "dynamic/.+-skipped\\.md"
---

<objective>
Create the seven per-workflow reference files (W-1..W-7) and the new index file `references/deep-re-workflows.md` per D-03 and D-04. Content is a verbatim reformatting of `.planning/research/FEATURES.md` §W-1..W-7 (lines 220-310) into skill-consumable form. Each W-N file uses a three-column markdown table (Step | Gateway-mode | Scripts-mode fallback | Artifact) per D-05 and the recommendation in 12-RESEARCH.md Architecture Patterns §Pattern 2 (table form preferred for density and regex-friendliness).

Each step within a W-N file MUST list both forms (D-05/D-08/D-09):
- Gateway-mode: `mcp__mare-toolbox__run_X(case_dir, sample, ...)` — explicit `mcp__mare-toolbox__` prefix (Pitfall 3)
- Scripts-mode fallback: either `scripts/<helper>.sh <args>` when a skill script covers it, or `run_shell <tool> <args>` when no script exists (D-09 second branch)

For dynamic-mode workflows (W-5, W-6 child-recurse triggers dynamic, W-7 qemu-user step), each dynamic step also documents the placeholder-artifact pattern: when `dynamic_mode_enabled=false`, the step writes `<case_dir>/dynamic/<step-name>-skipped.md` containing (1) which step was skipped, (2) the missing capability, (3) a one-line remediation hint ("run `./run_docker.sh --dynamic` and re-init"). The W-N file references the INDEX.md `skipped steps` row (D-18) without instructing the agent to drop arbitrary paths — `<step-name>` is constrained to a safe basename slug like `strace-all`, `gdb-trace`, `qemu-mipsel-run` (T-12-03 mitigation).

This plan turns RED tests `test_workflow_count_locked`, `test_workflow_index_present`, and `test_wn_files_reference_v1_1_wrappers` (×7 parametrized cases) GREEN. The dual-mode invariant tests (`test_dual_mode_invariant`, `test_no_abbreviated_prefix`) will partially turn GREEN here — the per-W-N file slices flip — but full GREEN waits for Plan 04 (SKILL.md + workflow.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-orchestrator-skill-update/12-CONTEXT.md
@.planning/phases/12-orchestrator-skill-update/12-RESEARCH.md
@.planning/phases/12-orchestrator-skill-update/12-VALIDATION.md
@.planning/phases/12-orchestrator-skill-update/12-01-PLAN.md

# Source of truth for W-N content
@.planning/research/FEATURES.md

# Existing skill scripts (referenced as scripts-mode fallbacks)
@workspace/.claude/skills/malware-analysis-orchestrator/scripts/collect_imports.sh
@workspace/.claude/skills/malware-analysis-orchestrator/scripts/collect_strings.sh

# Gateway wrapper catalog (for canonical wrapper names)
@mcp-gateway/src/mcp_gateway/tools/re_static.py
@mcp-gateway/src/mcp_gateway/tools/extract.py
@mcp-gateway/src/mcp_gateway/tools/r2_sessions.py
@mcp-gateway/src/mcp_gateway/tools/jobs.py
@mcp-gateway/src/mcp_gateway/tools/dynamic.py

<interfaces>
<!-- Canonical v1.1 wrapper names the W-N files MUST reference. -->
<!-- Use these EXACTLY; do not abbreviate. -->

re_static wrappers (11): run_file, run_die, run_xxd, run_readelf, run_objdump,
                          run_nm, run_rabin2, run_capstone_disasm, run_ropper,
                          run_jq, run_yq
extract wrappers (7):    run_binwalk, run_unblob, run_upx_test, run_upx_list,
                          run_upx_unpack, list_extracted_files, promote_extracted_sample
r2_sessions (4):         open_r2_session, r2_cmd, close_r2_session, list_sessions
jobs (4):                start_tool_job, get_tool_job, cancel_tool_job, list_tool_jobs
dynamic env-gated (7):   run_strace, run_ltrace, run_qemu_user, open_gdb_session,
                          gdb_exec, close_gdb_session, get_dynamic_capabilities
shell (1):               run_shell
re_artifacts (5):        write_artifact, append_artifact, list_artifacts,
                          get_artifact_tree, get_tool_log
cases (canonical):       init_case, get_active_case, set_active_case, list_uploads,
                          get_sample_info
backend pass-through:    get_active_backend → returns one of {ida, binary_ninja, ghidra}
                          IDA native examples: decompile(addr), list_funcs(...), xrefs_to(...)

Host-side tool prefix: mcp__mare-toolbox__<wrapper>  (canonical; never abbreviate)
Server name in .mcp.json: "mare-toolbox" (run_docker.sh:66)

Existing skill scripts (scripts-mode fallback targets):
- scripts/collect_strings.sh <sample>     → strings -a + rabin2 -zz + floss
- scripts/collect_imports.sh <sample>     → rabin2 -i + format-specific tools
- scripts/scan_yara.sh <sample>           → yara with bundled rules
- scripts/scan_capa.sh <sample>           → capa scan
- scripts/init_status_tree.sh <sample>    → case dir
- scripts/rank_signals.py --status-dir    → ranking
- scripts/build_hypothesis.py --status-dir → hypothesis baseline
- scripts/update_state.py --status-dir    → state + INDEX refresh
- scripts/resolve_case.sh <sample>        → latest case lookup
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seven W-N workflow files (verbatim from FEATURES.md §W-1..W-7)</name>
  <files>workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-1-packed-binary-triage.md, workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-2-elf-deep-dive.md, workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-3-pe-deep-dive.md, workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-4-rop-gadget-hunt.md, workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-5-dynamic-api-trace.md, workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-6-firmware-unpack.md, workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-7-cross-arch-iot.md</files>
  <read_first>
    - .planning/research/FEATURES.md lines 220-310 (W-1..W-7 source content, verbatim)
    - .planning/phases/12-orchestrator-skill-update/12-CONTEXT.md (D-03..D-05, D-08, D-09, D-18)
    - .planning/phases/12-orchestrator-skill-update/12-RESEARCH.md (Pattern 2 table form, Pitfall 3 abbreviated prefix)
    - mcp-gateway/tests/test_skill_md_dual_mode.py (WN_WRAPPER_PATTERNS dict — each W-N must match its pattern)
    - workspace/.claude/skills/malware-analysis-orchestrator/scripts/collect_imports.sh (for W-2 scripts fallback)
    - workspace/.claude/skills/malware-analysis-orchestrator/scripts/collect_strings.sh (for W-2/W-3 scripts fallback)
    - workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md (current backend priority is wrong; do NOT propagate it — use IDA > BN > Ghidra in W-3 step 4)
  </read_first>
  <action>
    Create exactly seven markdown files. Each file uses the template below. The `mcp__mare-toolbox__<wrapper>(...)` prefix is canonical (never abbreviated to `mcp__mare__`). Each step row in the table MUST list a scripts-mode fallback (a `scripts/...` script, `run_shell <tool> ...`, or the keyword `fallback` in the cell) so D-12 regex window passes.

    **Template (apply to each W-N file):**
    ```markdown
    # W-N: <Workflow Title>

    > Source: `.planning/research/FEATURES.md` §W-N (lines XXX-YYY).
    > Dual-mode-aware per D-05 / D-09. Backend priority: `IDA > Binary Ninja > Ghidra > r2`.

    **Entry conditions:** <when this workflow applies — detected_format + signals>
    **Goal:** <one-line outcome>
    **Dynamic-mode requirement:** <none | `dynamic_mode_enabled=true` | partial>

    ## Mode-aware step decision rule

    Read `<case_dir>/CURRENT_STATE.json`:
    ```
    if state.mode == "gateway":
        call mcp__mare-toolbox__<wrapper>(...)
    else:
        call scripts/<fallback>.sh ...    # local-script fallback
        # or: run_shell <tool> ...        # when no script exists and gateway is up
    ```

    ## Steps

    | # | Step | Gateway-mode call | Scripts-mode fallback | Expected artifact |
    |---|------|-------------------|------------------------|---------------------|
    | 1 | ... | `mcp__mare-toolbox__run_X(case_dir, sample, ...)` | `scripts/<helper>.sh <args>` OR `run_shell <tool> <args>` | `<case_dir>/<file>` |
    | 2 | ... | ... | ... | ... |

    ## Branching / recursion notes

    <only for W-1 (packer branch), W-6 (per-child recurse), W-7 (cross-arch)>

    ## Dynamic-mode skip behavior (D-18; W-5/W-6/W-7 only)

    When `state.dynamic_mode_enabled == false` for any dynamic-only step:

    1. Skip the live tool call.
    2. Write placeholder `<case_dir>/dynamic/<step-slug>-skipped.md` where `<step-slug>` is a safe basename slug (e.g., `strace-all`, `ltrace-libc`, `qemu-mipsel-run`, `gdb-trace`). Content:
       - Step name and W-N reference
       - Missing capability (e.g., "ptrace_scope=2, kernel restricts attach")
       - Remediation: "run `./run_docker.sh --dynamic` and re-init the case, OR re-probe via `scripts/update_state.py --status-dir <case_dir> --probe-dynamic`"
    3. Append a row to `<case_dir>/INDEX.md` under the `## Skipped steps` subsection: `| <step-slug> | <reason> | dynamic/<step-slug>-skipped.md |`
    ```

    **Per-file content (paste-ready, mapped from FEATURES.md verbatim with the table form):**

    ### File 1: `W-1-packed-binary-triage.md`
    Title: "Packed-Binary Triage (PE or ELF)". Entry: `detected_format ∈ {pe, elf}` AND (`run_die` deep packer field non-empty OR `binwalk` shows high-entropy regions). Goal: identify packer → unpack → re-triage. Dynamic: none.
    Steps (table rows):
    | 1 | Format and arch | `mcp__mare-toolbox__run_file(case_dir, sample)` | `scripts/collect_imports.sh <sample>` (writes 03_imports_raw.txt header section) or `run_shell file <sample>` | `<case_dir>/00_sample_profile.md` (file section) |
    | 2 | Packer ID | `mcp__mare-toolbox__run_die(case_dir, sample, deep=true)` | `run_shell die -d <sample>` (no skill-script wrapper for die — gateway-only or shell fallback) | `<case_dir>/00_sample_profile.md` (die section), `tool-logs/die-*.json` |
    | 3a | UPX branch: detect | `mcp__mare-toolbox__run_upx_test(case_dir, sample)` | `run_shell upx -t <sample>` fallback | `<case_dir>/tool-logs/upx-test-*.txt` |
    | 3b | UPX branch: list | `mcp__mare-toolbox__run_upx_list(case_dir, sample)` | `run_shell upx -l <sample>` fallback | `<case_dir>/tool-logs/upx-list-*.txt` |
    | 3c | UPX branch: unpack | `mcp__mare-toolbox__run_upx_unpack(case_dir, sample)` | `run_shell upx -d -o <out> <sample>` fallback | `<case_dir>/extracted/upx/<sample>.unpacked` |
    | 3d | UPX branch: promote child | `mcp__mare-toolbox__promote_extracted_sample(case_dir, child_path)` | `scripts/init_status_tree.sh <child_path> --new` (creates new case for child) | new `status/<NNN>-<child>/` case |
    | 3e | Recurse on child from step 1 (apply W-2 for ELF, W-3 for PE) | n/a | n/a | per W-2/W-3 |
    | 4a | Commercial protector (VMProtect/Themida/Enigma) | log; skip auto-unpack; `mcp__mare-toolbox__write_artifact(case_dir, "06_component_inventory.md", "## Commercial protector\n...\n", mode="append")` | `echo` + manual write to `06_component_inventory.md` fallback | `<case_dir>/06_component_inventory.md` |
    | 4b | Unknown packer: entropy + signature | `mcp__mare-toolbox__run_binwalk(case_dir, sample, mode="signature")` | `run_shell binwalk <sample>` fallback | `<case_dir>/tool-logs/binwalk-sig-*.txt` |
    | 4c | Unknown packer: hex dump of high-entropy regions | `mcp__mare-toolbox__run_xxd(case_dir, sample, offset=<N>, length=<L>)` | `run_shell xxd -s <N> -l <L> <sample>` fallback | `<case_dir>/tool-logs/xxd-*.txt` |
    | 4d | Unknown packer: unblob attempt | `mcp__mare-toolbox__run_unblob(case_dir, sample)` | `run_shell unblob <sample>` fallback | `<case_dir>/extracted/unblob/` |
    | 5 | On unpacked child: standard checklist | n/a (dispatch to W-2 for ELF, W-3 for PE) | n/a | per W-2/W-3 |

    Add a "Branching / recursion notes" section: "Step 3 (UPX branch) and step 4 (unknown packer) both terminate with a recurse-into-W-2-or-W-3 on the unpacked child. Limit recursion depth to 3 by checking `<case_dir>/INDEX.md` for nested case references."

    ### File 2: `W-2-elf-deep-dive.md`
    Title: "ELF Deep-Dive (Linux Malware Static)". Entry: `detected_format == elf`. Goal: full ELF static profile. Dynamic: none (W-5 follows up for dynamic).
    Rows (numbered 1..9 per FEATURES.md):
    | 1 | Arch confirm, dyn-linked vs static | `mcp__mare-toolbox__run_file(case_dir, sample)` | `scripts/collect_imports.sh <sample>` (file section) or `run_shell file <sample>` | `<case_dir>/00_sample_profile.md` |
    | 2 | Static metadata (rabin2 -j all) | `mcp__mare-toolbox__run_rabin2(case_dir, sample, command="all", json=true)` | `scripts/collect_imports.sh <sample>` writes 03_imports_raw.txt from `rabin2 -i` (subset) — fallback | `<case_dir>/00_sample_profile.md` (rabin2 section), `tool-logs/rabin2-all-*.json` |
    | 3 | Section sizes | `mcp__mare-toolbox__run_readelf(case_dir, sample, mode="sections")` | `run_shell readelf -S <sample>` fallback | `<case_dir>/tool-logs/readelf-sections-*.txt` |
    | 4 | Dynamic section (DT_NEEDED, DT_RPATH, DT_INIT) | `mcp__mare-toolbox__run_readelf(case_dir, sample, mode="dynamic")` | `run_shell readelf -d <sample>` fallback | `<case_dir>/tool-logs/readelf-dynamic-*.txt` |
    | 5 | Notes (build-id, GO buildinfo) | `mcp__mare-toolbox__run_readelf(case_dir, sample, mode="notes")` | `run_shell readelf -n <sample>` fallback | `<case_dir>/tool-logs/readelf-notes-*.txt` |
    | 6 | Undefined symbols, demangled | `mcp__mare-toolbox__run_nm(case_dir, sample, mode="undefined", demangle=true)` | `run_shell nm -D --demangle <sample>` fallback | `<case_dir>/tool-logs/nm-undef-*.txt` |
    | 7 | YARA + capa pipeline | (v1.0 scripts; same in both modes) | `scripts/scan_yara.sh <sample>; scripts/scan_capa.sh <sample>` — fallback | `<case_dir>/00_sample_profile.md` (YARA + capa sections) |
    | 8a | Open r2 session | `mcp__mare-toolbox__open_r2_session(case_dir, sample)` returns session_id | `run_shell r2 -A -q -c '...' <sample>` one-shot fallback | session handle |
    | 8b | r2: main-flow walk | `mcp__mare-toolbox__r2_cmd(session_id, "pdfj @ entry0")` | `r2 -A -c 'pdfj @ entry0' <sample>` fallback | `<case_dir>/disassembly/r2-entry0.json` (via write_artifact) |
    | 8c | r2: suspicious functions | `mcp__mare-toolbox__r2_cmd(session_id, "aflj~malware-ish-name")` | `r2 -A -c 'aflj' <sample>` fallback | `<case_dir>/xrefs/r2-functions.json` |
    | 8d | r2: C2 strings | `mcp__mare-toolbox__r2_cmd(session_id, "izj~http://|.onion|/api/")` | `r2 -A -c 'iz' <sample>` fallback | `<case_dir>/02_strings_interesting.md` (C2 section, via write_artifact) |
    | 8e | Close r2 session | `mcp__mare-toolbox__close_r2_session(session_id)` | n/a (one-shot exits) | — |
    | 9 | Hypothesis writeup | `mcp__mare-toolbox__write_artifact(case_dir, "05_behavior_hypotheses.md", "...")` | direct file write fallback (`scripts/build_hypothesis.py --status-dir <case_dir>` baseline) | `<case_dir>/05_behavior_hypotheses.md` |

    ### File 3: `W-3-pe-deep-dive.md`
    Title: "PE Deep-Dive (Windows Malware Static)". Entry: `detected_format == pe`. Goal: Win32 import surface + disasm. Dynamic: none.
    Rows (numbered 1..8 per FEATURES.md):
    | 1 | Format and packer | `mcp__mare-toolbox__run_file(...); mcp__mare-toolbox__run_die(case_dir, sample, deep=true)` | `run_shell file <sample>; run_shell die -d <sample>` fallback | `<case_dir>/00_sample_profile.md` |
    | 2 | rabin2 -j all (imports/exports/sections) | `mcp__mare-toolbox__run_rabin2(case_dir, sample, command="all", json=true)` | `scripts/collect_imports.sh <sample>` (subset) — fallback | `<case_dir>/00_sample_profile.md`, `tool-logs/rabin2-*.json` |
    | 3 | xxd of PE header at offset 0 | `mcp__mare-toolbox__run_xxd(case_dir, sample, offset=0, length=512)` | `run_shell xxd -l 512 <sample>` fallback | `<case_dir>/tool-logs/xxd-pe-header.txt` |
    | 4 | Disasm pipeline (IDA > BN > Ghidra) | first call `mcp__mare-toolbox__get_active_backend()` → then call backend-native `decompile(addr=...)` (IDA), `decompile_function` (BN/Ghidra) on entry/TLS-callback/WinMain | scripts-mode: direct backend MCP in `.mcp.json` — fallback uses `mcp__ida_mcp__decompile` / `mcp__binary_ninja_headless_mcp__decompile_function` / `mcp__ghidra_headless_mcp__decompile_function` per legacy-prefix appendix in SKILL.md | `<case_dir>/decompilation/entry.txt`, `decompilation/wmain.txt` |
    | 5 | Capstone disasm of shellcode regions | `mcp__mare-toolbox__run_capstone_disasm(case_dir, sample, offset=<N>, length=<L>, arch="x86", mode=32)` | `run_shell python3 -c 'import capstone; ...'` fallback | `<case_dir>/disassembly/capstone-shellcode.txt` |
    | 6 | capa + YARA | `scripts/scan_capa.sh <sample>; scripts/scan_yara.sh <sample>` (both modes call the scripts — D-08) | same — fallback | `<case_dir>/00_sample_profile.md` (capa + YARA sections) |
    | 7 | r2 session for xrefs + strings | `mcp__mare-toolbox__open_r2_session(...)` → `r2_cmd("axt @ <addr>"); r2_cmd("izj")` | `r2 -A -c 'axt @ ...' <sample>` fallback | `<case_dir>/xrefs/r2-pe.json` |
    | 8 | Writeup | `mcp__mare-toolbox__write_artifact(case_dir, "05_behavior_hypotheses.md", ...)` | direct file write fallback or `scripts/build_hypothesis.py --status-dir <case_dir>` | `<case_dir>/05_behavior_hypotheses.md` |

    ### File 4: `W-4-rop-gadget-hunt.md`
    Title: "ROP Gadget Hunt (Exploit Dev or ROP-using-Malware Reading)". Entry: agent decides (e.g., shellcode detected, exploit-dev context). Goal: structured gadget list. Dynamic: none.
    Rows (numbered 1..5 per FEATURES.md):
    | 1 | Arch confirm | `mcp__mare-toolbox__run_file(case_dir, sample)` | `run_shell file <sample>` fallback | `<case_dir>/00_sample_profile.md` |
    | 2 | NX/PIE/RELRO/Canary | `mcp__mare-toolbox__run_rabin2(case_dir, sample, command="info", json=true)` | `run_shell rabin2 -I <sample>` fallback (parse manually) | `<case_dir>/00_sample_profile.md` (security section) |
    | 3 | Gadget search (pop rdi, syscall, mov) | `mcp__mare-toolbox__run_ropper(case_dir, sample, search="pop rdi; ret", badbytes="00")` (repeat for syscall, mov [r??], r??) | `run_shell ropper --file <sample> --search 'pop rdi; ret'` fallback | `<case_dir>/rop/gadgets.json` |
    | 4 | Semantic filter | `mcp__mare-toolbox__run_ropper(case_dir, sample, semantic="...")` | `run_shell ropper --semantic '...'` fallback | `<case_dir>/rop/gadgets-semantic.json` |
    | 5 | Chain hypothesis writeup | `mcp__mare-toolbox__write_artifact(case_dir, "rop/chain-hypothesis.md", "...")` | direct file write fallback | `<case_dir>/rop/chain-hypothesis.md` |

    ### File 5: `W-5-dynamic-api-trace.md`
    Title: "Dynamic API Trace (Linux user-mode malware behavior)". Entry: `detected_format == elf` AND `state.dynamic_mode_enabled == true`. Goal: behavior trace. Dynamic: REQUIRED.
    Rows (numbered 1..8 per FEATURES.md):
    | 1 | Static recap (W-2 abbreviated) | per W-2 steps 1-4 | per W-2 fallback | `<case_dir>/00_sample_profile.md` |
    | 2 | Start strace job | `mcp__mare-toolbox__start_tool_job("run_strace", {sample, mode:"all", timeout_s:30, no_network:true})` returns job_id | scripts-mode lacks job system; fallback: `run_shell strace -ff -o <case_dir>/dynamic/strace.txt <sample>` synchronous (D-09) | `<case_dir>/dynamic/strace-all.txt` |
    | 3 | Poll partial output via MCP Resource | read `mare://cases/<case>/dynamic/strace-all.txt` while job runs | scripts-mode: tail the file directly — fallback | live `<case_dir>/dynamic/strace-all.txt` |
    | 4 | Job completion | `mcp__mare-toolbox__get_tool_job(job_id)` for exit_reason | scripts-mode: process exit — fallback | final `<case_dir>/dynamic/strace-all.txt` |
    | 5 | ltrace job | `mcp__mare-toolbox__start_tool_job("run_ltrace", {sample, timeout_s:30})` | `run_shell ltrace -o <case_dir>/dynamic/ltrace.txt <sample>` fallback | `<case_dir>/dynamic/ltrace.txt` |
    | 6 | jq over capa behaviors | `mcp__mare-toolbox__run_jq(case_dir, sample=case_dir/tool-logs/capa.json, query=".meta.attack")` | `run_shell jq '.meta.attack' <capa.json>` fallback | `<case_dir>/dynamic/capa-attack.json` |
    | 7 | gdb session follow-up (optional) | `mcp__mare-toolbox__open_gdb_session(case_dir, sample)` → `gdb_exec(session_id, "b open; run; bt")` → `close_gdb_session(session_id)` | `run_shell gdb -batch -ex 'b open' -ex 'run' -ex 'bt' <sample>` fallback | `<case_dir>/dynamic/gdb-open-bt.txt` |
    | 8 | Network-aware re-run (DEFERRED — out of scope per CONTEXT.md `<deferred>`) | n/a v1.1 | n/a | n/a |

    Add "Dynamic-mode skip behavior" section (D-18) using the safe-slug list: `strace-all`, `ltrace-libc`, `gdb-open-bt`. When `state.dynamic_mode_enabled == false`, write `<case_dir>/dynamic/<slug>-skipped.md` for each skipped step. INDEX.md `## Skipped steps` row format: `| W-5/<slug> | <reason> | dynamic/<slug>-skipped.md |`.

    ### File 6: `W-6-firmware-unpack.md`
    Title: "Embedded Firmware Unpack". Entry: `run_file` says `data` or specific firmware header (e.g., `Squashfs`, `JFFS2`, `uImage`). Goal: recursive extraction tree. Dynamic: per-child may require dynamic (W-5/W-7).
    Rows (numbered 1..8 per FEATURES.md):
    | 1 | File ID | `mcp__mare-toolbox__run_file(case_dir, sample)` | `run_shell file <sample>` fallback | `<case_dir>/00_sample_profile.md` |
    | 2 | binwalk signature pass | `mcp__mare-toolbox__run_binwalk(case_dir, sample, mode="signature")` | `run_shell binwalk <sample>` fallback | `<case_dir>/tool-logs/binwalk-sig.txt` |
    | 3 | binwalk entropy pass | `mcp__mare-toolbox__run_binwalk(case_dir, sample, mode="entropy")` | `run_shell binwalk -E <sample>` fallback | `<case_dir>/tool-logs/binwalk-entropy.txt` |
    | 4 | unblob job (firmware is usually big) | `mcp__mare-toolbox__start_tool_job("run_unblob", {sample, output_dir:"<case>/extracted/unblob"})` | `run_shell unblob -o <case>/extracted/unblob <sample>` synchronous fallback (may timeout >60s) | `<case_dir>/extracted/unblob/` |
    | 5 | unblob report | `mcp__mare-toolbox__get_tool_job(job_id)` reads `--report` JSON | scripts-mode: read `<case>/extracted/unblob/report.json` directly — fallback | `<case_dir>/extracted/unblob/report.json` |
    | 6 | List children | `mcp__mare-toolbox__list_extracted_files(case_dir, sub="extracted/unblob")` | `run_shell find <case>/extracted/unblob -type f` fallback | `<case_dir>/06_component_inventory.md` (extraction tree section) |
    | 7 | Per interesting child: promote + recurse | `mcp__mare-toolbox__promote_extracted_sample(case_dir, child_path)` → recurse into W-2 (ELF) or W-3 (PE) on the new case | `scripts/init_status_tree.sh <child_path> --new` then re-run W-2/W-3 — fallback | new `status/<NNN>-<child>/` cases |
    | 8 | Summarize tree | `mcp__mare-toolbox__run_jq(case_dir, file="extracted/unblob/report.json", query=".tree")` | `run_shell jq '.tree' <report.json>` fallback | `<case_dir>/06_component_inventory.md` (tree summary) |

    Note: dynamic-mode skip behavior applies only if a child sample triggers W-5/W-7. The W-6 file itself has no dynamic-only steps — note this explicitly so the skip-behavior section is brief.

    ### File 7: `W-7-cross-arch-iot.md`
    Title: "Cross-Arch Triage (MIPS/ARM IoT Malware)". Entry: `detected_format == elf` AND arch ∈ {mips, mipsel, arm, aarch64, ppc, sparc, riscv}. Goal: behavior trace via qemu-user. Dynamic: REQUIRED for step 4.
    Rows (numbered 1..5 per FEATURES.md):
    | 1 | Confirm arch | `mcp__mare-toolbox__run_file(case_dir, sample)` | `run_shell file <sample>` fallback | `<case_dir>/00_sample_profile.md` |
    | 2 | rabin2 -j all | `mcp__mare-toolbox__run_rabin2(case_dir, sample, command="all", json=true)` | `scripts/collect_imports.sh <sample>` (subset) — fallback | `<case_dir>/00_sample_profile.md`, `tool-logs/rabin2-*.json` |
    | 3 | Static disasm via r2 (MIPS/ARM out of the box) | `mcp__mare-toolbox__open_r2_session(case_dir, sample)` → `r2_cmd(... "aaa; pdfj @ entry0")` | `run_shell r2 -A -c 'pdfj @ entry0' <sample>` fallback | `<case_dir>/disassembly/r2-entry0-<arch>.json` |
    | 4 | qemu-user behavior trace (DYNAMIC) | `mcp__mare-toolbox__start_tool_job("run_qemu_user", {sample, arch:"mipsel", strace:true, timeout_s:30})` returns job_id; `get_tool_job(job_id)` to read trace | `run_shell qemu-mipsel-static -strace <sample>` fallback (scripts-mode rarely has qemu installed; check `scripts/probe_dynamic_tools.sh` output first) | `<case_dir>/dynamic/qemu-<arch>-strace.txt` |
    | 5 | Recurse into W-2 triage with trace context | per W-2 steps 7-9 | per W-2 fallback | per W-2 |

    Dynamic-mode skip behavior (step 4 only): safe slug `qemu-<arch>-run` (e.g., `qemu-mipsel-run`). If `state.dynamic_capabilities.qemu_archs` does NOT contain the sample's arch, write `<case_dir>/dynamic/qemu-<arch>-run-skipped.md` even when dynamic mode is on. INDEX.md row: `| W-7/qemu-<arch>-run | qemu-<arch>-static missing from container | dynamic/qemu-<arch>-run-skipped.md |`. This is the D-15 fine-grained skip case.

    **After creating all seven files, run the regression test to verify:**
    ```bash
    cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_workflow_count_locked tests/test_skill_md_dual_mode.py::test_wn_files_reference_v1_1_wrappers -v
    ```
    All 8 cases (1 count + 7 parametrized wrapper checks) MUST be GREEN before declaring task done.

    **Self-check before commit:** grep each file for `mcp__mare__` (without `-toolbox`) — must return zero matches. Grep each file for at least one wrapper from its allow-list (per WN_WRAPPER_PATTERNS in the test module).
  </action>
  <verify>
    <automated>cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_workflow_count_locked tests/test_skill_md_dual_mode.py::test_workflow_index_present tests/test_skill_md_dual_mode.py::test_wn_files_reference_v1_1_wrappers -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist: `ls workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-*.md | wc -l` returns `7`
    - Filenames match exact slugs: `ls workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/` returns `W-1-packed-binary-triage.md`, `W-2-elf-deep-dive.md`, `W-3-pe-deep-dive.md`, `W-4-rop-gadget-hunt.md`, `W-5-dynamic-api-trace.md`, `W-6-firmware-unpack.md`, `W-7-cross-arch-iot.md`
    - Each file contains required wrappers (regex match per W-N): `grep -E 'run_die|run_upx_test|run_upx_unpack' workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-1-*.md` returns ≥ 1 line; `grep -E 'run_rabin2|run_readelf|run_nm|open_r2_session' workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-2-*.md` returns ≥ 1 line; ... (one grep per W-N)
    - No abbreviated prefix: `grep -rE 'mcp__mare__(?!toolbox)' workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/` returns nothing
    - Every gateway-tool line has a fallback in window: `pytest tests/test_skill_md_dual_mode.py::test_dual_mode_invariant -v` shows GREEN for all 7 W-N parametrized cases (SKILL.md and workflow.md may still be RED at this stage — that's Plan 04)
    - W-5/W-6/W-7 document the dynamic skip pattern: `grep -E 'dynamic/[a-z0-9._-]+-skipped\.md' workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-5-*.md workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-7-*.md` returns ≥ 1 line per file
    - INDEX.md `Skipped steps` subsection mentioned: `grep -iE 'skipped[ \-_]steps' workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-5-*.md workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-6-*.md workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-7-*.md` returns ≥ 1 line in W-5 and W-7 (W-6 may use a brief explicit note instead)
    - Backend priority correct in W-3 step 4 row: `grep -E 'IDA[^|]*Binary Ninja[^|]*Ghidra|get_active_backend' workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-3-*.md` returns ≥ 1 line
    - Mode decision rule block present: `grep -F 'state.mode' workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-*.md` returns ≥ 7 lines (one per file)
  </acceptance_criteria>
  <done>All 7 W-N files exist with required wrappers and dual-mode rows. Tests `test_workflow_count_locked` + `test_wn_files_reference_v1_1_wrappers` (×7) are GREEN. `test_dual_mode_invariant` is GREEN for all W-N parametrized cases (SKILL.md/workflow.md cases may still be RED — Plan 04 closes those). No abbreviated `mcp__mare__` prefix anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Create the workflow index file (D-04)</name>
  <files>workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md</files>
  <read_first>
    - .planning/phases/12-orchestrator-skill-update/12-CONTEXT.md (D-04 verbatim)
    - workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/ (all 7 files from Task 1)
    - mcp-gateway/tests/test_skill_md_dual_mode.py (`test_workflow_index_present` requirements)
  </read_first>
  <action>
    Create `workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md` with this exact structure:

    ```markdown
    # Deep RE Workflows Index

    > Catalog of per-format / per-goal deep-RE workflows. Each row points to a
    > self-contained checklist file under `references/workflows/`. The SKILL.md
    > Workflow Decision Tree routes by `detected_format` + signals into one of
    > these. Agent retains override discretion.
    >
    > **Naming convention:** `W-N-<kebab-slug>.md`. The regression test
    > `mcp-gateway/tests/test_skill_md_dual_mode.py::test_workflow_count_locked`
    > pins this directory at exactly 7 files; adding W-8 requires a deliberate
    > test bump (Pitfall 7).

    ## Mode preflight (every workflow)

    1. Read `<case_dir>/CURRENT_STATE.json`.
    2. If `state.mode == "gateway"`: gateway-mode calls (`mcp__mare-toolbox__*`) are valid.
    3. Else (`state.mode == "scripts"`): use the scripts-mode fallback column per step.
    4. If a step requires `state.dynamic_mode_enabled == true` and it's `false`:
       skip the step, write `<case_dir>/dynamic/<step-slug>-skipped.md`, and add an
       INDEX.md "Skipped steps" row (D-18). Remediation hint: re-run with
       `./run_docker.sh --dynamic` or `scripts/update_state.py --probe-dynamic
       --status-dir <case_dir>`.

    ## Workflow Catalog

    | ID  | File | Entry condition | Dynamic required | One-line summary |
    |-----|------|-----------------|------------------|------------------|
    | W-1 | [workflows/W-1-packed-binary-triage.md](workflows/W-1-packed-binary-triage.md) | `detected_format ∈ {pe, elf}` AND `run_die` packer field non-empty (or high-entropy regions in `binwalk`) | no | Identify packer (UPX / commercial / unknown), unpack via `run_upx_*` triplet or `run_unblob`, promote child, recurse W-2/W-3 |
    | W-2 | [workflows/W-2-elf-deep-dive.md](workflows/W-2-elf-deep-dive.md) | `detected_format == elf` | no | rabin2 JSON + readelf modes (sections / dynamic / notes) + nm undef + r2 session for entry/xrefs/strings |
    | W-3 | [workflows/W-3-pe-deep-dive.md](workflows/W-3-pe-deep-dive.md) | `detected_format == pe` | no | rabin2 JSON + xxd PE header + IDA/BN/Ghidra decompile via `get_active_backend()` + capstone for shellcode + r2 xrefs |
    | W-4 | [workflows/W-4-rop-gadget-hunt.md](workflows/W-4-rop-gadget-hunt.md) | Exploit-dev / shellcode context (agent decides) | no | NX/PIE/RELRO context via rabin2 + structured gadget hunt via `run_ropper` with semantic filters |
    | W-5 | [workflows/W-5-dynamic-api-trace.md](workflows/W-5-dynamic-api-trace.md) | `detected_format == elf` AND `state.dynamic_mode_enabled == true` | **YES** | strace + ltrace via `start_tool_job` (long-running) + jq over capa + optional gdb session follow-up |
    | W-6 | [workflows/W-6-firmware-unpack.md](workflows/W-6-firmware-unpack.md) | `run_file` reports `data` or known firmware header (Squashfs, JFFS2, uImage, etc.) | partial (only if child triggers W-5/W-7) | binwalk → unblob job → `list_extracted_files` → `promote_extracted_sample` → per-child recurse |
    | W-7 | [workflows/W-7-cross-arch-iot.md](workflows/W-7-cross-arch-iot.md) | `detected_format == elf` AND arch ∈ {mips, mipsel, arm, aarch64, ppc, sparc, riscv} | **YES** for behavior step; fine-grained skip on `state.dynamic_capabilities.qemu_archs` membership | rabin2 + r2 static + `run_qemu_user` job for behavior trace on x86_64 host |

    ## Routing decision tree

    Apply in order; first match wins (agent may override):

    1. **Packer detected** (`run_die` deep field shows known packer) → W-1.
    2. **Firmware blob** (`run_file` says `data` or firmware header) → W-6.
    3. **Cross-arch ELF** (`arch ∈ {mips*, arm*, ppc, sparc, riscv}`) → W-7.
    4. **ELF, native x86/x86_64** → W-2 (static) → W-5 (dynamic, if enabled).
    5. **PE** → W-3.
    6. **Shellcode/exploit context** (any format, agent-judged) → W-4 (in addition to format workflow).
    7. **Mach-O** → no dedicated W-N file (v1.1 scope); use SKILL.md Mach-O Tooling section + ad-hoc.

    ## Fallback path

    All workflows preserve dual-mode operation (D-08, D-09). The fallback path is
    canonical: existing `scripts/` helpers are independently shell-testable
    (`collect_strings.sh`, `collect_imports.sh`, `scan_yara.sh`, `scan_capa.sh`).
    If a step has neither a gateway wrapper nor a skill script, use `run_shell
    <tool> <args>` (gateway mode) — that's the catch-all D-09 second branch.
    ```

    **Verification before commit:** Re-run `test_workflow_index_present`:
    ```bash
    cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_workflow_index_present -v
    ```
  </action>
  <verify>
    <automated>cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_workflow_index_present tests/test_skill_md_dual_mode.py::test_dual_mode_invariant -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md`
    - All 7 W-N IDs present: `grep -cE '^\| W-[1-7] ' workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md` returns `7`
    - All 7 W-N filenames linked: `grep -cE 'workflows/W-[1-7]-' workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md` returns ≥ 7
    - Routing decision tree present: `grep -F 'Routing decision tree' workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md` returns 1 line
    - Mode preflight present: `grep -F 'state.mode' workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md` returns ≥ 1 line
    - `test_workflow_index_present` GREEN: `cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_workflow_index_present -v 2>&1 | grep -F 'PASSED'` returns 1 line
    - No abbreviated prefix: `grep -E 'mcp__mare__(?!toolbox)' workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md` returns nothing
    - Every gateway-tool reference is wrapped in a fallback context: `pytest tests/test_skill_md_dual_mode.py::test_dual_mode_invariant -v 2>&1 | grep 'deep-re-workflows.md' | grep -F 'PASSED'` returns 1 line (the index file's parametrized case passes)
  </acceptance_criteria>
  <done>Index file exists with all 7 W-N workflows catalogued (entry conditions + dynamic flag + one-line summary), routing decision tree, mode-preflight block. `test_workflow_index_present` GREEN. Index file passes `test_dual_mode_invariant` parametrized case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent prose → run_shell | W-N files teach the agent to invoke `run_shell` with named tool wrappers and argv-list semantics, NEVER composing shell strings from sample-derived data |
| Agent prose → dynamic placeholder file paths | The `<step-slug>` token in `<case_dir>/dynamic/<step-slug>-skipped.md` is constrained to a closed list (`strace-all`, `ltrace-libc`, `gdb-trace`, `qemu-<arch>-run`) — not free-form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | W-N files teach `run_shell` invocations | mitigate | Every `run_shell` example uses the argv-list form (`run_shell <tool> <arg1> <arg2>`) — NEVER `run_shell "tool $sample"` or any shell-string concatenation. The W-N file content is reviewed for shell-injection prose patterns before commit. Verify: `grep -nE 'run_shell.*\$' workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/W-*.md` returns zero matches. |
| T-12-03 | Information disclosure | Placeholder file path `<case_dir>/dynamic/<step>-skipped.md` | mitigate | `<step>` is constrained to a closed slug list per workflow (W-5: strace-all, ltrace-libc, gdb-open-bt; W-7: qemu-<arch>-run where arch ∈ probe_dynamic_tools output). W-N files document this constraint explicitly — agents do NOT compose arbitrary paths. Verify: `grep -E 'dynamic/[^a-z0-9._<>-]' workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/` finds nothing |
| T-12-08 | Tampering | Index file linking to W-N files | accept | Relative paths only (`workflows/W-N-*.md`); no absolute or computed paths. Risk: low |
</threat_model>

<verification>
1. `cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py -k 'workflow or wn_files' -v` — all workflow tests GREEN
2. `cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_dual_mode_invariant -v` — W-1..W-7 + deep-re-workflows.md parametrized cases GREEN (SKILL.md/workflow.md cases may still be RED — Plan 04)
3. `cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_no_abbreviated_prefix -v` — all W-N + index cases GREEN
4. No shell-injection prose: `grep -rnE 'run_shell.*\$\{?[a-z_]' workspace/.claude/skills/malware-analysis-orchestrator/references/workflows/` returns zero matches
5. Full unit suite: `cd mcp-gateway && pytest tests/ -q --ignore=tests/e2e` — only Plan 03/04 tests still RED
</verification>

<success_criteria>
- [ ] 7 W-N files at `references/workflows/W-N-<slug>.md` (exact filenames per D-03 example slugs)
- [ ] 1 index file at `references/deep-re-workflows.md` with workflow catalog table
- [ ] All wrapper references use full `mcp__mare-toolbox__` prefix (no abbreviated `mcp__mare__`)
- [ ] Every gateway-tool reference has a scripts-mode fallback (`scripts/`, `run_shell`, or `fallback`) within ±3 lines
- [ ] W-5/W-7 (and W-6 child-recurse note) document the placeholder + INDEX.md row skip pattern
- [ ] Routing decision tree present in index file
- [ ] `test_workflow_count_locked`, `test_workflow_index_present`, `test_wn_files_reference_v1_1_wrappers` ALL GREEN
- [ ] `test_dual_mode_invariant` GREEN for all W-N + index parametrized cases
- [ ] `test_no_abbreviated_prefix` GREEN for all W-N + index parametrized cases
</success_criteria>

<output>
After completion, create `.planning/phases/12-orchestrator-skill-update/12-02-SUMMARY.md` documenting:
- 7 W-N file names + line counts
- Index file structure + catalog row count
- Tests flipped GREEN (and which still RED for Plan 04)
- Any deviations from the table-form template (Claude's Discretion per CONTEXT.md)
</output>
