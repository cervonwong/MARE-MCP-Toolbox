---
phase: 260423-f3k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/.claude/settings.json
  - workspace/.claude/statusline.sh
autonomous: true
requirements:
  - QUICK-FIX-STATUSLINE-PATHS
must_haves:
  truths:
    - "Claude agent started inside the container renders the statusline instead of nothing"
    - "The statusline script references the real in-container mount path (/agent) rather than the non-existent /workspace"
    - "The statusline's short-path substitution collapses /agent to the 🔬 emoji"
  artifacts:
    - path: "workspace/.claude/settings.json"
      provides: "Claude statusline command config pointing at in-container script path"
      contains: "/agent/.claude/statusline.sh"
    - path: "workspace/.claude/statusline.sh"
      provides: "Statusline renderer using /agent as the container-internal workspace root"
      contains: "/agent"
  key_links:
    - from: "workspace/.claude/settings.json"
      to: "workspace/.claude/statusline.sh"
      via: "statusLine.command bash invocation"
      pattern: "bash /agent/\\.claude/statusline\\.sh"
    - from: "workspace/.claude/statusline.sh"
      to: "/agent mount (compose.yaml)"
      via: "hardcoded /agent paths for status dir, .mcp.json, and short-path substitution"
      pattern: "/agent"
---

<objective>
Fix the inner-container Claude statusline added in commit 9593607. The commit wrote `/workspace/...` paths into `workspace/.claude/settings.json` and `workspace/.claude/statusline.sh`, but `run_docker.sh` mounts `workspace/` at `/agent` inside the container (compose.yaml line 11, `working_dir: /agent`). There is no `/workspace` path inside the container, so the statusline command silently fails.

Purpose: restore the statusline for the agent running inside the container by aligning hardcoded paths with the actual mount point.

Output: updated settings.json and statusline.sh that reference `/agent` instead of `/workspace`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@workspace/.claude/settings.json
@workspace/.claude/statusline.sh
@compose.yaml
@run_docker.sh

<interfaces>
<!-- Relevant mount facts the executor must respect -->

From compose.yaml:
- `working_dir: /agent`
- Volume: `"${HOST_PWD:-.}:/agent"` where `HOST_PWD` is set by `run_docker.sh` to `$SCRIPT_DIR/workspace`

From run_docker.sh line 8:
- `HOST_PWD="$SCRIPT_DIR/workspace"` — repo's `workspace/` directory is mounted as `/agent/` in the container

Conclusion: inside the container, `/agent/.claude/statusline.sh` IS the same file as `workspace/.claude/statusline.sh` on the host. `/workspace` does not exist in the container.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix statusline command path in settings.json</name>
  <files>workspace/.claude/settings.json</files>
  <action>In `workspace/.claude/settings.json`, update the `statusLine.command` value from `bash /workspace/.claude/statusline.sh` to `bash /agent/.claude/statusline.sh`. No other keys change. Preserve JSON formatting (2-space indent, trailing newline).</action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('workspace/.claude/settings.json')); assert d['statusLine']['command']=='bash /agent/.claude/statusline.sh', d['statusLine']['command']; print('OK')"</automated>
  </verify>
  <done>`workspace/.claude/settings.json` parses as valid JSON and its `statusLine.command` is `bash /agent/.claude/statusline.sh`.</done>
</task>

<task type="auto">
  <name>Task 2: Replace /workspace with /agent in statusline.sh</name>
  <files>workspace/.claude/statusline.sh</files>
  <action>In `workspace/.claude/statusline.sh`, change the three `/workspace` references to `/agent`:

1. Line ~50: `if [ -d /workspace/status ]` → `if [ -d /agent/status ]`
2. Line ~51: `sample=$(ls -1t /workspace/status 2>/dev/null | head -1)` → `sample=$(ls -1t /agent/status 2>/dev/null | head -1)`
3. Line ~56: `if [ -f /workspace/.mcp.json ]` → `if [ -f /agent/.mcp.json ]`
4. Lines ~57-59: each `/workspace/.mcp.json` inside the `grep -q` commands → `/agent/.mcp.json`
5. Line ~65: `short_path="${short_path/#\/workspace/🔬}"` → `short_path="${short_path/#\/agent/🔬}"`

Leave the `/home/agent` substitution on line 66 untouched — that path exists in the container and remains valid.

Do not modify any other logic, formatting, or the shebang. This is a pure find/replace of the string `/workspace` to `/agent` across the file.</action>
  <verify>
    <automated>bash -n workspace/.claude/statusline.sh && ! grep -n "/workspace" workspace/.claude/statusline.sh && grep -q "/agent/status" workspace/.claude/statusline.sh && grep -q "/agent/.mcp.json" workspace/.claude/statusline.sh && grep -q 'short_path/#\\/agent' workspace/.claude/statusline.sh && echo OK</automated>
  </verify>
  <done>`workspace/.claude/statusline.sh` passes `bash -n` syntax check, contains no `/workspace` references, and contains `/agent/status`, `/agent/.mcp.json`, and the `/agent → 🔬` short-path substitution.</done>
</task>

</tasks>

<verification>
Quick sanity checks after both tasks:

1. `python3 -c "import json; json.load(open('workspace/.claude/settings.json'))"` — settings.json is valid JSON.
2. `bash -n workspace/.claude/statusline.sh` — statusline script has no syntax errors.
3. `grep -rn "/workspace" workspace/.claude/` — returns nothing (no stale paths).
4. Manual: next container session started via `./run_docker.sh` should render the statusline for Claude running inside the container (not a blocking automated check per quick-mode constraints).
</verification>

<success_criteria>
- `workspace/.claude/settings.json` points `statusLine.command` at `/agent/.claude/statusline.sh` and remains valid JSON.
- `workspace/.claude/statusline.sh` has zero `/workspace` occurrences, passes `bash -n`, and uses `/agent` for the status dir check, `.mcp.json` check, and the short-path substitution.
- No other files, logic, or behavior change.
</success_criteria>

<output>
After completion, create `.planning/quick/260423-f3k-fix-inner-agent-statusline-paths-workspa/260423-f3k-SUMMARY.md` documenting the paths fixed and confirming the two verification commands passed.
</output>
