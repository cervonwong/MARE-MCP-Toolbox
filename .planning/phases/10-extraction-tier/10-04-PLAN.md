---
phase: 10
plan: 04
type: execute
wave: 2
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - mcp-gateway/src/mcp_gateway/tools/extract.py
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-03, EXTR-04, EXTR-05, EXTR-06]

must_haves:
  truths:
    - "mcp_gateway.tools.extract module exists and exports exactly 7 MCP tool handlers + a register(mcp) function"
    - "run_binwalk discriminates by mode= signatures|entropy|extract (D-02) — sync return for first two, job dispatch for extract"
    - "run_unblob always dispatches a Phase 9 job (D-03) and returns the 25-key snapshot + engine/mode/extraction_dir/meta_path"
    - "run_upx_test, run_upx_list, run_upx_unpack are synchronous and PARSED (D-04, D-09)"
    - "list_extracted_files walks extracted/* engine-agnostically with per-extraction MAX_FILES_PER_EXTRACTION cap and cross-extraction limit cap (D-05)"
    - "promote_extracted_sample is atomic + idempotent by sha256 + writes _lineage.json (D-06, D-14)"
    - "All 4 long-form tools have the D-23 disclaimer spliced into __doc__; all 3 UPX tools have the short-form disclaimer"
    - "All 6 D-22 error shapes are returnable from the appropriate tools (never raise out of MCP)"
    - "Job-dispatched wrappers call extraction._spawn_monitor (not bare asyncio.create_task)"
  artifacts:
    - path: "mcp-gateway/src/mcp_gateway/tools/extract.py"
      provides: "7 @mcp.tool() handlers + register(mcp) + disclaimer splices + structured error dicts"
      min_lines: 450
      contains: "register"
  key_links:
    - from: "mcp-gateway/src/mcp_gateway/tools/extract.py::run_unblob"
      to: "mcp_gateway.extraction._spawn_monitor"
      via: "monitor spawn after job dispatch"
      pattern: "_spawn_monitor"
    - from: "mcp-gateway/src/mcp_gateway/tools/extract.py::run_unblob"
      to: "mcp_gateway.tools.jobs.start_tool_job"
      via: "Phase 9 job dispatch"
      pattern: "start_tool_job"
    - from: "mcp-gateway/src/mcp_gateway/tools/extract.py::promote_extracted_sample"
      to: "mcp_gateway.extraction.write_upload"
      via: "atomic re-upload"
      pattern: "extraction.write_upload"
    - from: "mcp-gateway/src/mcp_gateway/tools/extract.py::promote_extracted_sample"
      to: "scripts/init_status_tree.sh (via subprocess_runner.run_script)"
      via: "shellout to init new case dir (Pitfall 5 — init_case is a closure)"
      pattern: "init_status_tree.sh"
---

<objective>
Wave 2 surface: Create `mcp-gateway/src/mcp_gateway/tools/extract.py` — the MCP-facing surface for Phase 10. Seven `@mcp.tool()` handlers (D-01: run_binwalk, run_unblob, run_upx_test, run_upx_list, run_upx_unpack, list_extracted_files, promote_extracted_sample) + a `register(mcp)` function that wires them. Each handler:
- Validates inputs via resolve_case_dir / resolve_sample (returns structured error dict on failure per D-22)
- Layers onto Phase 6 D-03 12-key dict (sync) or Phase 9 D-19 25-key snapshot (async) per D-10
- Splices the D-23 disclaimer into __doc__
- Never raises out of the MCP boundary

Plan 04 reuses every primitive from Plan 02/03 (extraction.extraction_dir, write_meta, write_upload, _spawn_monitor, etc.) and Phase 9's tools.jobs.start_tool_job/get_tool_job/cancel_tool_job. Per Pitfall 5: `init_case` (Phase 1-4) is a closure inside `tools/artifacts.py::register(mcp)` — promotion shells out to `scripts/init_status_tree.sh` via `subprocess_runner.run_script` (matches `tools/artifacts.py:30-33`).

Purpose: This is the agent-visible surface. Plan 04 is where every CONTEXT decision becomes a callable MCP tool.

Output: 1 new file `tools/extract.py` ~450-650 LoC. Wave 0 RED stubs for `test_run_binwalk.py`, `test_run_unblob.py`, `test_run_upx.py`, `test_list_extracted_files.py`, `test_promote_extracted_sample.py`, `test_disclaimers.py` all collect cleanly after this plan. Plan 05 wires the registration in tools/__init__.py and flips RED→GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-extraction-tier/10-CONTEXT.md
@.planning/phases/10-extraction-tier/10-RESEARCH.md
@.planning/phases/10-extraction-tier/10-02-SUMMARY.md
@.planning/phases/10-extraction-tier/10-03-SUMMARY.md
@mcp-gateway/src/mcp_gateway/extraction.py
@mcp-gateway/src/mcp_gateway/tools/jobs.py
@mcp-gateway/src/mcp_gateway/tools/r2_sessions.py
@mcp-gateway/src/mcp_gateway/tools/artifacts.py
@mcp-gateway/src/mcp_gateway/tools/samples.py
@mcp-gateway/src/mcp_gateway/tools/case_dirs.py
@mcp-gateway/src/mcp_gateway/subprocess_runner.py
@mcp-gateway/src/mcp_gateway/runner.py

<interfaces>
Exact tool surface (D-01 — locked):

```python
async def run_binwalk(
    case_dir: str,
    sample: str,
    *,
    mode: Literal["signatures","entropy","extract"] = "signatures",
    ctx: Context | None = None,
) -> dict

async def run_unblob(
    case_dir: str,
    sample: str,
    *,
    depth: int = 8,
    ctx: Context | None = None,
) -> dict

async def run_upx_test(case_dir: str, sample: str) -> dict
async def run_upx_list(case_dir: str, sample: str) -> dict
async def run_upx_unpack(case_dir: str, sample: str) -> dict

async def list_extracted_files(
    case_dir: str,
    *,
    engine: Literal["binwalk","unblob","upx"] | None = None,
    limit: int = 500,
    include_quarantined: bool = True,
) -> dict

async def promote_extracted_sample(
    parent_case_dir: str,
    child_path: str,
    *,
    force_new: bool = False,
) -> dict

def register(mcp: FastMCP) -> None
```

Result-dict layering (D-10):
- Sync (binwalk signatures/entropy, UPX trio): Phase 6 D-03 12-key dict + Phase 10 extensions (engine, mode, extraction_dir, symlinks_quarantined, meta_path) + per-tool extras
- Async (binwalk extract, unblob): Phase 9 D-19 25-key snapshot + same Phase 10 extension keys

D-22 error shapes (6 locked — return, never raise):
1. invalid case_dir: `{"error": "invalid case_dir", "case_dir": str, "hint": str}`
2. invalid sample: `{"error": "invalid sample", "sample": str, "hint": str}`
3. unsupported binwalk version: `{"error": "unsupported binwalk version", "stderr_head": str, "hint": str}`
4. child not under extracted/: `{"error": "child_path must live under parent case's extracted/", "parent_case_dir": str, "child_path": str}`
5. symlink-quarantine sentinel: `{"error": "child is a symlink quarantine sentinel (.symlink-target.txt) — read it for the original target, do not promote it", "child_path": str}`
6. extraction cap exceeded: `{"error": "extraction cap exceeded", "cap_bytes": int, "observed_bytes": int, "extraction_dir": str, "hint": str}`

D-23 disclaimers:
- Long form (4 tools — run_unblob, run_binwalk extract portion, list_extracted_files, promote_extracted_sample): full 12-line text from CONTEXT D-23 lines 1009-1024
- Short form (3 UPX tools): trimmed 4-line version without the in-memory job mention
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement tools/extract.py — 7 handlers, helpers, error dicts, disclaimers, register</name>
  <files>mcp-gateway/src/mcp_gateway/tools/extract.py</files>
  <read_first>
    - mcp-gateway/src/mcp_gateway/extraction.py (Plan 02+03 output — confirm all helpers exist)
    - mcp-gateway/src/mcp_gateway/tools/jobs.py first 200 lines (the surface pattern: module-level coroutine functions + register-wraps-with-mcp.tool() + disclaimer splice via __doc__.replace — verbatim Phase 9 reference)
    - mcp-gateway/src/mcp_gateway/tools/r2_sessions.py first 100 lines (alternate disclaimer-splice pattern + module-attribute import idiom `from mcp_gateway import jobs` rather than `from mcp_gateway.jobs import NAME`)
    - mcp-gateway/src/mcp_gateway/tools/artifacts.py lines 25-35 (Pitfall 5: init_case is a closure inside register(mcp); we will SHELL OUT to scripts/init_status_tree.sh instead via subprocess_runner.run_script)
    - mcp-gateway/src/mcp_gateway/subprocess_runner.py (SCRIPTS constant + run_script signature)
    - mcp-gateway/src/mcp_gateway/tools/samples.py (resolve_sample + UPLOADS_ROOT + STATUS_ROOT + ALLOWED_PREFIXES + SHA256_RE)
    - mcp-gateway/src/mcp_gateway/tools/case_dirs.py (resolve_case_dir signature)
    - mcp-gateway/src/mcp_gateway/runner.py grep for `run_tool` signature (sync wrappers — UPX trio + binwalk signatures/entropy — invoke this)
    - .planning/phases/10-extraction-tier/10-CONTEXT.md lines 96-378 (D-01..D-06 tool surface decisions); 460-525 (D-09/D-10 parsing + layering); 700-740 (D-14 lineage); 963-1029 (D-22/D-23 errors + disclaimers)
    - .planning/phases/10-extraction-tier/10-RESEARCH.md "Pattern 1" (Plan 02 pattern reference + the run_unblob wrapper example); "Code Examples" Example 1 (binwalk3 argv shape — matches Plan 02 _build_binwalk_extract_argv)
    - .planning/phases/10-extraction-tier/10-RESEARCH.md Pitfall 1 (binwalk version drift), Pitfall 4 (job cap reached before monitor spawn — wrapper must check start_tool_job error result), Pitfall 5 (init_case closure → shellout instead), Pitfall 9 (promote symlink sentinel rejection)
    - mcp-gateway/tests/extraction/test_run_binwalk.py, test_run_unblob.py, test_run_upx.py, test_list_extracted_files.py, test_promote_extracted_sample.py, test_disclaimers.py (RED stubs from Plan 01 — test function names define what Plan 05 will assert)
  </read_first>
  <behavior>
    Per-tool flow contracts:

    `run_binwalk(case_dir, sample, mode, ctx)`:
    - try resolve_case_dir(case_dir) → on error return D-22 shape 1
    - try resolve_sample(sample) → on error return D-22 shape 2
    - if mode == "signatures":
       - extraction_path = extraction.extraction_dir(case_path, "binwalk")  # used as scan output dir
       - argv = ["binwalk", "-l", str(extraction_path/"binwalk-report.json"), "-q", "--", sample_abs]  # JSON log scan
       - res = await runner.run_tool(argv, case_dir=case_path, slug="binwalk-signatures")  # 12-key dict
       - Parse binwalk-report.json (if produced) into signatures: list[dict] OR fall back to text parsing of stdout into {offset_dec, offset_hex, description} rows. If parser CANNOT lock to a schema, return D-22 shape 3.
       - quarantine_count, _ = extraction.quarantine_symlinks(extraction_path) (UPX/binwalk-scan rarely produces symlinks; cheap defense)
       - extraction.write_meta with finalized status
       - Return: {**res, "engine":"binwalk", "mode":"signatures", "extraction_dir": str(extraction_path.relative_to(case_path)), "symlinks_quarantined": quarantine_count, "meta_path": "extracted/.../...", "signatures": rows, "entropy": None, "entropy_plot": None, "job_id": None}
    - if mode == "entropy":
       - Similar to signatures but argv = ["binwalk", "-E", "-C", str(extraction_path), "-q", "--", sample_abs]
       - Parse entropy summary table OR capture PNG plot into extraction_path
       - Return shape: {..., "entropy": list[{block_start, block_end, entropy}], "entropy_plot": str|None, "signatures": None}
    - if mode == "extract":
       - extraction_path = extraction.extraction_dir(case_path, "binwalk")
       - sample_sha = extraction._hash_file_streaming(sample_abs)
       - extraction.write_meta with initial status="running" (D-08 17-field shape)
       - snapshot = await tools_jobs.start_tool_job(tool="binwalk_extract", kwargs={...}, case_dir=str(case_path), ctx=ctx)
       - if snapshot has "error" key (Phase 9 D-15): Pitfall 4 — clean up empty extraction_path or update meta to status=failed; return snapshot as-is
       - extraction._spawn_monitor(snapshot["job_id"], extraction_path) (Plan 03 GC-safe spawn)
       - extraction.update_meta(extraction_path, {"job_id": snapshot["job_id"], "argv": snapshot.get("argv",[]), "log_path": snapshot.get("log_path","")})
       - Return: {**snapshot, "engine":"binwalk", "mode":"extract", "extraction_dir": str(extraction_path.relative_to(case_path)), "meta_path": "...", "symlinks_quarantined": 0}

    `run_unblob(case_dir, sample, depth=8, ctx)`:
    - Same exact flow as run_binwalk(mode="extract") but tool="unblob" and engine="unblob"
    - depth kwarg validated to 1..16; out-of-range → D-22 shape 2 (invalid sample) with hint mentioning depth (or a new "invalid argument" shape — KEEP IT SIMPLE: out-of-range returns the snapshot's error from Phase 9 D-15 because tools.jobs.start_tool_job's _validate_kwargs will reject it first; D-22 shape 1/2 only fire for case_dir/sample paths)
    - Returns 25-key snapshot + engine="unblob" + mode="extract" + extraction_dir + meta_path

    `run_upx_test(case_dir, sample)`:
    - resolve_case_dir, resolve_sample (errors return D-22 1/2)
    - argv = ["upx", "-t", "--", sample_abs]
    - res = await runner.run_tool(argv, case_dir=case_path, slug="upx-test", timeout_s=120.0)
    - Parse stderr for "Not packed by UPX" / "[OK]" / "[ERROR]":
       - is_upx_packed = "Not packed by UPX" not in stderr
       - test_result: "ok" if exit_code==0 and "[OK]" in stderr; "not_packed" if "Not packed by UPX" in stderr; "corrupt" if exit_code!=0 and "[ERROR]" in stderr; else "error"
    - Return: {**res, "engine":"upx", "mode":"test", "extraction_dir": None, "symlinks_quarantined": 0, "meta_path": None, "is_upx_packed": bool, "test_result": str}

    `run_upx_list(case_dir, sample)`:
    - argv = ["upx", "-l", "--", sample_abs]
    - res = await runner.run_tool(...)
    - Parse UPX -l stderr table columns into rows: list[{file, compressed_size, uncompressed_size, ratio, format, name}]. UPX -l output format is documented in https://github.com/upx/upx (lines like "File size Ratio Format Name").
    - Return: {**res, "engine":"upx", "mode":"list", "extraction_dir": None, "rows": rows, ...}

    `run_upx_unpack(case_dir, sample)`:
    - extraction_path = extraction.extraction_dir(case_path, "upx")
    - basename = Path(sample_abs).name
    - unpacked = extraction_path / f"{basename}.unpacked"
    - argv = ["upx", "-d", "-o", str(unpacked), "--", sample_abs]
    - res = await runner.run_tool(...)
    - extraction.quarantine_symlinks(extraction_path)
    - extraction.write_meta(extraction_path, {full 17-field shape with status="succeeded"|"failed"})
    - Return: {**res, "engine":"upx", "mode":"unpack", "extraction_dir": ..., "unpacked_path": str(unpacked.relative_to(case_path)), "unpacked_size": unpacked.stat().st_size if exists else 0, "symlinks_quarantined": count, ...}

    `list_extracted_files(case_dir, engine=None, limit=500, include_quarantined=True)`:
    - resolve_case_dir → on error D-22 shape 1
    - Cap limit at 10000 (silently clamp; matches D-05)
    - extractions = extraction.enumerate_extractions(case_path) — Plan 02 helper
    - For each extraction (filtered by engine= prefix on dir name):
       - Walk extraction's _dir_abs with depth-1 plus recurse (engine dirs can be deeply nested for unblob); collect regular files (not symlinks) up to MAX_FILES_PER_EXTRACTION (extraction.MAX_FILES_PER_EXTRACTION)
       - For each file: {path: case-rel, size: int, is_symlink_quarantine: name.endswith(".symlink-target.txt")}
       - Filter quarantine entries if include_quarantined=False
       - files_truncated = True if file count hit MAX_FILES_PER_EXTRACTION
    - Apply cross-extraction limit cap: accumulate files across extractions, set top-level truncated=True if total exceeds limit
    - Strip the internal `_dir_abs` and `_meta` keys before returning
    - Return D-05 shape: {case_dir: abs, extractions: [...], total_extractions: int, total_files_listed: int, truncated: bool}

    `promote_extracted_sample(parent_case_dir, child_path, force_new=False)`:
    - resolve_case_dir(parent_case_dir) → D-22 shape 1 on error
    - confine_to(parent_case_dir, child_path) → if outside parent_case_dir/extracted/** → D-22 shape 4
    - If child_path basename ends ".symlink-target.txt" → D-22 shape 5
    - sha = extraction._hash_file_streaming(child_path_abs)
    - basename = Path(child_path_abs).name; check _is_invalid_filename → D-22 shape 2-style hint
    - if not force_new: existing = _existing_case_for_sha256(sha) (helper added in this file OR moved to extraction.py — DECISION: put it in tools/extract.py since it scans STATUS_ROOT which is a tools.* concern); if existing: return idempotent_reuse=True dict
    - digest, target_path = extraction.write_upload(Path(child_path_abs), basename) — atomic; idempotent on UPLOADS_ROOT/<sha>/
    - Shell out to init new case dir (Pitfall 5):
       argv = ["bash", str(SCRIPTS / "init_status_tree.sh"), str(target_path), "--new"]
       init_res = await subprocess_runner.run_script(argv, cwd="/agent", timeout=60.0)
       - Parse init_res for new case_dir path (the script writes to status/<NNN>-<basename>/ — extract from stdout or list status/ before+after)
       - SIMPLER APPROACH: list STATUS_ROOT before init, run init, list after, diff = new case dir
    - Write `<new_case_dir>/_lineage.json` (D-14 shape: promoted_sha256, parent_case_dir, parent_extraction_dir, child_path, promoted_at, promoted_by="promote_extracted_sample", version=1)
    - Return D-06 10-key shape: {new_case_dir, new_case_name, sha256, dedup, idempotent_reuse, parent_case_dir, parent_extraction_dir, child_path, promoted_at, lineage_path}

    _existing_case_for_sha256(sha) helper (D-14 — co-located in tools/extract.py):
    - Iterate STATUS_ROOT/* sorted; for each that matches CASE_NAME_RE: read _lineage.json if present, return first match where promoted_sha256 == sha

    Disclaimer splice pattern (matches Phase 8 / Phase 9 r2_sessions / jobs):
    - Define module-level constants _EXTRACTION_DISCLAIMER_LONG and _EXTRACTION_DISCLAIMER_SHORT
    - Each function's docstring contains the placeholder `{_EXTRACTION_DISCLAIMER}` (long or short)
    - After all functions defined, do: `run_unblob.__doc__ = (run_unblob.__doc__ or "").replace("{_EXTRACTION_DISCLAIMER}", _EXTRACTION_DISCLAIMER_LONG)` etc for each tool
    - run_binwalk gets the LONG form (D-23 says "extract portion" of run_binwalk gets the disclaimer; for simplicity splice the long form into all of run_binwalk's docstring)
    - list_extracted_files, promote_extracted_sample, run_unblob: LONG
    - run_upx_test, run_upx_list, run_upx_unpack: SHORT

    Long disclaimer text (verbatim from CONTEXT D-23):
    ```
    Extraction state lives in case_dir/extracted/<engine>-<ts>-<rand4>/
    with a `_mare_meta.json` provenance sidecar. The in-memory job
    registry for unblob/binwalk_extract is volatile (gateway restart
    cancels in-flight jobs and forgets terminal jobs), but the on-disk
    extraction tree + sidecar are preserved.

    Promotion lineage lives in <new_case_dir>/_lineage.json. Promotion
    is idempotent by sha256 (re-promoting the same child returns the
    existing case dir); pass force_new=True to bypass.

    Extraction tools are shared across all bearer-token clients
    (no per-Mcp-Session-Id keying). Any client with the bearer token
    can see and cancel any extraction job. (Per-session keying deferred
    to v1.2.)
    ```

    Short disclaimer (UPX tools — no in-memory job mention):
    ```
    Extraction state lives in case_dir/extracted/upx-<ts>-<rand4>/
    with a `_mare_meta.json` provenance sidecar.

    UPX tools are shared across all bearer-token clients
    (no per-Mcp-Session-Id keying). (Per-session keying deferred to v1.2.)
    ```

    register(mcp) function:
    - Per Phase 9 r2_sessions/jobs pattern: `mcp.tool()(run_binwalk); mcp.tool()(run_unblob); ...; mcp.tool()(promote_extracted_sample)` — register all 7 tools by wrapping with mcp.tool() at registration time (not @decorator at definition — allows the module to be imported without a FastMCP instance and tests to call the coroutines directly)
  </behavior>
  <action>
    Create `mcp-gateway/src/mcp_gateway/tools/extract.py` using the Write tool. Total ~450-650 LoC.

    Structure (top to bottom):
    1. Module docstring documenting Phase 10 D-01..D-23 scope
    2. `from __future__ import annotations` + imports:
       - stdlib: asyncio, json, re, datetime, pathlib, typing
       - mcp: from mcp.server.fastmcp import FastMCP, Context
       - mcp_gateway primitives:
         ```
         from mcp_gateway import extraction
         from mcp_gateway.runner import run_tool
         from mcp_gateway.subprocess_runner import SCRIPTS, run_script
         from mcp_gateway.tools import jobs as tools_jobs
         from mcp_gateway.tools.case_dirs import resolve_case_dir
         from mcp_gateway.tools.samples import resolve_sample, UPLOADS_ROOT, STATUS_ROOT, SHA256_RE
         from mcp_gateway.tools.cases import CASE_NAME_RE
         from mcp_gateway.uploads import _is_invalid_filename
         from mcp_gateway.artifacts_io import confine_to
         ```
    3. Module constants:
       - `_EXTRACTION_DISCLAIMER_LONG` = the 12-line verbatim text from D-23
       - `_EXTRACTION_DISCLAIMER_SHORT` = the 4-line UPX-tools form
       - `_BINWALK_SIG_RE`, `_UPX_LIST_RE` and other parser regexes
    4. Private helpers:
       - `_utc_now_iso()` (or import from extraction module if exported — Plan 02 has `_utc_now_iso` as private; this file may redefine inline)
       - `_existing_case_for_sha256(sha: str) -> Path | None` (D-14 scan helper)
       - `_parse_binwalk_signatures(report_json_path: Path, stdout: str) -> list[dict] | None`
       - `_parse_binwalk_entropy(extraction_path: Path, stdout: str) -> list[dict]`
       - `_parse_upx_test_stderr(stderr: str, exit_code: int) -> tuple[bool, str]` → (is_packed, test_result)
       - `_parse_upx_list_stderr(stderr: str) -> list[dict]`
       - `_validate_child_path(parent_case_dir: Path, child_path: str) -> tuple[Path, dict | None]` — returns (resolved_path, error_dict_or_None)
       - `_init_new_case_for_sha(sha: str, target_basename: str, target_path: Path) -> Path` — shellout to scripts/init_status_tree.sh, returns new case dir (lists STATUS_ROOT before+after to identify new dir)
       - `_write_lineage_json(new_case_dir: Path, payload: dict) -> Path`
    5. Seven async functions (run_binwalk, run_unblob, run_upx_test, run_upx_list, run_upx_unpack, list_extracted_files, promote_extracted_sample) — each implements its behaviour contract above
    6. Disclaimer splice block: `run_unblob.__doc__ = (run_unblob.__doc__ or "").replace("{_EXTRACTION_DISCLAIMER}", _EXTRACTION_DISCLAIMER_LONG)` for the 4 long-form + 3 short-form
    7. `def register(mcp: FastMCP) -> None:` — calls `mcp.tool()(run_binwalk)` through all 7

    Implementation rules (defensive):
    - Every handler's outermost try/except: `try ... except Exception as e: log.exception(...); return {"error": "internal", "hint": str(e)}` — defense in depth even though tools should not raise
    - Use `extraction._spawn_monitor` (NOT bare asyncio.create_task) for job-dispatched wrappers
    - When tools_jobs.start_tool_job returns an error dict (Phase 9 D-15 shape — keys include "error"), Pitfall 4: update_meta with status="failed", do NOT spawn monitor, return the error dict unchanged
    - Use `case_path` (Path object from resolve_case_dir) consistently for confine_to / extraction.extraction_dir; convert to str only when crossing into tools_jobs (it expects str)
    - For promote_extracted_sample STATUS_ROOT diff trick:
      ```
      pre = {p.name for p in STATUS_ROOT.iterdir() if p.is_dir()}
      init_result = await run_script(argv, cwd="/agent", timeout=60.0)
      post = {p.name for p in STATUS_ROOT.iterdir() if p.is_dir()}
      new_cases = sorted(post - pre)
      if not new_cases:
          # init failed silently — return error
          return {"error": "init_case shellout did not create a new case dir", "stderr_head": init_result.get("stderr_head","")}
      new_case_dir = STATUS_ROOT / new_cases[-1]
      ```

    Self-check before commit:
    - `python -c "from mcp_gateway.tools import extract; print([n for n in dir(extract) if not n.startswith('_')])"` lists the 7 tool function names + `register`
    - `python -c "from mcp_gateway.tools import extract; assert callable(extract.register)"`
    - Disclaimer present in 4 long-form tools: `python -c "from mcp_gateway.tools import extract; assert 'in-memory job' in extract.run_unblob.__doc__; assert 'in-memory job' in extract.list_extracted_files.__doc__; assert 'in-memory job' in extract.promote_extracted_sample.__doc__"`
    - All RED-stub tests collect: `cd mcp-gateway && python -m pytest tests/extraction/ --collect-only -q` exits 0
  </action>
  <verify>
    <automated>cd mcp-gateway && python -c "from mcp_gateway.tools import extract; assert callable(extract.run_binwalk); assert callable(extract.run_unblob); assert callable(extract.run_upx_test); assert callable(extract.run_upx_list); assert callable(extract.run_upx_unpack); assert callable(extract.list_extracted_files); assert callable(extract.promote_extracted_sample); assert callable(extract.register); print('surface_ok')" && python -m pytest tests/extraction/ --collect-only -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from mcp_gateway.tools import extract"` exits 0
    - All 7 tool function names exist and are callable: `python -c "from mcp_gateway.tools import extract; [getattr(extract,n) for n in ['run_binwalk','run_unblob','run_upx_test','run_upx_list','run_upx_unpack','list_extracted_files','promote_extracted_sample','register']]"` exits 0
    - `grep -cE 'async def (run_binwalk|run_unblob|run_upx_test|run_upx_list|run_upx_unpack|list_extracted_files|promote_extracted_sample)' mcp-gateway/src/mcp_gateway/tools/extract.py` returns 7
    - `grep -c 'def register(' mcp-gateway/src/mcp_gateway/tools/extract.py` returns 1
    - `grep -c '_spawn_monitor' mcp-gateway/src/mcp_gateway/tools/extract.py` returns ≥ 2 (binwalk extract path + unblob path)
    - `grep -c 'extraction.write_upload\|extraction\.write_upload' mcp-gateway/src/mcp_gateway/tools/extract.py` returns ≥ 1
    - `grep -c 'init_status_tree.sh' mcp-gateway/src/mcp_gateway/tools/extract.py` returns ≥ 1 (Pitfall 5 — shellout)
    - `grep -c 'force_new\|idempotent_reuse' mcp-gateway/src/mcp_gateway/tools/extract.py` returns ≥ 2
    - `grep -c '"error":\s*"invalid case_dir"\|"error": "invalid case_dir"' mcp-gateway/src/mcp_gateway/tools/extract.py` returns ≥ 1
    - `grep -c '"error":\s*"invalid sample"\|"error": "invalid sample"' mcp-gateway/src/mcp_gateway/tools/extract.py` returns ≥ 1
    - `grep -c 'symlink quarantine sentinel\|.symlink-target.txt' mcp-gateway/src/mcp_gateway/tools/extract.py` returns ≥ 1
    - `grep -c 'extraction cap exceeded\|cap_bytes' mcp-gateway/src/mcp_gateway/tools/extract.py` returns ≥ 1
    - Long disclaimer spliced: `python -c "from mcp_gateway.tools import extract; assert 'in-memory job' in extract.run_unblob.__doc__"` exits 0
    - Long disclaimer in list_extracted_files: `python -c "from mcp_gateway.tools import extract; assert 'shared across all bearer-token clients' in extract.list_extracted_files.__doc__"` exits 0
    - Short disclaimer in UPX tools: `python -c "from mcp_gateway.tools import extract; assert 'shared across all bearer-token clients' in extract.run_upx_test.__doc__"` exits 0
    - Long disclaimer NOT in UPX (UPX gets short form which lacks "in-memory job"): `python -c "from mcp_gateway.tools import extract; assert 'in-memory job' not in (extract.run_upx_test.__doc__ or '')"` exits 0
    - `cd mcp-gateway && python -m pytest tests/extraction/ --collect-only -q` exits 0 (every test file collects)
    - `grep -c 'asyncio.create_task' mcp-gateway/src/mcp_gateway/tools/extract.py` returns 0 (use _spawn_monitor instead — centralized retention)
    - `grep -c '@mcp.tool' mcp-gateway/src/mcp_gateway/tools/extract.py` returns 0 (tools are wrapped via mcp.tool()(fn) in register, NOT @decorator — matches Phase 8/9 pattern; Phase 8 r2_sessions notes Rule 1 fix on the decorator idiom)
    - `grep -c 'mcp.tool()(' mcp-gateway/src/mcp_gateway/tools/extract.py` returns 7 (register wraps all 7 tools)
  </acceptance_criteria>
  <done>tools/extract.py created; 7 MCP tool handlers + register(mcp) defined; disclaimers spliced (long form on 4 tools, short form on 3); 6 D-22 error shapes returnable; job-dispatched wrappers use _spawn_monitor; promotion shells out to init_status_tree.sh (Pitfall 5); all Wave 0 RED stubs collect cleanly.</done>
</task>

</tasks>

<threat_model>
  <asvs_level>1</asvs_level>
  <threats>
    <threat id="T-10-04-01">
      <description>Argv injection via case_dir/sample/child_path kwargs (a sample name like `--exec=malicious` or a child_path with `..` traversal could escape into subprocess argv).</description>
      <severity>high</severity>
      <mitigation>Every handler validates case_dir via resolve_case_dir (STATUS_ROOT containment) and sample via resolve_sample (ALLOWED_PREFIXES allowlist); child_path validated via confine_to(parent_case_dir, child_path) (rejects traversal) AND additional check that resolved path starts with `<parent_case_dir>/extracted/`. Plan 02's argv builders use `--` separator before sample (defense in depth). Acceptance criterion `grep -c '"error": "invalid case_dir"' ≥ 1` enforces error path is implemented.</mitigation>
    </threat>
    <threat id="T-10-04-02">
      <description>Promotion of attacker-named binary with traversal/control chars in basename writes into wrong UPLOADS_ROOT subtree.</description>
      <severity>high</severity>
      <mitigation>`extraction.write_upload` calls `_is_invalid_filename(target_basename)` (Phase 2 predicate — rejects `/`, `\\`, `..`, leading dot, control chars). Acceptance criterion `grep -c 'write_upload' ≥ 1` enforces use of the audited primitive; Plan 02 acceptance criteria enforce the predicate's presence inside write_upload.</mitigation>
    </threat>
    <threat id="T-10-04-03">
      <description>Promotion of a `.symlink-target.txt` sentinel as if it were a real sample creates a "case" whose sha256 is the hash of the sentinel text, not any real binary; analyst confusion + wasted analysis effort.</description>
      <severity>medium</severity>
      <mitigation>Handler explicitly checks `child_path basename.endswith(".symlink-target.txt")` AFTER confine_to but BEFORE hashing; returns D-22 shape 5 error dict. Acceptance criterion `grep -c '.symlink-target.txt' ≥ 1` enforces.</mitigation>
    </threat>
    <threat id="T-10-04-04">
      <description>Job cap reached (Phase 9 MAX_JOBS_INFLIGHT) → start_tool_job returns error → wrapper has already minted extraction_dir + wrote running-status meta → orphaned `running` sidecar forever, monitor never spawned, analyst confused.</description>
      <severity>medium</severity>
      <mitigation>Wrapper checks `if "error" in snapshot:` BEFORE calling _spawn_monitor; on error, update_meta with status="failed" + an explanation in argv field, do NOT spawn monitor, return the error dict unchanged. Pitfall 4 explicitly addressed in behavior block.</mitigation>
    </threat>
    <threat id="T-10-04-05">
      <description>Tools raise an unhandled exception out of the MCP boundary, breaking the gateway's `tools never raise` invariant (Phase 6 D-04 / Phase 9 D-15) and causing client errors instead of structured error dicts.</description>
      <severity>medium</severity>
      <mitigation>Every handler has an outermost try/except returning a fallback `{"error": "internal", ...}` dict (action block). Each input-validation step has its own try/except returning a specific D-22 shape. Phase 9 D-15 contract preserved.</mitigation>
    </threat>
    <threat id="T-10-04-06">
      <description>list_extracted_files response exceeds MCP 25k-token cap on firmware extractions with 10k+ files, breaking the client.</description>
      <severity>medium</severity>
      <mitigation>Per-extraction cap MAX_FILES_PER_EXTRACTION (default 5000 from Plan 02 env constant); cross-extraction `limit` kwarg default 500 max 10000; `files_truncated` / `truncated` booleans surfaced so agents know to use range-read via Phase 7 get_artifact_tree or Resources for the full tree (D-05).</mitigation>
    </threat>
    <threat id="T-10-04-07">
      <description>UPX-output parser misreads a malformed `upx -l` table and either crashes (raises) or returns wrong data (e.g., negative size, ratio > 1.0) that misleads downstream analysis.</description>
      <severity>low</severity>
      <mitigation>D-09 robust-default rule: if a parser can't extract a field, the row contains `raw: str` with the original line and structured fields = None. Parsers never crash on edge inputs (zero-length, ANSI-coloured, partial). Tests assert this (Plan 05 fills bodies). Worst case: agent sees raw lines and can fall back to run_shell parsing.</mitigation>
    </threat>
    <threat id="T-10-04-08">
      <description>Symlink in UPX-unpack output (rare but possible) goes unquarantined because UPX wrappers are sync and don't spawn a monitor; Resources walker follows the symlink → host file disclosure.</description>
      <severity>medium</severity>
      <mitigation>run_upx_unpack calls extraction.quarantine_symlinks(extraction_path) INLINE after subprocess returns (matches CONTEXT D-15 sync-wrapper timing rule: "called from inside the wrapper AFTER the subprocess returns and BEFORE the result dict is returned"). Same applies to run_binwalk(mode=signatures|entropy) inline quarantine for paranoia.</mitigation>
    </threat>
    <threat id="T-10-04-09">
      <description>Disclaimer text drifts from D-23's locked wording across phases (e.g. Plan 04 paraphrases "in-memory" as "ephemeral"), breaking Plan 05's regression test that asserts verbatim phrases.</description>
      <severity>low</severity>
      <mitigation>Plan 04 defines `_EXTRACTION_DISCLAIMER_LONG` and `_EXTRACTION_DISCLAIMER_SHORT` as module-level string constants with verbatim D-23 text; splice into __doc__ via .replace pattern (Phase 8/9 precedent). test_disclaimers.py (Plan 05 body fill-in) will assert specific phrases.</mitigation>
    </threat>
  </threats>
</threat_model>

<verification>
- `python -c "from mcp_gateway.tools import extract"` exits 0
- All 7 tool handlers + register exist and are callable
- 6 D-22 error shapes present in source (grep-verifiable)
- Long disclaimer spliced into 4 tools (verified via __doc__ substring check); short disclaimer in 3 UPX tools
- _spawn_monitor used (not bare asyncio.create_task) — `grep -c 'asyncio.create_task' = 0` and `grep -c '_spawn_monitor' ≥ 2`
- All 13 Wave 0 RED-stub test files collect cleanly
</verification>

<success_criteria>
1. mcp-gateway/src/mcp_gateway/tools/extract.py exists (~450-650 LoC)
2. Module imports without error; 7 tool functions + register all callable
3. D-23 disclaimers spliced verbatim into __doc__ (long form on run_unblob/run_binwalk/list_extracted_files/promote_extracted_sample; short form on the 3 UPX tools)
4. 6 D-22 error shapes implemented (grep-verifiable on error literals)
5. Job-dispatched wrappers (run_binwalk extract mode + run_unblob) use extraction._spawn_monitor (centralized GC-safe spawn site)
6. Promotion shells out to scripts/init_status_tree.sh (Pitfall 5 respected — no `from mcp_gateway.tools.artifacts import init_case` attempted)
7. Threat-model mitigations are in code (write_upload + _is_invalid_filename for basename safety, confine_to + extracted/ prefix check for child_path, symlink-sentinel rejection, Job-cap-reached early-return)
</success_criteria>

<output>
After completion, create `.planning/phases/10-extraction-tier/10-04-SUMMARY.md` documenting:
- File created (final LoC count)
- The 7 tool surface signatures (verbatim final positional + kwarg shape)
- Disclaimer-splice confirmation (which constant goes into which tools)
- D-22 error-shape inventory (the 6 error-key strings actually returned)
- Confirmation that test collection still passes for all 13 RED stubs
- Any deviations from the action block's template (with rationale per Rule 1/2/3)
</output>
