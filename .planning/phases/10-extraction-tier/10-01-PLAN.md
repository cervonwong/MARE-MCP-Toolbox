---
phase: 10
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Dockerfile
  - mcp-gateway/tests/extraction/__init__.py
  - mcp-gateway/tests/extraction/conftest.py
  - mcp-gateway/tests/extraction/test_extraction_dir.py
  - mcp-gateway/tests/extraction/test_meta_sidecar.py
  - mcp-gateway/tests/extraction/test_quarantine_symlinks.py
  - mcp-gateway/tests/extraction/test_extract_monitor.py
  - mcp-gateway/tests/extraction/test_list_extracted_files.py
  - mcp-gateway/tests/extraction/test_promote_extracted_sample.py
  - mcp-gateway/tests/extraction/test_run_binwalk.py
  - mcp-gateway/tests/extraction/test_run_unblob.py
  - mcp-gateway/tests/extraction/test_run_upx.py
  - mcp-gateway/tests/extraction/test_job_specs_unblob.py
  - mcp-gateway/tests/extraction/test_job_specs_binwalk_extract.py
  - mcp-gateway/tests/extraction/test_disclaimers.py
  - mcp-gateway/tests/extraction/test_tool_list_phase10.py
  - scripts/probe_extraction_tools.sh
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-03, EXTR-04, EXTR-05, EXTR-06]

must_haves:
  truths:
    - "All 13 extraction test files + conftest.py + __init__.py exist under mcp-gateway/tests/extraction/"
    - "Every test file imports the not-yet-existing Phase 10 modules at function top (RED-stub discipline)"
    - "pytest collection passes (no syntax errors); pytest execution fails with ImportError for tests of unbuilt modules"
    - "Dockerfile is migrated from binwalk (v2) to binwalk3 (v3) for the Kali apt install line"
    - "A probe script exists that prints binwalk/unblob/upx --version output for in-container Wave-0 verification (resolves Assumption A1/A2/A3)"
  artifacts:
    - path: "Dockerfile"
      provides: "binwalk3 apt install (replaces binwalk v2 line)"
      contains: "binwalk3"
    - path: "mcp-gateway/tests/extraction/__init__.py"
      provides: "package marker"
      min_lines: 0
    - path: "mcp-gateway/tests/extraction/conftest.py"
      provides: "_require_binwalk_or_skip, _require_unblob_or_skip, _require_upx_or_skip, fake-tree builder helpers"
      min_lines: 40
    - path: "mcp-gateway/tests/extraction/test_extraction_dir.py"
      provides: "RED stubs for extraction_dir naming + rand4 collision"
      min_lines: 30
    - path: "mcp-gateway/tests/extraction/test_meta_sidecar.py"
      provides: "RED stubs for write_meta/update_meta/read_meta semantics"
      min_lines: 25
    - path: "mcp-gateway/tests/extraction/test_quarantine_symlinks.py"
      provides: "RED stubs for D-15/D-16 sweep + sentinel body format"
      min_lines: 30
    - path: "mcp-gateway/tests/extraction/test_extract_monitor.py"
      provides: "RED stubs for D-17 cap-exceeded + clean exit"
      min_lines: 30
    - path: "mcp-gateway/tests/extraction/test_list_extracted_files.py"
      provides: "RED stubs for D-05 caps + engine filter + quarantine flag"
      min_lines: 40
    - path: "mcp-gateway/tests/extraction/test_promote_extracted_sample.py"
      provides: "RED stubs for D-06/D-13/D-14 atomic + idempotent + force_new + lineage"
      min_lines: 40
    - path: "mcp-gateway/tests/extraction/test_run_binwalk.py"
      provides: "RED stubs for D-02 mode-discriminated returns"
      min_lines: 30
    - path: "mcp-gateway/tests/extraction/test_run_unblob.py"
      provides: "RED stubs for D-03 job dispatch + meta sidecar updates"
      min_lines: 30
    - path: "mcp-gateway/tests/extraction/test_run_upx.py"
      provides: "RED stubs for D-04 test/list/unpack parsing"
      min_lines: 30
    - path: "mcp-gateway/tests/extraction/test_job_specs_unblob.py"
      provides: "RED stubs for _build_unblob_argv pure-function tests"
      min_lines: 25
    - path: "mcp-gateway/tests/extraction/test_job_specs_binwalk_extract.py"
      provides: "RED stubs for _build_binwalk_extract_argv pure-function tests"
      min_lines: 25
    - path: "mcp-gateway/tests/extraction/test_disclaimers.py"
      provides: "RED stubs for D-23 docstring regression (4 long + 3 short)"
      min_lines: 25
    - path: "mcp-gateway/tests/extraction/test_tool_list_phase10.py"
      provides: "RED stubs for EXPECTED_TOOLS bump 47 -> 54 invariant"
      min_lines: 25
    - path: "scripts/probe_extraction_tools.sh"
      provides: "in-container probe of binwalk/unblob/upx versions and CLI flags"
      min_lines: 15
  key_links:
    - from: "Dockerfile"
      to: "binwalk3 apt package"
      via: "apt-get install"
      pattern: "binwalk3"
    - from: "mcp-gateway/tests/extraction/test_extraction_dir.py"
      to: "mcp_gateway.extraction (not yet existing)"
      via: "function-top import"
      pattern: "from mcp_gateway import extraction"
    - from: "mcp-gateway/tests/extraction/test_run_unblob.py"
      to: "mcp_gateway.tools.extract (not yet existing)"
      via: "function-top import"
      pattern: "from mcp_gateway.tools import extract"
---

<objective>
Wave 0: Land the 13-file RED-stub test scaffold for Phase 10 (mirroring Phase 6/7/8/9 precedent), migrate the Dockerfile from binwalk v2 to binwalk3 (binwalk v2 EOL'd 2025-12-12), and create an in-container probe script that resolves Assumptions A1/A2/A3 from 10-RESEARCH.md (binwalk3 availability, binwalk3 --depth flag absence, unblob Rich Progress emission shape).

Purpose: Establish Nyquist-compliant test scaffold BEFORE implementation. Every Phase 10 EXTR-XX requirement gets at least one RED-stub test that Wave 1/2 flips GREEN. Migrating to binwalk3 in this wave unblocks Plan 02's argv builders (which target binwalk3's CLI).

Output:
- 16 new files under mcp-gateway/tests/extraction/ (13 test files + __init__.py + conftest.py + a placeholder; total 15)
- 1 Dockerfile edit (binwalk → binwalk3)
- 1 new probe script (scripts/probe_extraction_tools.sh)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-extraction-tier/10-CONTEXT.md
@.planning/phases/10-extraction-tier/10-RESEARCH.md
@.planning/phases/10-extraction-tier/10-VALIDATION.md
@mcp-gateway/tests/test_tool_list.py
@mcp-gateway/src/mcp_gateway/jobs.py
@Dockerfile

<interfaces>
<!-- Phase 10 will create these — Wave 0 tests reference them by name. Do NOT implement here. -->

Future mcp_gateway.extraction module exports (Plan 02 + Plan 03 deliver):
```python
# Public surface (locked per CONTEXT D-19 Discretion):
def extraction_dir(case_dir: str | Path, engine: Literal["binwalk","unblob","upx"]) -> Path
def quarantine_symlinks(extraction_dir: Path) -> tuple[int, list[str]]
def write_meta(extraction_dir: Path, payload: dict) -> Path
def update_meta(extraction_dir: Path, patch: dict) -> dict
def read_meta(extraction_dir: Path) -> dict
def enumerate_extractions(case_dir: str | Path) -> list[dict]
async def start_extract_monitor(job_id: str, extraction_dir: Path, *, interval_s: float | None = None, max_bytes: int | None = None) -> None
def write_upload(child_path: Path, target_basename: str) -> tuple[str, Path]

# Pure argv builders for JobToolSpec (Plan 02):
def _build_unblob_argv(case_dir: Path, kwargs: dict) -> list[str]
def _build_binwalk_extract_argv(case_dir: Path, kwargs: dict) -> list[str]

# Module-level env constants (CONTEXT D-18):
MAX_EXTRACT_MB: int            # default 4096 from MCP_GATEWAY_MAX_EXTRACT_MB
EXTRACT_MONITOR_INTERVAL_S: float  # default 5.0 from MCP_GATEWAY_EXTRACT_MONITOR_INTERVAL_S
MAX_FILES_PER_EXTRACTION: int  # default 5000 from MCP_GATEWAY_LIST_EXTRACT_FILES_PER_EXTRACTION
MAX_EXTRACT_BYTES: int         # = MAX_EXTRACT_MB * 1024 * 1024
```

Future mcp_gateway.tools.extract module exports (Plan 04):
```python
async def run_binwalk(case_dir: str, sample: str, *, mode: Literal["signatures","entropy","extract"] = "signatures", ctx=None) -> dict
async def run_unblob(case_dir: str, sample: str, *, depth: int = 8, ctx=None) -> dict
async def run_upx_test(case_dir: str, sample: str) -> dict
async def run_upx_list(case_dir: str, sample: str) -> dict
async def run_upx_unpack(case_dir: str, sample: str) -> dict
async def list_extracted_files(case_dir: str, *, engine=None, limit: int = 500, include_quarantined: bool = True) -> dict
async def promote_extracted_sample(parent_case_dir: str, child_path: str, *, force_new: bool = False) -> dict
def register(mcp: FastMCP) -> None
```

Existing reuse (read first):
- mcp_gateway.runner.ReToolRunner / run_tool — sync subprocess execution (Phase 6 D-01..D-04 12-key dict)
- mcp_gateway.jobs.JobToolSpec, register_job_tool, JOB_TOOL_REGISTRY — Phase 9 spec registry
- mcp_gateway.tools.jobs.start_tool_job / get_tool_job / cancel_tool_job — Phase 9 surface (25-key dict)
- mcp_gateway.artifacts_io.confine_to, ensure_subdir, tool_log_path, EXPANDED_CASE_SUBDIRS — Phase 6/7
- mcp_gateway.tools.samples.resolve_sample, UPLOADS_ROOT, EXAMPLES_ROOT, STATUS_ROOT — Phase 2
- mcp_gateway.uploads.UPLOAD_DIR, MAX_BYTES, _is_invalid_filename — atomic upload pattern
- mcp_gateway.tools.case_dirs.resolve_case_dir — STATUS_ROOT validation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Dockerfile from binwalk to binwalk3 + create in-container probe script</name>
  <files>Dockerfile, scripts/probe_extraction_tools.sh</files>
  <read_first>
    - Dockerfile (current state — need to find the `binwalk` apt install line)
    - .planning/phases/10-extraction-tier/10-RESEARCH.md (Standard Stack table + Pitfall 1 + Assumption A1/A2/A3 in the Assumptions Log)
    - .planning/phases/10-extraction-tier/10-CONTEXT.md lines 1180-1195 (Dockerfile reference confirming binwalk/unblob/upx-ucl pre-installed)
  </read_first>
  <action>
    Step A — Edit Dockerfile:
    1. Locate the apt-get install line in Dockerfile that includes the word `binwalk` standalone (NOT `binwalk3`, NOT a comment). Per 10-RESEARCH.md Standard Stack table this is currently around Dockerfile:52, but the executor MUST grep to confirm the exact line: `grep -n '^[[:space:]]*binwalk[[:space:]]' Dockerfile` or `grep -n 'binwalk' Dockerfile`.
    2. Replace the standalone `binwalk \` (or `binwalk\n`) token with `binwalk3 \` (preserving the trailing backslash + newline if present). Add an above-line comment exactly: `# binwalk v2.4.3 reached EOL 2025-12-12 -- migrated to binwalk3 (Rust v3.1.0+, kali-rolling 2026-03-09)`. The comment MUST appear on the line immediately above the `binwalk3 \` line.
    3. Do NOT change any other Dockerfile content. `unblob`, `upx-ucl`, and the rest of the install list stay as-is.

    Step B — Create scripts/probe_extraction_tools.sh:
    1. Make the script executable (chmod 0755 effect via shebang `#!/usr/bin/env bash`).
    2. Script body:
       ```bash
       #!/usr/bin/env bash
       # Phase 10 Wave 0 probe — resolves Assumptions A1/A2/A3 from 10-RESEARCH.md.
       # Run INSIDE the container after `./run_docker.sh` rebuild:
       #   docker exec -it <container> bash /agent/scripts/probe_extraction_tools.sh
       set -u
       echo "=== binwalk ==="
       command -v binwalk && binwalk --version 2>&1 | head -3
       echo
       echo "=== binwalk --help (look for -d/--depth — A2: should be ABSENT in binwalk3) ==="
       binwalk --help 2>&1 | grep -E -- '(--depth|^[[:space:]]*-d,)' || echo "(no --depth flag found — confirms binwalk3)"
       echo
       echo "=== unblob ==="
       command -v unblob && unblob --version 2>&1 | head -3
       echo
       echo "=== upx-ucl / upx ==="
       command -v upx && upx --version 2>&1 | head -3
       echo
       echo "=== apt policy binwalk3 (A1 confirmation) ==="
       apt-cache policy binwalk3 2>&1 | head -5
       ```
    3. The script MUST run cleanly with `set -u` (no unbound vars). Failures from `command -v` are tolerated (the `&&` short-circuits cleanly).

    Step C — Verify the parent directory `scripts/` exists (it should — Phase 5 wrote scripts/compute_image_hash.sh there).
  </action>
  <verify>
    <automated>grep -c '^[[:space:]]*binwalk3' Dockerfile | grep -q '^[1-9]' && test -x scripts/probe_extraction_tools.sh && bash -n scripts/probe_extraction_tools.sh && ! grep -q '^[[:space:]]*binwalk[[:space:]]' Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^[[:space:]]*binwalk3' Dockerfile` returns a count ≥ 1
    - `grep '^[[:space:]]*binwalk[[:space:]]' Dockerfile` returns NO matches (i.e. the old standalone binwalk line is gone — must not match either with trailing space or backslash; only `binwalk3` should remain)
    - `grep -B1 'binwalk3' Dockerfile` shows the EOL comment line directly above
    - `test -x scripts/probe_extraction_tools.sh` succeeds (file is executable)
    - `bash -n scripts/probe_extraction_tools.sh` returns 0 (syntax check)
    - `grep -c 'binwalk' scripts/probe_extraction_tools.sh` returns ≥ 4 (covers version, help, apt-cache lines)
    - `grep -c 'unblob' scripts/probe_extraction_tools.sh` returns ≥ 2
    - `grep -c 'upx' scripts/probe_extraction_tools.sh` returns ≥ 2
  </acceptance_criteria>
  <done>Dockerfile migrated to binwalk3 with EOL comment; probe script exists, is executable, passes bash -n, and covers all three tools + the A2 --depth-flag absence check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create extraction/__init__.py + conftest.py + 13 RED-stub test files</name>
  <files>
    mcp-gateway/tests/extraction/__init__.py,
    mcp-gateway/tests/extraction/conftest.py,
    mcp-gateway/tests/extraction/test_extraction_dir.py,
    mcp-gateway/tests/extraction/test_meta_sidecar.py,
    mcp-gateway/tests/extraction/test_quarantine_symlinks.py,
    mcp-gateway/tests/extraction/test_extract_monitor.py,
    mcp-gateway/tests/extraction/test_list_extracted_files.py,
    mcp-gateway/tests/extraction/test_promote_extracted_sample.py,
    mcp-gateway/tests/extraction/test_run_binwalk.py,
    mcp-gateway/tests/extraction/test_run_unblob.py,
    mcp-gateway/tests/extraction/test_run_upx.py,
    mcp-gateway/tests/extraction/test_job_specs_unblob.py,
    mcp-gateway/tests/extraction/test_job_specs_binwalk_extract.py,
    mcp-gateway/tests/extraction/test_disclaimers.py,
    mcp-gateway/tests/extraction/test_tool_list_phase10.py
  </files>
  <read_first>
    - mcp-gateway/tests/test_tool_list.py (EXPECTED_TOOLS pattern — Plan 05 will extend, this plan creates a Phase-10-only mirror invariant)
    - mcp-gateway/tests/test_jobs/conftest.py if exists (precedent for Phase 9 conftest style); otherwise look at mcp-gateway/tests/conftest.py
    - mcp-gateway/src/mcp_gateway/jobs.py lines 326-382 (the three ship-with specs: _sleep_probe, _log_burst_probe, capa — the reference pattern Plan 02's specs will replicate)
    - .planning/phases/10-extraction-tier/10-CONTEXT.md lines 1031-1071 (D-24 file list with per-file purpose)
    - .planning/phases/10-extraction-tier/10-RESEARCH.md "Validation Architecture" → "Phase Requirements → Test Map" (line ~700-735 — each EXTR-XX → test function name + automated command; the test function names below MUST match these one-to-one)
  </read_first>
  <behavior>
    RED-stub discipline (Phase 6/7/8/9 precedent — verbatim):
    - Every test function imports the not-yet-existing module AT THE TOP OF THE FUNCTION BODY (not at module top), so pytest collection passes (no ImportError at import time) but pytest execution ImportErrors at the first line of each function.
    - Tests have empty / minimal bodies after the import (e.g. `assert True` placeholder) — Wave 5 (Plan 05) populates the real behavioural bodies.
    - `pytest.skip` is FORBIDDEN at function bodies; the only legal skip is via the `_require_<tool>_or_skip` conftest fixtures for slow-integration tests.
    - Tests marked `@pytest.mark.slow` use `pytest.importorskip` for the tool OR a `_require_<tool>_or_skip` fixture; non-slow tests use mocks.

    Specific test function names required (one-to-one with 10-RESEARCH.md Validation Architecture Phase Requirements → Test Map):
    - test_run_binwalk.py: `test_signatures_mode_parses_rows`, `test_entropy_mode_parses_rows`, `test_extract_mode_dispatches_job`, `test_errors_structured`
    - test_run_unblob.py: `test_dispatches_job_with_meta`, `test_report_json_parsed` (slow), `test_errors_structured`
    - test_run_upx.py: `test_test_not_packed`, `test_list_parses_columns`, `test_unpack_writes_output` (slow)
    - test_list_extracted_files.py: `test_engine_agnostic_enumeration`, `test_files_per_extraction_cap`, `test_limit_truncation`, `test_engine_filter`, `test_exclude_quarantined`
    - test_promote_extracted_sample.py: `test_promotion_flow`, `test_idempotent_by_sha256`, `test_force_new_bypasses_idempotent`, `test_rejects_outside_extracted`, `test_rejects_symlink_sentinel`, `test_sha256_recomputed`, `test_errors_structured`
    - test_quarantine_symlinks.py: `test_sentinel_body_format`, `test_idempotent`, `test_no_follow_walk`
    - test_extract_monitor.py: `test_cap_exceeded_cancels_job`, `test_clean_exit_on_terminal`, `test_monitor_poll_count_updates_meta`
    - test_extraction_dir.py: `test_binwalk_engine_prefix`, `test_unblob_engine_prefix`, `test_upx_engine_prefix`, `test_rand4_avoids_collision`, `test_rejects_invalid_engine`
    - test_meta_sidecar.py: `test_write_meta_creates_file`, `test_update_meta_is_atomic`, `test_read_meta_roundtrip`, `test_update_meta_preserves_unspecified_fields`
    - test_job_specs_unblob.py: `test_argv_shape`, `test_sample_resolved`, `test_depth_bounds`, `test_extraction_dir_confinement`
    - test_job_specs_binwalk_extract.py: `test_argv_shape`, `test_matryoshka_flag`, `test_sample_resolved`, `test_extraction_dir_confinement`
    - test_disclaimers.py: `test_run_unblob_disclaimer`, `test_run_binwalk_disclaimer`, `test_list_extracted_files_disclaimer`, `test_promote_extracted_sample_disclaimer`, `test_run_upx_test_short_disclaimer`, `test_run_upx_list_short_disclaimer`, `test_run_upx_unpack_short_disclaimer`
    - test_tool_list_phase10.py: `test_expected_phase10_tools_present`, `test_phase10_tool_count_is_seven`
  </behavior>
  <action>
    Create exactly 15 files (1 init, 1 conftest, 13 test files). Use the Write tool, NOT heredoc.

    File 1 — `mcp-gateway/tests/extraction/__init__.py`: EMPTY file (package marker; just create with empty content).

    File 2 — `mcp-gateway/tests/extraction/conftest.py` (~80 LoC):
    ```python
    """Phase 10 extraction test fixtures.

    Mirrors Phase 9 conftest pattern: _require_<tool>_or_skip gates for slow integration
    tests; fake extraction-tree builder for unit tests that don't need real subprocesses.
    """
    from __future__ import annotations
    import json
    import os
    import shutil
    from pathlib import Path
    import pytest


    @pytest.fixture
    def _require_binwalk_or_skip() -> None:
        if shutil.which("binwalk") is None:
            pytest.skip("binwalk not on PATH (Phase 10 slow integration)")


    @pytest.fixture
    def _require_unblob_or_skip() -> None:
        if shutil.which("unblob") is None:
            pytest.skip("unblob not on PATH (Phase 10 slow integration)")


    @pytest.fixture
    def _require_upx_or_skip() -> None:
        if shutil.which("upx") is None and shutil.which("upx-ucl") is None:
            pytest.skip("upx/upx-ucl not on PATH (Phase 10 slow integration)")


    @pytest.fixture
    def fake_extraction_tree(tmp_path: Path):
        """Builder factory: returns a callable that mints fake `extracted/<engine>-<ts>-<rand>/`
        subdirs with a populated _mare_meta.json and N regular files.

        Used by test_list_extracted_files.py + test_extract_monitor.py for unit-level
        coverage without invoking real binwalk/unblob/upx.
        """
        case = tmp_path / "case-001"
        (case / "extracted").mkdir(parents=True)

        def _make(engine: str, ts: str = "20260519T143211Z", rand4: str = "a3f9",
                  file_count: int = 0, status: str = "succeeded",
                  symlinks_quarantined: int = 0, cap_exceeded: bool = False) -> Path:
            d = case / "extracted" / f"{engine}-{ts}-{rand4}"
            d.mkdir()
            meta = {
                "engine": engine, "mode": "extract", "started_at": "2026-05-19T14:32:11Z",
                "completed_at": "2026-05-19T14:35:00Z", "status": status,
                "exit_code": 0 if status == "succeeded" else 1,
                "case_dir": str(case), "extraction_dir": f"extracted/{engine}-{ts}-{rand4}",
                "sample": "/agent/uploads/deadbeef/sample.bin",
                "sample_sha256": "deadbeef" * 8, "argv": [engine, "/agent/uploads/deadbeef/sample.bin"],
                "job_id": None, "log_path": "tool-logs/foo.txt",
                "symlinks_quarantined": symlinks_quarantined, "cap_exceeded": cap_exceeded,
                "extract_bytes_total": 0, "monitor_polls": 0,
            }
            (d / "_mare_meta.json").write_text(json.dumps(meta))
            for i in range(file_count):
                (d / f"file{i:04d}.bin").write_bytes(b"x" * 16)
            return d

        return case, _make
    ```

    Files 3-15 (the 13 test files) — use this RED-stub template VERBATIM, substituting `{MODULE}` and `{TEST_NAMES}`:
    ```python
    """Phase 10 Wave 0 RED-stub — Wave 1/2 turns these GREEN.

    Per CONTEXT D-24: function-top imports of not-yet-existing modules; pytest collection
    passes, execution ImportErrors. pytest.skip forbidden except via _require_<tool>_or_skip.
    """
    from __future__ import annotations
    import pytest


    def test_X():
        from mcp_gateway import extraction  # noqa: F401  RED until Plan 02
        assert True  # body populated by Plan 05 Wave-3 GREEN flip
    ```

    Per-file mapping (each test function gets one such body with the imports matching the module the test will exercise):

    `test_extraction_dir.py` (5 tests, all import `mcp_gateway.extraction`):
    - test_binwalk_engine_prefix, test_unblob_engine_prefix, test_upx_engine_prefix, test_rand4_avoids_collision, test_rejects_invalid_engine

    `test_meta_sidecar.py` (4 tests, all import `mcp_gateway.extraction`):
    - test_write_meta_creates_file, test_update_meta_is_atomic, test_read_meta_roundtrip, test_update_meta_preserves_unspecified_fields

    `test_quarantine_symlinks.py` (3 tests, all import `mcp_gateway.extraction`):
    - test_sentinel_body_format, test_idempotent, test_no_follow_walk

    `test_extract_monitor.py` (3 tests, all import `mcp_gateway.extraction`):
    - test_cap_exceeded_cancels_job, test_clean_exit_on_terminal, test_monitor_poll_count_updates_meta

    `test_list_extracted_files.py` (5 tests, all import `from mcp_gateway.tools import extract`):
    - test_engine_agnostic_enumeration, test_files_per_extraction_cap, test_limit_truncation, test_engine_filter, test_exclude_quarantined

    `test_promote_extracted_sample.py` (7 tests, all import `from mcp_gateway.tools import extract`):
    - test_promotion_flow, test_idempotent_by_sha256, test_force_new_bypasses_idempotent, test_rejects_outside_extracted, test_rejects_symlink_sentinel, test_sha256_recomputed, test_errors_structured

    `test_run_binwalk.py` (4 tests, import `from mcp_gateway.tools import extract`; `test_extract_mode_dispatches_job` marked `@pytest.mark.slow` + uses `_require_binwalk_or_skip`):
    - test_signatures_mode_parses_rows, test_entropy_mode_parses_rows, test_extract_mode_dispatches_job (slow), test_errors_structured

    `test_run_unblob.py` (3 tests, import `from mcp_gateway.tools import extract`; `test_report_json_parsed` marked `@pytest.mark.slow` + uses `_require_unblob_or_skip`):
    - test_dispatches_job_with_meta, test_report_json_parsed (slow), test_errors_structured

    `test_run_upx.py` (3 tests, import `from mcp_gateway.tools import extract`; `test_unpack_writes_output` marked `@pytest.mark.slow` + uses `_require_upx_or_skip`):
    - test_test_not_packed, test_list_parses_columns, test_unpack_writes_output (slow)

    `test_job_specs_unblob.py` (4 tests, all import `mcp_gateway.extraction`):
    - test_argv_shape, test_sample_resolved, test_depth_bounds, test_extraction_dir_confinement

    `test_job_specs_binwalk_extract.py` (4 tests, all import `mcp_gateway.extraction`):
    - test_argv_shape, test_matryoshka_flag, test_sample_resolved, test_extraction_dir_confinement

    `test_disclaimers.py` (7 tests, all import `from mcp_gateway.tools import extract`):
    - test_run_unblob_disclaimer, test_run_binwalk_disclaimer, test_list_extracted_files_disclaimer, test_promote_extracted_sample_disclaimer, test_run_upx_test_short_disclaimer, test_run_upx_list_short_disclaimer, test_run_upx_unpack_short_disclaimer

    `test_tool_list_phase10.py` (2 tests, import `from mcp_gateway.tools import extract` and `from mcp.server.fastmcp import FastMCP`):
    - test_expected_phase10_tools_present (asserts the 7 Phase 10 tool names register cleanly via `extract.register(mcp)`)
    - test_phase10_tool_count_is_seven (asserts FastMCP tool count == 7 after `extract.register(mcp)` on a fresh FastMCP instance)

    Final touches:
    - Every test function body has EXACTLY the import + `assert True`. No real behavioural assertions. Plan 05 fills bodies in.
    - For `@pytest.mark.slow` tests, decorate the function and add the corresponding `_require_<tool>_or_skip` as a fixture argument (e.g. `def test_extract_mode_dispatches_job(_require_binwalk_or_skip):`).
    - Total file count after this task: 1 __init__.py (empty), 1 conftest.py (~80 LoC), 13 test files (~25-40 LoC each).
  </action>
  <verify>
    <automated>cd mcp-gateway && python -m pytest tests/extraction/ --collect-only -q 2>&1 | tail -20 && python -m pytest tests/extraction/ --no-header -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `find mcp-gateway/tests/extraction -type f -name '*.py' | wc -l` returns ≥ 15 (1 __init__ + 1 conftest + 13 test files)
    - `cd mcp-gateway && python -m pytest tests/extraction/ --collect-only -q 2>&1 | grep -E 'test_.*collected'` shows ≥ 50 tests collected (sum of 5+4+3+3+5+7+4+3+3+4+4+7+2 = 54)
    - `cd mcp-gateway && python -m pytest tests/extraction/ --collect-only -q 2>&1` exits 0 (no syntax errors, no import errors at collection time)
    - `cd mcp-gateway && python -m pytest tests/extraction/ -m "not slow" --no-header 2>&1 | grep -E 'ImportError|ModuleNotFoundError|errors'` matches — execution fails with ImportError because mcp_gateway.extraction does not yet exist
    - `grep -l 'from mcp_gateway import extraction' mcp-gateway/tests/extraction/*.py | wc -l` returns ≥ 6 (extraction_dir, meta_sidecar, quarantine_symlinks, extract_monitor, job_specs_unblob, job_specs_binwalk_extract)
    - `grep -l 'from mcp_gateway.tools import extract' mcp-gateway/tests/extraction/*.py | wc -l` returns ≥ 6 (list_extracted_files, promote_extracted_sample, run_binwalk, run_unblob, run_upx, disclaimers, tool_list_phase10 = 7)
    - `grep -c 'pytest.skip' mcp-gateway/tests/extraction/test_*.py | grep -v ':0$'` returns NO matches — pytest.skip is forbidden in test bodies (only conftest's _require_*_or_skip fixtures use it)
    - `grep -c '_require_binwalk_or_skip\|_require_unblob_or_skip\|_require_upx_or_skip' mcp-gateway/tests/extraction/conftest.py` returns ≥ 3
    - `grep -c '@pytest.mark.slow' mcp-gateway/tests/extraction/test_run_binwalk.py mcp-gateway/tests/extraction/test_run_unblob.py mcp-gateway/tests/extraction/test_run_upx.py` returns ≥ 3 (one per slow test)
  </acceptance_criteria>
  <done>15 files created under mcp-gateway/tests/extraction/; pytest collects ≥ 50 tests cleanly; execution fails with ImportError on every test (RED state); no test uses pytest.skip outside the _require_*_or_skip fixtures.</done>
</task>

</tasks>

<threat_model>
  <asvs_level>1</asvs_level>
  <threats>
    <threat id="T-10-W0-01">
      <description>Test scaffold accidentally implements real behaviour (not RED), breaking the Wave 0 → Wave 1/2 contract. Plan 02's GREEN flip becomes ambiguous and Plan 05 has no clean checkpoint.</description>
      <severity>medium</severity>
      <mitigation>Task 2 acceptance criteria explicitly verify execution fails with ImportError; test bodies are `assert True` placeholder only; Plan 05 owns the behavioural body fill-in.</mitigation>
    </threat>
    <threat id="T-10-W0-02">
      <description>Dockerfile migration breaks the Kali apt install (e.g. binwalk3 not yet in the chosen kali-rolling pin), causing the next `./run_docker.sh` rebuild to fail and blocking all of Plan 02-05.</description>
      <severity>medium</severity>
      <mitigation>scripts/probe_extraction_tools.sh includes `apt-cache policy binwalk3` which resolves Assumption A1 at the next container build. If binwalk3 is unavailable, the rebuild fails-loud at apt-get install (does not silently fall back). Plan 02's `_build_binwalk_extract_argv` accepts binwalk3 CLI per RESEARCH Example 1.</mitigation>
    </threat>
    <threat id="T-10-W0-03">
      <description>RED-stub tests collected by pytest, then accidentally marked as passing because the `assert True` body is reached (function-top import inside the function is bypassed if Python evaluates `assert True` before the import on some optimizations).</description>
      <severity>low</severity>
      <mitigation>Python always executes statements in textual order — the `from mcp_gateway import extraction` line precedes `assert True` lexically, so ImportError fires first. Task 2 acceptance criteria asserts `ImportError|ModuleNotFoundError` appears in pytest output. There is no scenario where the import is reordered after `assert True`.</mitigation>
    </threat>
    <threat id="T-10-W0-04">
      <description>Probe script (scripts/probe_extraction_tools.sh) executes attacker-controlled argv if `binwalk --help` shells out unsafely. The script is read-only diagnostics and is invoked only by the operator on demand.</description>
      <severity>low</severity>
      <mitigation>Script uses `set -u`, no `eval`, no user-controlled args. `apt-cache policy` and `--version` are read-only operations. The script is a manual operator probe (not MCP-reachable).</mitigation>
    </threat>
  </threats>
</threat_model>

<verification>
- `pytest tests/extraction/ --collect-only -q` exits 0; collects ≥ 50 tests
- `pytest tests/extraction/ --no-header -q` execution fails with ImportError (RED state confirmed)
- Dockerfile contains `binwalk3` (not `binwalk` v2 standalone) on the apt install line
- scripts/probe_extraction_tools.sh exists, is executable, passes `bash -n`
- `grep -c 'binwalk3' Dockerfile` ≥ 1 AND `grep -c '^[[:space:]]*binwalk[[:space:]]' Dockerfile` == 0
</verification>

<success_criteria>
1. 13 RED-stub test files + __init__.py + conftest.py exist under mcp-gateway/tests/extraction/
2. pytest collection passes; execution fails with ImportError on every test (Phase 6/7/8/9 RED-stub discipline maintained)
3. Dockerfile migrated binwalk → binwalk3 with EOL comment
4. Probe script created and ready to run in-container after the next image rebuild
5. Every Phase 10 EXTR-XX requirement has at least one RED-stub test that maps via VALIDATION.md's per-task verification map
</success_criteria>

<output>
After completion, create `.planning/phases/10-extraction-tier/10-01-SUMMARY.md` documenting:
- Files created (count + paths)
- Dockerfile diff (the single binwalk → binwalk3 line change)
- Probe script execution instructions for the operator (when they next rebuild)
- Confirmation of RED state (count of ImportErrors observed during pytest run)
- Any Wave-1 implications discovered (e.g. if grep revealed binwalk was already binwalk3 in Dockerfile, surface that)
</output>
