---
phase: 10
plan: 05
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03, 10-04]
files_modified:
  - mcp-gateway/src/mcp_gateway/tools/__init__.py
  - mcp-gateway/tests/test_tool_list.py
  - mcp-gateway/tests/extraction/test_extraction_dir.py
  - mcp-gateway/tests/extraction/test_meta_sidecar.py
  - mcp-gateway/tests/extraction/test_quarantine_symlinks.py
  - mcp-gateway/tests/extraction/test_extract_monitor.py
  - mcp-gateway/tests/extraction/test_list_extracted_files.py
  - mcp-gateway/tests/extraction/test_promote_extracted_sample.py
  - mcp-gateway/tests/extraction/test_run_binwalk.py
  - mcp-gateway/tests/extraction/test_run_unblob.py
  - mcp-gateway/tests/extraction/test_run_upx.py
  - mcp-gateway/tests/extraction/test_job_specs_unblob.py
  - mcp-gateway/tests/extraction/test_job_specs_binwalk_extract.py
  - mcp-gateway/tests/extraction/test_disclaimers.py
  - mcp-gateway/tests/extraction/test_tool_list_phase10.py
  - .planning/phases/10-extraction-tier/10-VALIDATION.md
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-03, EXTR-04, EXTR-05, EXTR-06]

must_haves:
  truths:
    - "mcp_gateway.tools.__init__.register_all_tools registers tools.extract via one import + one register call (D-20)"
    - "Registration order: AFTER r2_sessions + jobs, BEFORE backend_passthrough (D-20)"
    - "mcp-gateway/tests/test_tool_list.py::EXPECTED_TOOLS contains all 7 Phase 10 tool names (bump 47 → 54)"
    - "test_tool_count_in_range accepts the new range (35-60) per CONTEXT D-01 bump rationale"
    - "All 13 Wave-0 RED-stub test files have GREEN behavioural bodies — pure-function + mocked-subprocess tests pass on dev host; slow-integration tests skip cleanly when binwalk3/unblob/upx are missing"
    - "10-VALIDATION.md flipped to nyquist_compliant: true + wave_0_complete: true + per-task verification map populated with one row per task across plans 01-05"
    - "Full pytest run on mcp-gateway/tests/ remains green (no Phase 5-9 regression)"
    - "_lineage.json is auto-exposed via Phase 7 D-26 Resources walker (depth-2; top-level case files included automatically — no walker edits needed; confirmation via existing tools/resources.py test pass)"
  artifacts:
    - path: "mcp-gateway/src/mcp_gateway/tools/__init__.py"
      provides: "register_all_tools wires tools.extract"
      contains: "extract"
    - path: "mcp-gateway/tests/test_tool_list.py"
      provides: "EXPECTED_TOOLS bumped 47 -> 54; tool_count_in_range bumped 35-50 -> 35-60"
      contains: "run_binwalk"
    - path: ".planning/phases/10-extraction-tier/10-VALIDATION.md"
      provides: "per-task verification map populated; nyquist_compliant: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "mcp-gateway/src/mcp_gateway/tools/__init__.py"
      to: "mcp_gateway.tools.extract"
      via: "register_all_tools"
      pattern: "extract"
    - from: "mcp-gateway/tests/test_tool_list.py::EXPECTED_TOOLS"
      to: "Phase 10 seven tool names"
      via: "set literal addition"
      pattern: "run_binwalk"
---

<objective>
Wave 3 integration: Wire `tools/extract.py` into `register_all_tools` (D-20: AFTER r2_sessions + jobs, BEFORE backend_passthrough), bump `EXPECTED_TOOLS` from 47 to 54 + tool-count range 35-50 to 35-60 (D-01 — Rule-1 deviation, precedent Phase 7-08 + 9-03), flip all 13 Wave-0 RED-stub tests to GREEN with behavioural bodies, and finalize `10-VALIDATION.md` (nyquist_compliant: true, per-task map populated, sign-off green).

Purpose: This is the wire-up that makes Phase 10 a live MCP surface. Plan 02-04 produced the implementation; Plan 05 makes it visible to MCP clients and locks the invariants via tests.

Output:
- 2 source-file edits (tools/__init__.py + test_tool_list.py)
- 13 test-file body fills (RED → GREEN)
- 1 VALIDATION.md final-state update
- Confirmation that full pytest suite remains green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-extraction-tier/10-CONTEXT.md
@.planning/phases/10-extraction-tier/10-RESEARCH.md
@.planning/phases/10-extraction-tier/10-02-SUMMARY.md
@.planning/phases/10-extraction-tier/10-03-SUMMARY.md
@.planning/phases/10-extraction-tier/10-04-SUMMARY.md
@mcp-gateway/src/mcp_gateway/tools/__init__.py
@mcp-gateway/src/mcp_gateway/tools/extract.py
@mcp-gateway/src/mcp_gateway/extraction.py
@mcp-gateway/tests/test_tool_list.py
@mcp-gateway/tests/extraction/conftest.py
@.planning/phases/10-extraction-tier/10-VALIDATION.md

<interfaces>
Existing tools/__init__.py::register_all_tools structure (Plan 05 extends):
```python
def register_all_tools(mcp: FastMCP) -> None:
    from . import (..., r2_sessions, jobs, collision_check, ...)  # imports
    cases.register(mcp)
    ...
    r2_sessions.register(mcp)   # Phase 8
    jobs.register(mcp)          # Phase 9
    backend_passthrough.register(mcp)  # last — Phase 10 inserts AFTER jobs, BEFORE this
```

EXPECTED_TOOLS bump (test_tool_list.py):
- Current: 47 names (22 v1.0 + 17 Phase 7 + 4 Phase 8 + 4 Phase 9)
- Target: 54 names (add 7 Phase 10: run_binwalk, run_unblob, run_upx_test, run_upx_list, run_upx_unpack, list_extracted_files, promote_extracted_sample)
- test_tool_count_in_range: bump range from `35 <= n <= 50` to `35 <= n <= 60` (CONTEXT D-01 absorbs Phase 11's conditional dynamic tools by widening to 60)

RED-stub test bodies (current state from Plan 01): each test = `from mcp_gateway... import ...; assert True`
GREEN state: behavioural bodies that exercise the actual primitives + surface (full assertions on the locked contracts from CONTEXT D-01..D-23)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire tools.extract into register_all_tools + bump EXPECTED_TOOLS + tool-count range</name>
  <files>mcp-gateway/src/mcp_gateway/tools/__init__.py, mcp-gateway/tests/test_tool_list.py</files>
  <read_first>
    - mcp-gateway/src/mcp_gateway/tools/__init__.py (current state — 60 LoC)
    - mcp-gateway/tests/test_tool_list.py first 100 lines (current EXPECTED_TOOLS set + test_tool_count_in_range range)
    - .planning/phases/10-extraction-tier/10-CONTEXT.md lines 96-122 (D-01: 47 → 54 bump + Rule-1 precedent)
    - .planning/phases/10-extraction-tier/10-CONTEXT.md lines 930-948 (D-20: register order — AFTER r2_sessions + jobs, BEFORE backend_passthrough)
  </read_first>
  <action>
    Step A — Edit `mcp-gateway/src/mcp_gateway/tools/__init__.py` (use Edit tool):
    1. Add `extract` to the `from . import (...)` block. Place between `jobs` and `collision_check`:
       ```python
       from . import (
           cases,
           artifacts,
           workflows,
           disasm,
           resources,
           backend_passthrough,
           shell,            # Phase 7 D-16
           re_static,        # Phase 7 D-16
           re_artifacts,     # Phase 7 D-16
           r2_sessions,      # Phase 8 D-05
           jobs,             # Phase 9 D-05
           extract,          # Phase 10 D-20
           collision_check,
       )  # noqa: F401
       ```
    2. Add `extract.register(mcp)` between `jobs.register(mcp)` and `backend_passthrough.register(mcp)`:
       ```python
       jobs.register(mcp)         # Phase 9 D-05
       extract.register(mcp)      # Phase 10 D-20 -- 7 tools: run_binwalk, run_unblob, run_upx_{test,list,unpack}, list_extracted_files, promote_extracted_sample
       backend_passthrough.register(mcp)
       ```
    3. Update the module docstring's "Phase 9 additions" section to add "Phase 10 additions" mirroring the existing format:
       ```
       Phase 10 additions (D-20):
         - extract.register(mcp): run_binwalk, run_unblob, run_upx_test, run_upx_list,
           run_upx_unpack, list_extracted_files, promote_extracted_sample (7 extraction tools).
       ```

    Step B — Edit `mcp-gateway/tests/test_tool_list.py`:
    1. Append 7 Phase 10 tool names to `EXPECTED_TOOLS` set. Place after the Phase 9 line:
       ```python
       # Phase 9 D-05 background jobs (4)
       "start_tool_job", "get_tool_job", "cancel_tool_job", "list_tool_jobs",
       # Phase 10 D-01 extraction tier (7)
       "run_binwalk", "run_unblob", "run_upx_test", "run_upx_list", "run_upx_unpack",
       "list_extracted_files", "promote_extracted_sample",
       ```
    2. Update the docstring header to add "Phase 10 D-01 adds 7 more (extraction tier): run_binwalk, run_unblob, run_upx_test/list/unpack, list_extracted_files, promote_extracted_sample. Total after Phase 10: 54."
    3. Update `test_tool_count_in_range`:
       - Change `assert 35 <= n <= 50` to `assert 35 <= n <= 60`
       - Update the docstring/comment: "Phase 10 D-01 expands to 54 (47 + 7); range 35-60 absorbs Phase 11's conditional dynamic tools (env-gated, default-off)."
    4. Do NOT touch `test_atomic_tools_map_to_scripts` — Phase 10 adds no new atomic-scripts mapping.
  </action>
  <verify>
    <automated>cd mcp-gateway && python -c "from mcp_gateway.tools import register_all_tools; from mcp.server.fastmcp import FastMCP; m = FastMCP('t', stateless_http=True); register_all_tools(m); print('register_all_tools_ok')" && python -m pytest tests/test_tool_list.py -x --no-header -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\bextract\b' mcp-gateway/src/mcp_gateway/tools/__init__.py` returns ≥ 3 (import line + register call + comment/docstring)
    - `grep -c 'extract.register(mcp)' mcp-gateway/src/mcp_gateway/tools/__init__.py` returns 1
    - Order verification — line number of `extract` is BETWEEN `jobs` and `backend_passthrough` registrations: `python -c "import re; lines=open('mcp-gateway/src/mcp_gateway/tools/__init__.py').readlines(); jobs=[i for i,l in enumerate(lines) if 'jobs.register(mcp)' in l][0]; extract=[i for i,l in enumerate(lines) if 'extract.register(mcp)' in l][0]; bpt=[i for i,l in enumerate(lines) if 'backend_passthrough.register(mcp)' in l][0]; assert jobs < extract < bpt, f'order violation: jobs={jobs} extract={extract} bpt={bpt}'"` exits 0
    - `grep -c 'run_binwalk\|run_unblob\|run_upx_test\|run_upx_list\|run_upx_unpack\|list_extracted_files\|promote_extracted_sample' mcp-gateway/tests/test_tool_list.py` returns ≥ 7
    - `grep -c '35 <= n <= 60' mcp-gateway/tests/test_tool_list.py` returns 1 (range bumped)
    - `grep -c '35 <= n <= 50' mcp-gateway/tests/test_tool_list.py` returns 0 (old range removed)
    - `cd mcp-gateway && python -m pytest tests/test_tool_list.py -x --no-header -q` exits 0 (full Phase 7/8/9 invariants + Phase 10 additions all pass)
    - `python -c "from mcp.server.fastmcp import FastMCP; from mcp_gateway.tools import register_all_tools; m = FastMCP('t', stateless_http=True); register_all_tools(m); import asyncio; from mcp.shared.memory import create_connected_server_and_client_session; tools = asyncio.run((lambda: __import__('asyncio').run.__self__)() ) if False else None"` — simpler: `python -c "from mcp.server.fastmcp import FastMCP; from mcp_gateway.tools import register_all_tools; m = FastMCP('t', stateless_http=True); register_all_tools(m); print('count:', len(m._tool_manager._tools))"` prints a count ≥ 50 and ≤ 60
  </acceptance_criteria>
  <done>tools/__init__.py wires extract.register between jobs and backend_passthrough; EXPECTED_TOOLS bumped 47 → 54; tool-count range bumped to 35-60; full test_tool_list.py passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip all 13 Wave-0 RED-stub test files to GREEN behavioural bodies</name>
  <files>
    mcp-gateway/tests/extraction/test_extraction_dir.py,
    mcp-gateway/tests/extraction/test_meta_sidecar.py,
    mcp-gateway/tests/extraction/test_quarantine_symlinks.py,
    mcp-gateway/tests/extraction/test_extract_monitor.py,
    mcp-gateway/tests/extraction/test_list_extracted_files.py,
    mcp-gateway/tests/extraction/test_promote_extracted_sample.py,
    mcp-gateway/tests/extraction/test_run_binwalk.py,
    mcp-gateway/tests/extraction/test_run_unblob.py,
    mcp-gateway/tests/extraction/test_run_upx.py,
    mcp-gateway/tests/extraction/test_job_specs_unblob.py,
    mcp-gateway/tests/extraction/test_job_specs_binwalk_extract.py,
    mcp-gateway/tests/extraction/test_disclaimers.py,
    mcp-gateway/tests/extraction/test_tool_list_phase10.py
  </files>
  <read_first>
    - mcp-gateway/src/mcp_gateway/extraction.py (Plan 02+03 output — confirms public surface for tests)
    - mcp-gateway/src/mcp_gateway/tools/extract.py (Plan 04 output — confirms surface signatures)
    - mcp-gateway/tests/extraction/conftest.py (Plan 01 — _require_*_or_skip fixtures + fake_extraction_tree)
    - All 13 RED-stub test files (current state — Plan 01 placeholders with `assert True`)
    - .planning/phases/10-extraction-tier/10-RESEARCH.md "Validation Architecture → Phase Requirements → Test Map" (line ~700-735 — each test function name → one EXTR-XX requirement)
    - mcp-gateway/tests/jobs/conftest.py if exists (Phase 9 GREEN-flip precedent — pattern reference for behavioural bodies)
  </read_first>
  <behavior>
    Each test function body asserts ONE specific contract from CONTEXT D-01..D-23, derived from the test function name (function names were locked in Plan 01).

    Categories:
    - Pure-function tests (no subprocess, no MCP roundtrip): unit tests on extraction.py helpers + argv builders. Use tmp_path fixture + extraction.write_meta / extraction.quarantine_symlinks / etc. directly. ~20 tests.
    - Mocked-subprocess tests: use unittest.mock or monkeypatch to stub runner.run_tool and tools_jobs.start_tool_job; assert wrapper's parsing + dict shape. ~15 tests.
    - Slow-integration tests (marked @pytest.mark.slow): use the _require_<tool>_or_skip fixtures from conftest; run real binwalk/unblob/upx against synthetic fixtures. ~5 tests. Skip cleanly on dev hosts missing the tools.
    - Disclaimer regression tests: assert specific verbatim substrings appear in __doc__.
    - Tool-list invariant tests: register tools.extract on a fresh FastMCP, count, verify the 7 names.

    Common patterns:
    - Use `monkeypatch.setenv("MCP_GATEWAY_STATUS_DIR", str(tmp_path / "status"))` + similar for UPLOADS / EXAMPLES roots when needed to point to tmp_path. Reload affected modules via importlib.reload if module-level constants are captured at import.
    - For tests that need a "case dir" structure, use the conftest `fake_extraction_tree` fixture.
    - For mocking start_tool_job: monkeypatch `mcp_gateway.tools.jobs.start_tool_job` with an async function returning a Phase 9 D-19 25-key snapshot dict (or an error dict for error-path tests).
    - For tests asserting the `extracted/<engine>-<ts>-<rand4>/` naming, regex assertion: `re.match(r"extracted/(binwalk|unblob|upx)-\d{8}T\d{6}Z-[0-9a-f]{4}", ...)`.
  </behavior>
  <action>
    Use Edit tool on each of the 13 test files to REPLACE the `assert True` placeholder bodies with behavioural assertions. Each file is small (~50-150 LoC after fill-in).

    Below are the per-file behavioural-body targets. Be specific; each assertion grounds one decision.

    1. `test_extraction_dir.py` (5 tests, all unit):
       - test_binwalk_engine_prefix: extraction.extraction_dir(case_dir, "binwalk") returns Path that ends in `extracted/binwalk-<ts>-<rand4>/`; dir exists; regex match on basename.
       - test_unblob_engine_prefix: same with "unblob".
       - test_upx_engine_prefix: same with "upx".
       - test_rand4_avoids_collision: call extraction_dir 5x within 1 second; all paths distinct (set of basenames has 5 entries).
       - test_rejects_invalid_engine: extraction_dir(case_dir, "ghidra") raises ValueError mentioning the allowed set.

    2. `test_meta_sidecar.py` (4 tests, all unit):
       - test_write_meta_creates_file: write_meta(ext_dir, payload) creates `_mare_meta.json` with json.dumps-roundtripped content == payload.
       - test_update_meta_is_atomic: simulate a partial-write race by writing then concurrent-update; assert the file always contains a valid JSON document at any point.
       - test_read_meta_roundtrip: write → read returns identical dict.
       - test_update_meta_preserves_unspecified_fields: write {a:1, b:2}; update({a:10}); read returns {a:10, b:2}.

    3. `test_quarantine_symlinks.py` (3 tests, all unit):
       - test_sentinel_body_format: create symlink in tmp dir → quarantine_symlinks(dir) → assert .symlink-target.txt content matches the 5-line D-16 format verbatim (substring asserts for each of the 5 header lines).
       - test_idempotent: run quarantine twice; second run returns (0, []) and produces no extra files.
       - test_no_follow_walk: create a symlink pointing OUTSIDE the extraction_dir; quarantine runs without traversing the target (test by setting up a tree where following the symlink would hit a file with a known sentinel-detectable name; assert that sentinel file is NOT processed).

    4. `test_extract_monitor.py` (3 tests, integration with mocks):
       - test_cap_exceeded_cancels_job: monkeypatch tools_jobs.get_tool_job/cancel_tool_job; create extraction_dir with files totaling > 1MB; set MCP_GATEWAY_MAX_EXTRACT_MB=0 (any size > 0 triggers); run start_extract_monitor with interval_s=0.01; assert .MARE_EXTRACT_CAP_EXCEEDED marker file exists, meta status="cap_exceeded", cancel_tool_job was called once.
       - test_clean_exit_on_terminal: monkeypatch get_tool_job to return status="succeeded" on second call; start monitor; await with timeout=2s; assert monitor exits without raising, meta status="succeeded", symlinks_quarantined recorded.
       - test_monitor_poll_count_updates_meta: monkeypatch get_tool_job to return non-terminal 3 times then "succeeded"; assert meta.monitor_polls == 3 after terminal exit.

    5. `test_list_extracted_files.py` (5 tests, unit with fake_extraction_tree fixture):
       - test_engine_agnostic_enumeration: build 1 binwalk + 1 unblob + 1 upx extraction; call list_extracted_files(case, engine=None); assert 3 extractions returned, each with correct engine field.
       - test_files_per_extraction_cap: build extraction with 5001 files; call; assert files_truncated=True and files list has exactly MAX_FILES_PER_EXTRACTION entries.
       - test_limit_truncation: build 3 extractions × 200 files each = 600 files; call with limit=500; assert top-level truncated=True and total_files_listed == 500.
       - test_engine_filter: build 1 of each engine; call with engine="binwalk"; assert only binwalk extraction returned.
       - test_exclude_quarantined: build extraction with a real file + a .symlink-target.txt; call with include_quarantined=False; assert the .symlink-target.txt entry is absent.

    6. `test_promote_extracted_sample.py` (7 tests, integration with monkeypatched UPLOADS/STATUS roots):
       - test_promotion_flow: set up parent case with `extracted/unblob-.../child.bin`; call promote; assert returned dict has new_case_dir, sha256, dedup, idempotent_reuse=False, lineage_path; assert <new_case_dir>/_lineage.json exists with promoted_sha256, parent_case_dir, etc.
       - test_idempotent_by_sha256: call promote twice on the same child; second call returns idempotent_reuse=True, same new_case_dir.
       - test_force_new_bypasses_idempotent: call with force_new=True after a prior promotion; new_case_dir differs from the prior one.
       - test_rejects_outside_extracted: call with child_path="../somefile"; assert error dict with "child_path must live under parent case's extracted/" key.
       - test_rejects_symlink_sentinel: promote a file ending `.symlink-target.txt`; assert error dict with "symlink quarantine sentinel" key.
       - test_sha256_recomputed: stash a fake sha in a fixture meta; promote; assert returned sha256 == hashlib.sha256 of actual child bytes (not the fake).
       - test_errors_structured: pass invalid parent_case_dir → assert {"error": "invalid case_dir", ...} dict returned (not raised).

    7. `test_run_binwalk.py` (4 tests, 1 slow):
       - test_signatures_mode_parses_rows: monkeypatch runner.run_tool to return a Phase 6 12-key dict with stdout containing 2 fake binwalk lines; call run_binwalk(mode="signatures"); assert result["mode"]=="signatures", result["signatures"] is a list with 2 rows, result["extraction_dir"] is case-rel string.
       - test_entropy_mode_parses_rows: same approach for mode="entropy"; assert result["entropy"] is a list and result["signatures"] is None.
       - test_extract_mode_dispatches_job (slow, _require_binwalk_or_skip): monkeypatch tools_jobs.start_tool_job to return a Phase 9 D-19 snapshot with status="pending"; call run_binwalk(mode="extract"); assert result["job_id"] exists, result["mode"]=="extract", result["extraction_dir"] is case-rel.
       - test_errors_structured: pass invalid case_dir → assert {"error": "invalid case_dir", ...} returned.

    8. `test_run_unblob.py` (3 tests, 1 slow):
       - test_dispatches_job_with_meta: monkeypatch tools_jobs.start_tool_job; call run_unblob; assert returned dict has engine="unblob", mode="extract", job_id, meta_path; assert <extraction_dir>/_mare_meta.json exists with status="running".
       - test_report_json_parsed (slow, _require_unblob_or_skip): mark as @pytest.mark.slow; placeholder body that runs unblob on a small fixture; if unblob isn't on PATH it skips via the fixture.
       - test_errors_structured: invalid sample → D-22 shape 2 error dict.

    9. `test_run_upx.py` (3 tests, 1 slow):
       - test_test_not_packed: monkeypatch runner.run_tool to return stderr="Not packed by UPX"; call run_upx_test; assert is_upx_packed=False, test_result="not_packed".
       - test_list_parses_columns: monkeypatch runner.run_tool to return a fake UPX -l table; assert rows is a list with parsed columns (file/compressed_size/uncompressed_size/ratio/format).
       - test_unpack_writes_output (slow, _require_upx_or_skip): real upx -d on a fixture; assert unpacked_path exists in extraction_dir.

    10. `test_job_specs_unblob.py` (4 tests, unit):
        - test_argv_shape: call extraction._build_unblob_argv(case_dir, {sample:..., extraction_dir:..., depth:5}); assert argv == ["unblob", "--report", ..., "-e", ..., "-d", "5", "--", sample_abs].
        - test_sample_resolved: monkeypatch tools.samples.resolve_sample; assert argv contains the resolved absolute path (not the sha256/path input).
        - test_depth_bounds: depth=0 raises ValueError; depth=17 raises ValueError; depth=8 succeeds.
        - test_extraction_dir_confinement: extraction_dir outside case_dir raises ValueError.

    11. `test_job_specs_binwalk_extract.py` (4 tests, unit):
        - test_argv_shape: call _build_binwalk_extract_argv with matryoshka=False; assert argv == ["binwalk", "-e", "-C", ..., "-l", ..., "-q", "--", sample_abs] (no -M).
        - test_matryoshka_flag: matryoshka=True (default); argv contains "-M" between "-e" and "-C".
        - test_sample_resolved: same as unblob.
        - test_extraction_dir_confinement: same as unblob.

    12. `test_disclaimers.py` (7 tests, unit):
        - test_run_unblob_disclaimer: assert "shared across all bearer-token clients" in run_unblob.__doc__; assert "in-memory job registry" in run_unblob.__doc__.
        - test_run_binwalk_disclaimer: same long form on run_binwalk.
        - test_list_extracted_files_disclaimer: same long form.
        - test_promote_extracted_sample_disclaimer: same long form + "Promotion lineage lives in" present.
        - test_run_upx_test_short_disclaimer: assert "shared across all bearer-token clients" in run_upx_test.__doc__; assert "in-memory job" NOT in run_upx_test.__doc__ (short form has no in-memory mention).
        - test_run_upx_list_short_disclaimer: same.
        - test_run_upx_unpack_short_disclaimer: same.

    13. `test_tool_list_phase10.py` (2 tests, unit):
        - test_expected_phase10_tools_present: register tools.extract.register on a fresh FastMCP; assert all 7 Phase 10 names are in `m._tool_manager._tools` (private attr is OK per test_tool_list.py precedent for count sanity).
        - test_phase10_tool_count_is_seven: same setup; assert exactly 7 names registered.

    Slow-test gating discipline:
    - Every @pytest.mark.slow test calls one of `_require_binwalk_or_skip` / `_require_unblob_or_skip` / `_require_upx_or_skip` fixtures from conftest.
    - Non-slow tests use unittest.mock.AsyncMock or monkeypatch — no real subprocesses.
  </action>
  <verify>
    <automated>cd mcp-gateway && python -m pytest tests/extraction/ -x --no-header -m "not slow" -q 2>&1 | tail -10 && python -m pytest tests/extraction/ --no-header -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd mcp-gateway && python -m pytest tests/extraction/ -m "not slow" --no-header -q` exits 0 (all non-slow tests PASS — RED → GREEN flip complete for fast tests)
    - `cd mcp-gateway && python -m pytest tests/extraction/ --no-header -q` exits 0 OR shows only `s` (skipped) entries for slow tests — NO failures
    - `grep -c 'assert True' mcp-gateway/tests/extraction/test_*.py` returns 0 (all placeholder asserts replaced with behavioural assertions — UNLESS a test file legitimately uses `assert True` as a final no-op after the real assertion, which should be avoided; the grep is a hard ratchet on placeholder elimination)
    - Test count holds: `cd mcp-gateway && python -m pytest tests/extraction/ --collect-only -q 2>&1 | grep -E 'collected'` shows ≥ 50 tests collected
    - Slow-test gating verified: `grep -c '_require_binwalk_or_skip\|_require_unblob_or_skip\|_require_upx_or_skip' mcp-gateway/tests/extraction/test_*.py` returns ≥ 5 (at least one per slow test)
    - All 7 Phase 10 tool name strings appear in `test_tool_list_phase10.py`: `grep -c 'run_binwalk\|run_unblob\|run_upx_test\|run_upx_list\|run_upx_unpack\|list_extracted_files\|promote_extracted_sample' mcp-gateway/tests/extraction/test_tool_list_phase10.py` returns ≥ 7
    - Full repo pytest remains green: `cd mcp-gateway && python -m pytest tests/ -x --no-header -q -m "not slow"` exits 0 (no Phase 5-9 regression)
  </acceptance_criteria>
  <done>All 13 test files have GREEN behavioural bodies; non-slow tests pass on dev host; slow tests skip cleanly when external tools missing; no `assert True` placeholders remain.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize 10-VALIDATION.md — per-task verification map + nyquist_compliant: true</name>
  <files>.planning/phases/10-extraction-tier/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-extraction-tier/10-VALIDATION.md (current state — frontmatter says nyquist_compliant: false)
    - .planning/phases/10-extraction-tier/10-01-PLAN.md through 10-04-PLAN.md (every task — extract the test commands from `<acceptance_criteria>` blocks for the per-task map)
  </read_first>
  <action>
    Use Edit tool on `.planning/phases/10-extraction-tier/10-VALIDATION.md`:

    Step A — Update frontmatter:
    - `status: draft` → `status: validated`
    - `nyquist_compliant: false` → `nyquist_compliant: true`
    - `wave_0_complete: false` → `wave_0_complete: true`

    Step B — Populate the "Per-Task Verification Map" table. Replace the placeholder row with one row per task across plans 01-05. Each row maps a task to its test command (from the task's `<acceptance_criteria>` automated command) + the EXTR-XX requirement(s) it addresses.

    Rows to add (one per task — there are ~10 tasks total across plans 01-05):

    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 10-01.T1 | 01 | 0 | EXTR-01..06 | T-10-W0-02 | Dockerfile uses binwalk3 (v2 EOL) + probe script for capability verification | unit | `grep -c '^[[:space:]]*binwalk3' Dockerfile \| grep -q '^[1-9]'` | ✅ | ✅ green |
    | 10-01.T2 | 01 | 0 | EXTR-01..06 | T-10-W0-01 | Wave 0 RED-stub scaffold for all 13 test files | unit | `cd mcp-gateway && python -m pytest tests/extraction/ --collect-only -q` | ✅ | ✅ green |
    | 10-02.T1 | 02 | 1 | EXTR-01..06 | T-10-02-03 | Primitive layer with locked surface + env constants + JobToolSpec registration | unit | `python -c "from mcp_gateway import extraction; from mcp_gateway.jobs import JOB_TOOL_REGISTRY; assert 'unblob' in JOB_TOOL_REGISTRY and 'binwalk_extract' in JOB_TOOL_REGISTRY"` | ✅ | ✅ green |
    | 10-03.T1 | 03 | 1 | EXTR-02, EXTR-06 | T-10-03-01, T-10-03-02, T-10-03-03 | Archive-bomb monitor + GC-safe task retention + post-terminal symlink quarantine timing | integration | `cd mcp-gateway && python -m pytest tests/extraction/test_extract_monitor.py -x --no-header -q` | ✅ | ✅ green |
    | 10-04.T1 | 04 | 2 | EXTR-01..06 | T-10-04-01..09 | 7 MCP tool handlers + disclaimer splice + D-22 error shapes + monitor spawn | unit + integration | `cd mcp-gateway && python -m pytest tests/extraction/ -m "not slow" -x --no-header -q` | ✅ | ✅ green |
    | 10-05.T1 | 05 | 3 | EXTR-01..06 | — | register_all_tools wires extract; EXPECTED_TOOLS 47→54; range bump | unit | `cd mcp-gateway && python -m pytest tests/test_tool_list.py -x --no-header -q` | ✅ | ✅ green |
    | 10-05.T2 | 05 | 3 | EXTR-01..06 | All | Wave 0 RED → GREEN flip on all 13 test files | unit + integration | `cd mcp-gateway && python -m pytest tests/extraction/ -x --no-header -q -m "not slow"` | ✅ | ✅ green |
    | 10-05.T3 | 05 | 3 | EXTR-01..06 | — | Nyquist sign-off; VALIDATION.md finalized | docs | n/a — verified by frontmatter `nyquist_compliant: true` | ✅ | ✅ green |

    Step C — Update Wave 0 Requirements section: tick all four checkboxes:
    - [x] `tests/extraction/test_extraction.py` → replaced by full 13-file extraction/ test package (Plan 01 expanded the original 2-file scope)
    - [x] `tests/test_tools_extract.py` → replaced by full 13-file extraction/ test package
    - [x] `tests/conftest.py` → replaced by mcp-gateway/tests/extraction/conftest.py (Plan 01)
    - [x] Docker probe (in container): scripts/probe_extraction_tools.sh exists (Plan 01) — to be run after the operator's next `./run_docker.sh` rebuild

    Step D — Update Validation Sign-Off section: tick all checkboxes; set **Approval: green**.
  </action>
  <verify>
    <automated>grep -c '^nyquist_compliant: true' .planning/phases/10-extraction-tier/10-VALIDATION.md && grep -c '^wave_0_complete: true' .planning/phases/10-extraction-tier/10-VALIDATION.md && grep -c 'Approval:.*green' .planning/phases/10-extraction-tier/10-VALIDATION.md && grep -cE '\| 10-0[12345]\.T[0-9]' .planning/phases/10-extraction-tier/10-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^nyquist_compliant: true' .planning/phases/10-extraction-tier/10-VALIDATION.md` returns 1
    - `grep -c '^wave_0_complete: true' .planning/phases/10-extraction-tier/10-VALIDATION.md` returns 1
    - `grep -c '^status: validated' .planning/phases/10-extraction-tier/10-VALIDATION.md` returns 1
    - `grep -c 'Approval:.*green\|Approval: green' .planning/phases/10-extraction-tier/10-VALIDATION.md` returns 1
    - `grep -cE '\| 10-0[12345]\.T[0-9]' .planning/phases/10-extraction-tier/10-VALIDATION.md` returns ≥ 7 (at least one row per task across plans 01-05)
    - Every row's Status column = ✅ green: `grep -c '✅ green' .planning/phases/10-extraction-tier/10-VALIDATION.md` returns ≥ 7
    - The placeholder `pending` row has been removed: `grep -c 'pending \| — \| —' .planning/phases/10-extraction-tier/10-VALIDATION.md` returns 0
  </acceptance_criteria>
  <done>10-VALIDATION.md finalized: nyquist_compliant=true, wave_0_complete=true, per-task verification map populated, approval=green.</done>
</task>

</tasks>

<threat_model>
  <asvs_level>1</asvs_level>
  <threats>
    <threat id="T-10-05-01">
      <description>register_all_tools wires extract in wrong order, breaking Phase 7 collision_check invariant (collisions surface AFTER backend_passthrough registers).</description>
      <severity>medium</severity>
      <mitigation>Task 1 acceptance criterion enforces line-number order: `jobs < extract < backend_passthrough`. The collision_check is imported BUT NOT registered (per Phase 7 D-11) — assert_no_collisions runs from app.py lifespan AFTER all register calls. Phase 10's 7 tool names are gateway-domain (no backend-pass-through analogues), so collision is structurally impossible; the check still runs to enforce the invariant.</mitigation>
    </threat>
    <threat id="T-10-05-02">
      <description>EXPECTED_TOOLS bump misses a Phase 10 tool name, causing test_no_unexpected_tools to fail at the next test run or vice-versa.</description>
      <severity>low</severity>
      <mitigation>Task 1 acceptance criterion enforces all 7 names present in test_tool_list.py via grep; test_tool_list.py's three top-level tests (test_all_expected_tools_present, test_tool_count_in_range, test_no_unexpected_tools) cross-check at runtime. Any drift fails at pytest time.</mitigation>
    </threat>
    <threat id="T-10-05-03">
      <description>Wave 0 RED → GREEN flip leaves stale `assert True` placeholders that mask actual failures (false positive on test pass).</description>
      <severity>medium</severity>
      <mitigation>Task 2 acceptance criterion `grep -c 'assert True' mcp-gateway/tests/extraction/test_*.py` returns 0 — hard ratchet on placeholder elimination. If any file legitimately ends in `assert True`, it must be replaced with a meaningful assertion.</mitigation>
    </threat>
    <threat id="T-10-05-04">
      <description>Slow-integration tests run without _require_<tool>_or_skip gates, failing on dev hosts that lack binwalk3/unblob/upx and blocking PR merges.</description>
      <severity>low</severity>
      <mitigation>Task 2 acceptance criterion enforces ≥ 5 occurrences of `_require_*_or_skip` across test files (one per slow test); pytest -m "not slow" pre-merge gate ensures dev hosts pass. Container CI (after the binwalk3 migration lands) runs the slow tests.</mitigation>
    </threat>
    <threat id="T-10-05-05">
      <description>Phase 5-9 regression: extending tools/__init__.py introduces an import cycle or breaks a Phase 5-9 invariant.</description>
      <severity>medium</severity>
      <mitigation>Task 2 acceptance criterion runs the full pytest suite minus slow tests: `cd mcp-gateway && python -m pytest tests/ -x --no-header -q -m "not slow"` exits 0. Any Phase 5-9 test failure surfaces immediately. Phase 10 imports follow the D-19 hierarchy (extraction.py imports nothing from tools.*; tools/extract.py only imports from primitives + tools.jobs + tools.samples + tools.case_dirs + tools.cases).</mitigation>
    </threat>
    <threat id="T-10-05-06">
      <description>VALIDATION.md frontmatter remains nyquist_compliant: false because the per-task map populator skips it, breaking the GSD verification gate.</description>
      <severity>low</severity>
      <mitigation>Task 3 acceptance criteria are explicit grep checks on each frontmatter field; status flips to `validated` + `nyquist_compliant: true` + `wave_0_complete: true` + Approval: green only if every check passes. The placeholder `pending` row must be removed.</mitigation>
    </threat>
  </threats>
</threat_model>

<verification>
- `python -c "from mcp_gateway.tools import register_all_tools; from mcp.server.fastmcp import FastMCP; m=FastMCP('t', stateless_http=True); register_all_tools(m); print(len(m._tool_manager._tools))"` prints a count between 50 and 60
- `cd mcp-gateway && python -m pytest tests/test_tool_list.py -x --no-header -q` exits 0 (Phase 10 EXPECTED_TOOLS additions + range bump pass)
- `cd mcp-gateway && python -m pytest tests/extraction/ -x --no-header -q -m "not slow"` exits 0 (all GREEN bodies pass on dev host)
- `cd mcp-gateway && python -m pytest tests/ -x --no-header -q -m "not slow"` exits 0 (no Phase 5-9 regression)
- 10-VALIDATION.md frontmatter: nyquist_compliant=true, wave_0_complete=true, status=validated, Approval=green
- Per-task verification map populated with ≥ 7 rows (one per task across plans 01-05)
</verification>

<success_criteria>
1. tools/__init__.py wires `extract.register(mcp)` between jobs and backend_passthrough (D-20 order respected)
2. test_tool_list.py EXPECTED_TOOLS bumped 47 → 54; tool_count_in_range bumped 35-50 → 35-60
3. All 13 Wave-0 RED-stub test files have GREEN behavioural bodies; non-slow tests pass on dev host
4. Slow-integration tests skip cleanly when binwalk3/unblob/upx are missing (via _require_*_or_skip)
5. Full pytest suite remains green (no Phase 5-9 regression)
6. 10-VALIDATION.md flipped: nyquist_compliant=true, wave_0_complete=true, status=validated, Approval=green; per-task verification map populated
7. _lineage.json auto-exposed via existing Phase 7 D-26 Resources walker (no walker edits — confirmed by inspection of tools/resources.py and Plan 04's lineage write path)
</success_criteria>

<output>
After completion, create `.planning/phases/10-extraction-tier/10-05-SUMMARY.md` documenting:
- tools/__init__.py edit summary (3 lines added)
- test_tool_list.py edit summary (7 tool names added to EXPECTED_TOOLS; range bumped 35-50 → 35-60)
- 13-file RED → GREEN flip summary (pass/skip counts on dev host)
- VALIDATION.md frontmatter final state
- Phase-level gate: full pytest invocation result (count of passes / skips / fails)
- Any Phase 5-9 surprises during regression (should be NONE)
- Phase 10 ready for `/gsd-verify-work` sign-off
</output>
