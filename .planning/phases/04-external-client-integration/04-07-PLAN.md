---
phase: 04-external-client-integration
plan: 07
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .planning/phases/04-external-client-integration/04-UAT.md
autonomous: false
requirements: [CLI-01]

must_haves:
  truths:
    - "A documented manual UAT checklist exists for verifying a real Claude Code session against the running container (D-12)"
    - "Checklist references the canonical onboarding flow (run_docker.sh --remote, templates/claude-code/.mcp.json, /mcp connect, tool call, resource browse)"
    - "Each step has an explicit pass/fail criterion the human-signoff gate can check"
  artifacts:
    - path: ".planning/phases/04-external-client-integration/04-UAT.md"
      provides: "Manual UAT checklist for CLI-01 human signoff (D-12 part b)"
  key_links:
    - from: ".planning/phases/04-external-client-integration/04-UAT.md"
      to: "templates/claude-code/.mcp.json and run_docker.sh --remote/--print-config"
      via: "explicit step instructions referencing those artifacts"
      pattern: "templates/claude-code"
---

<objective>
Create the manual UAT checklist for CLI-01 (D-12 part b). The automated raw-MCP smoke test (Plan 05) proves wire compatibility, but the actual Claude Code binary's behavior — config discovery, env-var expansion, MCP panel rendering, tool-call UX — can only be verified by hand. This is the human-signoff gate for shipping Phase 4.

Purpose: CLI-01 — bind the human verification to a reproducible script so any team member can run it identically.
Output: One markdown checklist file with a sequenced procedure + pass/fail criteria. Gated as a `checkpoint:human-action` task so the user explicitly walks through it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-external-client-integration/04-CONTEXT.md
@.planning/phases/04-external-client-integration/04-RESEARCH.md
@.planning/phases/04-external-client-integration/04-VALIDATION.md

<interfaces>
The manual UAT covers what the httpx smoke can't:
- Claude Code's `.mcp.json` discovery from project scope vs user scope
- `${VAR}` env-var expansion at MCP-config-parse time
- The MCP panel's "connected" state rendering
- Tool call rendering in the chat
- Resource browsing UX

Steps (per RESEARCH Manual-Only Verifications + D-12):
1. Start container with `./run_docker.sh --remote`
2. Copy ready-block snippet (or shell-export tokens) into `.mcp.json` location
3. Open Claude Code
4. Confirm `mare-toolbox` MCP shows "connected"
5. Run a tool call (e.g., `list_uploads`)
6. Browse a resource (e.g., `mare://cases/.../INDEX.md`)
7. Confirm result rendering in the chat
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the manual UAT checklist file</name>
  <files>.planning/phases/04-external-client-integration/04-UAT.md</files>
  <read_first>
    - .planning/phases/04-external-client-integration/04-CONTEXT.md (D-12)
    - .planning/phases/04-external-client-integration/04-RESEARCH.md (Manual-Only Verifications table)
    - templates/claude-code/.mcp.json (Plan 01 — the file users place)
  </read_first>
  <action>
Create `.planning/phases/04-external-client-integration/04-UAT.md` with the content below.

**IMPORTANT executor note:** In the embedded markdown below, the section dividers are rendered as `***` (alternative markdown HR) ONLY to avoid colliding with the YAML frontmatter delimiters of THIS plan file. When you write the actual `04-UAT.md` file, REPLACE every `***` divider with the standard `---` horizontal rule so the published checklist uses canonical Markdown HR.

The exact content (substitute `***` -> `---` on output):

````markdown
# Phase 04 — Manual UAT Checklist (CLI-01)

**Gate:** Human signoff for Phase 04 ship.
**Covers:** Real Claude Code binary connecting to a real running container — verifies behavior the automated httpx smoke (`mcp-gateway/tests/e2e/test_claude_code_smoke.py`) cannot: config discovery, env-var expansion at parse time, MCP panel rendering, tool-call UX, resource browsing.

**Prerequisites:** macOS or Linux host with Claude Code installed; Docker + Docker Compose v2; this repo cloned locally.

***

## 1. Start the container in remote mode

Action:
```bash
cd <repo-root>
./run_docker.sh --remote
```

PASS criteria:
- [ ] Command exits 0
- [ ] Stdout shows the ready-block: `MARE-MCP-Toolbox Gateway is ready`, with a `URL:` and `Token:` line
- [ ] `docker compose ps` shows `kali` in `running` state
- [ ] `cat workspace/.mcp-gateway-token` returns a non-empty token string

FAIL -> fix the gateway boot before proceeding (`docker compose logs kali`).

***

## 2. Verify --print-config reproduces the ready-block

Action:
```bash
./run_docker.sh --print-config
```

PASS criteria:
- [ ] Exits 0
- [ ] Stdout contains the same `MARE-MCP-Toolbox Gateway is ready` block as step 1
- [ ] The `Token:` value matches the one in `workspace/.mcp-gateway-token`

FAIL -> check Plan 02 (D-11) implementation.

***

## 3. Place the Claude Code config

Choice A — project scope (recommended for testing):
```bash
mkdir -p /tmp/mare-uat
cp templates/claude-code/.mcp.json /tmp/mare-uat/
export MARE_GATEWAY_TOKEN=$(cat workspace/.mcp-gateway-token)
export MARE_GATEWAY_URL=http://localhost:8080/mcp
cd /tmp/mare-uat
```

Choice B — user scope:
```bash
mkdir -p ~/.claude
cp templates/claude-code/.mcp.json ~/.claude/.mcp.json
export MARE_GATEWAY_TOKEN=$(cat <repo-root>/workspace/.mcp-gateway-token)
```

PASS criteria:
- [ ] `.mcp.json` is in the directory you'll launch Claude Code from (or in `~/.claude/`)
- [ ] `echo $MARE_GATEWAY_TOKEN` prints a non-empty value
- [ ] `echo $MARE_GATEWAY_URL` prints `http://localhost:8080/mcp` (or your custom URL)

***

## 4. Open Claude Code and verify connection

Action: Launch Claude Code (CLI or app) from the directory containing `.mcp.json` (Choice A) or with global config (Choice B).

In Claude Code, check the MCP panel (or `/mcp` command):

PASS criteria:
- [ ] `mare-toolbox` server appears in the MCP panel
- [ ] Status shows **connected** / **ready** (NOT "error", NOT "stopped")
- [ ] No red 401 / "unauthorized" indicator
- [ ] Tool count is at least 22 (Phase 2 surface) — backend passthrough may add more

FAIL paths:
- 401 -> token mismatch. Re-run `./run_docker.sh --print-config`, refresh `MARE_GATEWAY_TOKEN`, restart Claude Code.
- "Connection refused" -> container not running, or wrong URL. Confirm `curl http://localhost:8080/healthz`.
- "Connected" but tool count = 0 -> backend init failure. Check `docker compose logs kali`.

***

## 5. Run a tool call

In a Claude Code chat, run:

```
Use the mare-toolbox MCP server to call list_uploads. Show me the raw JSON result.
```

PASS criteria:
- [ ] Claude Code visibly invokes the `list_uploads` tool (UI shows tool-call indicator)
- [ ] Result is a JSON array (possibly empty if no prior uploads)
- [ ] No error rendered

Optional follow-up: ask it to call `get_active_backend` and confirm the response shape `{ "backend": "ida" | "bn" | "ghidra" | "none" }`.

***

## 6. Browse a resource

Prerequisite: at least one case directory must exist under `/agent/status/`. If you've never run a triage, do so first via the mastra starter or by asking Claude Code to call `mare_run_triage` against `/agent/examples/samples/mfc42ul.dll`.

In a Claude Code chat:

```
List the MCP resources from the mare-toolbox server.
```

PASS criteria:
- [ ] Claude Code returns a list of `mare://cases/<case>/<artifact>` URIs
- [ ] At least one URI ends in `.json`, `.md`, or `.txt`

Then ask:

```
Read the resource mare://cases/<your-case>/CURRENT_STATE.json and show me the content.
```

PASS criteria:
- [ ] Content renders (JSON shape from `artifact-spec.md` — `sample_path`, `phase`, `artifacts`, etc.)
- [ ] No traversal-rejection error for legitimate URIs

***

## 7. Negative — wrong token rejected

Action:
```bash
export MARE_GATEWAY_TOKEN=obviously-wrong-token-xyz
```

Restart Claude Code (or trigger an MCP reload).

PASS criteria:
- [ ] `mare-toolbox` shows error / 401 / disconnected
- [ ] No tool calls succeed

Restore the real token (`export MARE_GATEWAY_TOKEN=$(cat <repo-root>/workspace/.mcp-gateway-token)`) and confirm the connection comes back.

***

## 8. Cleanup

```bash
docker compose down
unset MARE_GATEWAY_TOKEN MARE_GATEWAY_URL
# clear shell scrollback if you exposed the token publicly
```

***

## Signoff

When ALL boxes above are checked:

- [ ] **CLI-01 manual UAT: PASSED** — _signed_ ____________ _date_ ___________

If any box failed, file a gap-closure note in the relevant phase-04 SUMMARY and re-run after the fix.

***

## Related artifacts

- Automated raw-MCP smoke (CI-friendly, complementary): [`mcp-gateway/tests/e2e/test_claude_code_smoke.py`](../../mcp-gateway/tests/e2e/test_claude_code_smoke.py)
- The CC config template under test: [`templates/claude-code/.mcp.json`](../../templates/claude-code/.mcp.json)
- The runtime print helper: `./run_docker.sh --print-config` (run_docker.sh)
````

This file lives in the phase planning dir — not user-facing repo docs. Its purpose is to gate Phase 4's "complete" signal until a human walks through it.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-external-client-integration/04-UAT.md && grep -q 'Manual UAT Checklist' .planning/phases/04-external-client-integration/04-UAT.md && grep -q 'templates/claude-code/.mcp.json' .planning/phases/04-external-client-integration/04-UAT.md && grep -q -- '--print-config' .planning/phases/04-external-client-integration/04-UAT.md && grep -q 'mare://cases/' .planning/phases/04-external-client-integration/04-UAT.md && grep -q 'CLI-01 manual UAT: PASSED' .planning/phases/04-external-client-integration/04-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-external-client-integration/04-UAT.md` exists
    - Contains 8 numbered sections covering: container start, --print-config, config placement, Claude Code connect, tool call, resource browse, negative auth, cleanup
    - References `templates/claude-code/.mcp.json`, `--print-config`, `mare://cases/`, `MARE_GATEWAY_TOKEN`
    - Contains an explicit signoff line `CLI-01 manual UAT: PASSED`
    - Each section has at least one `- [ ]` checkbox PASS criterion
  </acceptance_criteria>
  <done>UAT checklist file created with sequenced procedure, explicit pass/fail criteria, and signoff line.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Human walks through 04-UAT.md and signs off CLI-01</name>
  <files>.planning/phases/04-external-client-integration/04-UAT.md</files>
  <read_first>
    - .planning/phases/04-external-client-integration/04-UAT.md (the checklist created in Task 1)
  </read_first>
  <action>
The user (operator) walks through the 8-section checklist in `04-UAT.md` against a real workstation that has Claude Code installed and Docker available.

Steps:
1. Open `.planning/phases/04-external-client-integration/04-UAT.md`.
2. Walk through every numbered step (1-8) in order on a real workstation with Claude Code installed.
3. Check off every `- [ ]` PASS criterion as it is verified (edit the markdown file in place to mark `[x]`).
4. If any step fails: stop, file a gap-closure note in the phase-04 SUMMARY, fix the underlying plan/task, and re-walk from step 1.
5. Once all 8 sections pass, sign the final line `CLI-01 manual UAT: PASSED — _signed_ ___ _date_ ___` (replace blanks with operator name + ISO date).
6. Type `approved` in the orchestrator chat to resume Phase 4 completion.

This task is a `checkpoint:human-action` gate — Claude does NOT automate it. The orchestrator pauses until the operator returns the resume signal.
  </action>
  <verify>
    <automated>grep -q 'CLI-01 manual UAT: PASSED' .planning/phases/04-external-client-integration/04-UAT.md && grep -E '^\- \[x\]' .planning/phases/04-external-client-integration/04-UAT.md | wc -l | awk '$1 >= 8 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - At least 8 `- [x]` boxes exist in `04-UAT.md` (verified by grep count)
    - The signoff line `CLI-01 manual UAT: PASSED` is present and the trailing `_signed_` and `_date_` placeholders have been replaced with non-empty values
    - User explicitly types "approved" in the orchestrator to resume
  </acceptance_criteria>
  <done>Manual UAT signed off; orchestrator resumed; Phase 4 cleared for completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human to checklist | The checklist is only as effective as the human running it. Each step must be unambiguously verifiable. |
| Test environment to token in shell | Step 7 deliberately uses an obviously-wrong-token literal; step 8 instructs cleanup of scrollback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-T1 | I (Information Disclosure) | UAT exposes the live token in shell scrollback | accept | Already a known-and-documented behavior of `--remote` and `--print-config`. Step 8 instructs scrollback cleanup. |
| T-04-T2 | E (Elevation of Privilege) | Wrong-token negative test (step 7) might leave Claude Code in a bad state | mitigate | Step 7 explicitly instructs restoring the real token afterward; PASS criteria for step 4 are re-asserted before signoff. |
| T-04-T3 | R (Repudiation) | Signoff is informal | accept | Phase 4 is single-developer; a Markdown checklist is sufficient. CI gating is out of scope per CONTEXT deferred. |
</threat_model>

<verification>
- `04-UAT.md` exists with 8 numbered sections + signoff
- Grep checks for required references pass
- Human signoff line present (filled by Task 2's checkpoint)
</verification>

<success_criteria>
- Manual UAT checklist file exists, sequenced, with explicit pass/fail criteria per step
- Checkpoint task gates Phase 4 completion on human signoff
- Phase 4 cannot be marked complete until the UAT is signed
</success_criteria>

<output>
Create `.planning/phases/04-external-client-integration/04-07-SUMMARY.md` documenting: file path, section list, signoff line presence, completion status of the human checkpoint.
</output>
