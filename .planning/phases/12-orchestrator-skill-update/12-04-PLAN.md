---
phase: 12
plan: 04
type: execute
wave: 2
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md
  - workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md
  - workspace/.claude/skills/malware-analysis-orchestrator/references/deep-analysis-checklist.md
  - mcp-gateway/tests/snapshots/SKILL.md.sha256
autonomous: true
requirements: [SKILL-01, SKILL-03]
requirements_addressed: [SKILL-01, SKILL-02, SKILL-03, SKILL-04]
tags: [skill-content, backend-priority, dual-mode-prose, snapshot-baseline]
must_haves:
  truths:
    - "SKILL.md `Disassembly and Decompilation` section lists priority `IDA Pro MCP > Binary Ninja MCP > Ghidra MCP > r2 (CLI)` — IDA first"
    - "Legacy v1.0 BN-first ordering (`Binary Ninja MCP server -- primary tool`) is REMOVED from SKILL.md"
    - "SKILL.md teaches `mcp__mare-toolbox__get_active_backend()` first-call pattern (D-02) and references W-N files via the Workflow Decision Tree"
    - "SKILL.md has a new `## Dual-Mode Operation` section documenting D-07/D-09 decision rule and the `run_shell` sentinel"
    - "SKILL.md has a `## Dynamic-Mode Skip Behavior` section documenting D-18 placeholder + INDEX.md row pattern"
    - "SKILL.md description frontmatter mentions IDA Pro MCP alongside Binary Ninja and Ghidra (single-line YAML; YAML parses cleanly)"
    - "SKILL.md legacy-prefix appendix lists `mcp__ida_mcp__*`, `mcp__binary_ninja_headless_mcp__*`, `mcp__ghidra_headless_mcp__*` for scripts-mode users (D-02 second sentence)"
    - "`references/workflow.md` updated: tool prefixes corrected to v1.1, cross-reference to W-N files added at Phase 0/1/2 step lists (D-01 sweep + D-04 routing pointer)"
    - "`references/deep-analysis-checklist.md` gains a `## Workflow Selection` subsection pointing at `references/deep-re-workflows.md` (D-06)"
    - "`mcp-gateway/tests/snapshots/SKILL.md.sha256` baseline created via `UPDATE_SKILL_SNAPSHOT=1 pytest ...::test_skill_md_snapshot`"
    - "ALL 12 test functions in `test_skill_md_dual_mode.py` flip GREEN (or PASS-with-warning for snapshot)"
  artifacts:
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md"
      provides: "Top-level skill instructions reflecting v1.1 tool surface, IDA-first priority, dual-mode operation, dynamic-mode skip pattern, Workflow Decision Tree routing to W-N files"
      contains: "IDA Pro MCP > Binary Ninja MCP > Ghidra MCP"
      min_lines: 280
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md"
      provides: "Phase 0-8 workflow with v1.1 tool prefixes and W-N cross-references"
      contains: "deep-re-workflows.md"
      min_lines: 100
    - path: "workspace/.claude/skills/malware-analysis-orchestrator/references/deep-analysis-checklist.md"
      provides: "Component-prioritization framework + new Workflow Selection subsection (D-06)"
      contains: "Workflow Selection"
      min_lines: 80
    - path: "mcp-gateway/tests/snapshots/SKILL.md.sha256"
      provides: "Soft snapshot baseline; refresh via `UPDATE_SKILL_SNAPSHOT=1 pytest ::test_skill_md_snapshot`"
      contains: ""
      min_lines: 1
  key_links:
    - from: "SKILL.md `Workflow Decision Tree` section"
      to: "references/deep-re-workflows.md (index of W-N files)"
      via: "explicit link in the routing rule list"
      pattern: "deep-re-workflows\\.md|references/workflows/W-"
    - from: "SKILL.md `Dual-Mode Operation` section"
      to: "run_shell sentinel + CURRENT_STATE.json `mode` field"
      via: "decision rule code block"
      pattern: "run_shell|state\\.mode"
    - from: "SKILL.md `Disassembly Backend Guidance` section"
      to: "get_active_backend() first-call pattern + legacy-prefix appendix"
      via: "explicit instruction"
      pattern: "get_active_backend|mcp__ida_mcp__"
    - from: "references/workflow.md Phase 0 step list"
      to: "v1.1 wrapper names (run_file, run_die, run_rabin2)"
      via: "tool-prefix sweep"
      pattern: "mcp__mare-toolbox__|scripts/"
    - from: "references/deep-analysis-checklist.md `Workflow Selection`"
      to: "deep-re-workflows.md catalog"
      via: "relative link"
      pattern: "deep-re-workflows\\.md"
---

<objective>
Rewrite `SKILL.md` to land the SKILL-01 backend-priority fix (D-01), the v1.1 tool surface (D-02), the dual-mode operation section (D-07/D-09), the Workflow Decision Tree routing into W-N files (D-04), and the dynamic-mode skip-behavior section (D-18). Sweep `references/workflow.md` and `references/deep-analysis-checklist.md` for the same priority fix and add cross-references to the W-N catalog (D-06). Then create the SKILL.md sha256 baseline (D-13) by running the test under `UPDATE_SKILL_SNAPSHOT=1`.

This plan is the final wave. It flips the remaining RED tests from Plan 01 GREEN: `test_backend_priority_correct`, `test_no_legacy_bn_first_priority`, `test_dual_mode_invariant` (SKILL.md + workflow.md parametrized cases), `test_skill_documents_dynamic_skip_behavior`, and creates the snapshot file so `test_skill_md_snapshot` produces no UserWarning on a re-run.

Frontmatter discipline (Pitfall 1, Pitfall 8): the `description:` field stays single-line YAML scalar. Length stays comparable to the current 461 characters. Edit pattern: replace "Binary Ninja MCP usage, Ghidra MCP usage" with "IDA Pro MCP, Binary Ninja MCP, or Ghidra MCP usage" — a +20-character delta, within token budget.

Per Claude's Discretion in CONTEXT.md and 12-RESEARCH.md A6: `references/agent-roles.md` and `references/interesting-signals.md` are tool-agnostic; NO edits needed (verified by reading both files — agent-roles.md has no tool-name references; interesting-signals.md mentions raw CLI tool names like `strings`, `rabin2` which remain valid in both modes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-orchestrator-skill-update/12-CONTEXT.md
@.planning/phases/12-orchestrator-skill-update/12-RESEARCH.md
@.planning/phases/12-orchestrator-skill-update/12-VALIDATION.md
@.planning/phases/12-orchestrator-skill-update/12-01-PLAN.md
@.planning/phases/12-orchestrator-skill-update/12-02-PLAN.md
@.planning/phases/12-orchestrator-skill-update/12-03-PLAN.md

# Files this plan modifies
@workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md
@workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md
@workspace/.claude/skills/malware-analysis-orchestrator/references/deep-analysis-checklist.md

# Files referenced (Plan 02 outputs)
@workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md

# Authoritative gateway tool surface
@mcp-gateway/README.md

<interfaces>
<!-- Canonical strings to use VERBATIM in SKILL.md edits -->

Backend priority (D-01) — exact substring to insert:
  "IDA Pro MCP > Binary Ninja MCP > Ghidra MCP > r2 (CLI)"

Canonical server name (D-10):
  ".mcp.json" key:   "mare-toolbox"
  Host tool prefix:  "mcp__mare-toolbox__*"

First-call pattern (D-02):
  mcp__mare-toolbox__get_active_backend()
  → returns {"backend": "ida" | "binary_ninja" | "ghidra"}
  → agent then uses BACKEND-NATIVE tool names per get_active_backend() output:
       IDA:           decompile(addr=...), list_funcs(...), xrefs_to(...), idalib_*
       Binary Ninja:  decompile_function(...), get_callers(...), etc.
       Ghidra:        decompile_function(...), reference.to(...), etc.

Sentinel tool (D-07):
  run_shell (canonical mode-detection target — exists only in gateway mode)
  Secondary: get_active_backend

Legacy prefixes (D-02 appendix, scripts-mode users wiring backends directly in .mcp.json):
  mcp__ida_mcp__*
  mcp__binary_ninja_headless_mcp__*
  mcp__ghidra_headless_mcp__*

Dynamic-mode skip artifact path (D-18):
  <case_dir>/dynamic/<step-slug>-skipped.md
  INDEX.md "## Skipped steps" subsection with row:
  | Step | Reason | Placeholder |

Current SKILL.md violations to remove:
  - Line 102: "If Binary Ninja MCP or Ghidra MCP is available (prefer Binary Ninja MCP; fall back to Ghidra MCP; fall back to `r2`)"
  - Line 141: "**Binary Ninja MCP server** -- primary tool"
  - Line 163: "Disassembly: Binary Ninja MCP -> Ghidra MCP -> `r2`."
  - Lines 208-260: separate equal-weight "Binary Ninja MCP Guidance" + "Ghidra MCP Guidance" sections (collapse into one Disassembly Backend Guidance section per D-02)

PyYAML availability (sanity check): if absent in test env, the frontmatter test in Plan 01 uses regex parse — frontmatter edit must keep the YAML well-formed regardless.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md with D-01 backend priority + D-02 active-backend pattern + D-07/D-09 dual-mode section + D-04 W-N routing + D-18 dynamic-skip section</name>
  <files>workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md</files>
  <read_first>
    - workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md (current 275-line state; preserve Overview, Required Rules, Quick Start, Artifact Discipline, Completion Criteria, Expected Deliverable Style sections verbatim where the tool surface is unchanged)
    - .planning/phases/12-orchestrator-skill-update/12-CONTEXT.md (D-01, D-02, D-04, D-07, D-09, D-10, D-18)
    - .planning/phases/12-orchestrator-skill-update/12-RESEARCH.md State of the Art table (lines 489-503) — the precise old/new mapping
    - workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md (Plan 02 output — link target for Workflow Decision Tree)
    - mcp-gateway/tests/test_skill_md_dual_mode.py (`test_backend_priority_correct`, `test_no_legacy_bn_first_priority`, `test_dual_mode_invariant`, `test_skill_documents_dynamic_skip_behavior` requirements)
  </read_first>
  <action>
    Make six targeted edits to SKILL.md. Preserve frontmatter shape, Overview section, Required Rules numbering, Quick Start scripts list, Artifact Discipline section, Completion Criteria, and Expected Deliverable Style verbatim where unaffected.

    **Edit 1 — Frontmatter `description:` field (line 3):**
    Replace:
    ```
    description: Structured malware triage and reverse-engineering orchestration for PE, ELF, and Mach-O binaries with strict artifact dumping to a status folder. Use when requests involve malware sample analysis, strings triage, API/import analysis, behavioral hypothesis generation, component mapping, deep-analysis planning, Binary Ninja MCP usage, Ghidra MCP usage, or role-based orchestration (orchestrator/planner/reporter) with complete intermediate outputs.
    ```
    With (single line; keep YAML valid):
    ```
    description: Structured malware triage and reverse-engineering orchestration for PE, ELF, and Mach-O binaries with strict artifact dumping to a status folder. Use when requests involve malware sample analysis, strings triage, API/import analysis, behavioral hypothesis generation, component mapping, deep-analysis planning, IDA Pro MCP / Binary Ninja MCP / Ghidra MCP usage via the mare-toolbox gateway or local disassembler MCP backends, or role-based orchestration (orchestrator/planner/reporter) with complete intermediate outputs.
    ```
    Verify YAML parses: `python3 -c "import yaml; [print(d) for d in yaml.safe_load_all(open('workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md'))][0]"` exits 0.

    **Edit 2 — Workflow Decision Tree (lines 92-102):**
    Replace the entire `## Workflow Decision Tree` section with:
    ```markdown
    ## Workflow Decision Tree

    1. Detect binary format using `file` command output (gateway: `mcp__mare-toolbox__run_file(case_dir, sample)`; scripts: `run_shell file <sample>` fallback).
    2. **Route into a deep-RE workflow** per [`references/deep-re-workflows.md`](references/deep-re-workflows.md):
       - Packer detected (`run_die` deep field) → **W-1** (`references/workflows/W-1-packed-binary-triage.md`)
       - Firmware blob (file reports `data` or known header) → **W-6** (`references/workflows/W-6-firmware-unpack.md`)
       - Cross-arch ELF (mips/arm/ppc/sparc/riscv) → **W-7** (`references/workflows/W-7-cross-arch-iot.md`)
       - ELF, native x86/x86_64 → **W-2** (`references/workflows/W-2-elf-deep-dive.md`) then optionally **W-5** (`references/workflows/W-5-dynamic-api-trace.md`) if `state.dynamic_mode_enabled == true`
       - PE → **W-3** (`references/workflows/W-3-pe-deep-dive.md`)
       - Mach-O → no dedicated W-N (v1.1); apply the Mach-O tooling section below + ad-hoc steps
       - Shellcode/exploit-dev context → **W-4** (`references/workflows/W-4-rop-gadget-hunt.md`) in addition to format workflow
    3. If imports are packed/obfuscated and packer is unknown: continue with strings and static structure, prioritize unpacking in deep plan (W-1 step 4 — unknown packer branch).
    4. If clear C2, persistence, or kernel indicators are present: raise priority and move them to top of `<case_dir>/09_priority_queue.md`.
    5. If capa is available and format is PE or ELF: run capa scan for ATT&CK-mapped capability identification (gateway: `scripts/scan_capa.sh <sample>` or `run_shell capa <sample>`; scripts: `scripts/scan_capa.sh <sample>` — fallback).
    6. **Disassembly backend** — see the `## Disassembly Backend Guidance` section below. Priority `IDA Pro MCP > Binary Ninja MCP > Ghidra MCP > r2 (CLI)`. Agent calls `mcp__mare-toolbox__get_active_backend()` first in gateway mode (fallback: read `.mcp.json` to determine which backend MCP is wired) and uses the backend's native tool names.
    ```

    **Edit 3 — Disassembly section (lines 139-143):**
    Replace the entire `### Disassembly and Decompilation` numbered list (lines 139-143) with:
    ```markdown
    ### Disassembly and Decompilation

    Backend priority: **`IDA Pro MCP > Binary Ninja MCP > Ghidra MCP > r2 (CLI)`**. Matches `mcp-gateway/README.md` and `mcp-gateway/src/mcp_gateway/app.py`.

    1. **IDA Pro MCP** — primary disassembler in gateway mode. Backend-native tools surface directly (collision check at gateway startup); use `mcp__mare-toolbox__get_active_backend()` to confirm IDA is pinned, then call native names like `decompile(addr=...)`, `list_funcs(...)`, `xrefs_to(...)`, `idalib_*` (see `mcp-gateway/README.md` §"IDA Pro MCP native tools" for the full catalog). Scripts-mode users wiring IDA directly in `.mcp.json`: tool prefix is `mcp__ida_mcp__*` (legacy appendix below). Fallback when neither path is available: see step 4.
    2. **Binary Ninja MCP** — secondary disassembler. Gateway-mode: native tool names after `get_active_backend()` returns `"binary_ninja"` (e.g., `decompile_function(...)`, `get_callers(...)`). Scripts-mode prefix: `mcp__binary_ninja_headless_mcp__*` (NOT `import binaryninja`).
    3. **Ghidra MCP** — tertiary disassembler. Gateway-mode: native tool names after `get_active_backend()` returns `"ghidra"` (e.g., `decompile_function(...)`, `reference.to(...)`, `ghidra_script(...)`). Scripts-mode prefix: `mcp__ghidra_headless_mcp__*` (NOT `import pyghidra`).
    4. **r2 (radare2 CLI)** — fallback when no disassembler MCP is available. Gateway-mode: use `mcp__mare-toolbox__open_r2_session(...)` + `r2_cmd(session_id, "<command>")` for iterative analysis. Scripts-mode fallback: `run_shell r2 -A -c '<cmd>' <sample>` one-shot.
    ```

    **Edit 4 — Fallback Order list (line 163):**
    Replace:
    ```
    4. Disassembly: Binary Ninja MCP -> Ghidra MCP -> `r2`.
    ```
    With:
    ```
    4. Disassembly: IDA Pro MCP -> Binary Ninja MCP -> Ghidra MCP -> `r2` (gateway: call `mcp__mare-toolbox__get_active_backend()` to discover pinned backend; scripts fallback: backend prefix in `.mcp.json`).
    ```

    **Edit 5 — REPLACE the entire `## Binary Ninja MCP Guidance` + `## Ghidra MCP Guidance` block (lines 208-260) with a single `## Disassembly Backend Guidance` section (D-02 collapse):**
    ```markdown
    ## Disassembly Backend Guidance

    The active disassembler backend is determined at gateway start by priority `IDA > Binary Ninja > Ghidra`. The skill talks to the active backend through the gateway's backend pass-through — backend-native tool definitions win over gateway wrappers, so agents see the richest schema available.

    ### First-call pattern (gateway mode)

    ```
    backend = mcp__mare-toolbox__get_active_backend()  # returns {"backend": "ida" | "binary_ninja" | "ghidra"}
    if backend == "ida":
        decompile(addr=<entry_addr>)            # IDA native
        list_funcs(...)                         # IDA native
        xrefs_to(addr=<addr>)                   # IDA native
    elif backend == "binary_ninja":
        decompile_function(function=<name>)     # BN native
        get_callers(function=<name>)            # BN native
    elif backend == "ghidra":
        decompile_function(function=<name>)     # Ghidra native
        reference.to(address=<addr>)            # Ghidra native
    ```

    Do NOT `import binaryninja`, `import pyghidra`, or `import ida_*` directly — go through the MCP surface. The gateway exposes capabilities including:

    - Disassembly and decompilation (HLIL/MLIL/LLIL for BN; C-like + P-code for Ghidra; IDA native pseudocode)
    - Cross-references (API xrefs, string xrefs, call graphs)
    - Patching (binary modification, NOP-ing, instruction replacement)
    - Scripting (backend-specific: `ghidra_script`, BN snippets, IDA `idalib_*`)
    - Type and structure recovery
    - Symbol and annotation management

    ### Scripts-mode prefix appendix (legacy)

    When the user wires a disassembler MCP directly in `.mcp.json` (inner-agent default; no gateway), the tool prefix matches the `.mcp.json` server key. Canonical legacy prefixes:

    - `mcp__ida_mcp__*` (IDA Pro headless MCP)
    - `mcp__binary_ninja_headless_mcp__*` (Binary Ninja MCP)
    - `mcp__ghidra_headless_mcp__*` (Ghidra MCP)

    In gateway mode the canonical prefix is `mcp__mare-toolbox__*` for all gateway-native and pass-through tools (one server, one prefix). The `mare-toolbox` server name is set in `.mcp.json` by `run_docker.sh`.

    ### Artifacts (any backend)

    If a disassembler MCP is available, include these subdirectories under the case directory:

    1. `disassembly/` for function disassembly.
    2. `decompilation/` for decompiler output (HLIL/MLIL for BN; C-like for Ghidra; pseudocode for IDA).
    3. `xrefs/` for API and string cross-references.
    4. `patches/` for any binary patches applied during analysis.

    Capture only high-value functions first:

    1. Entry point and initialization chain.
    2. Persistence and service management functions.
    3. Network connect/send/recv and protocol handlers.
    4. Process injection and memory manipulation functions.
    5. Kernel/device communication and IOCTL handlers.
    ```

    **Edit 6 — INSERT two new sections BEFORE `## Completion Criteria` (currently line 261):**
    ```markdown
    ## Dual-Mode Operation

    The skill operates in two modes determined ONCE at case-init by `scripts/init_status_tree.sh` and cached in `<case_dir>/CURRENT_STATE.json` as `"mode": "gateway" | "scripts"`.

    ### Mode detection sentinel (D-07)

    The canonical sentinel is the `run_shell` tool — it exists ONLY in gateway mode. Secondary confirmation is `get_active_backend` tool presence. `init_status_tree.sh` probes the MCP gateway HTTP endpoint with the bearer token from `MCP_GATEWAY_TOKEN`; if reachable, mode = `"gateway"`; otherwise mode = `"scripts"`.

    ### Per-step decision rule (D-09)

    Every step in W-1..W-7 (and the workflow.md phases) follows this rule:

    ```
    if state.mode == "gateway":
        call mcp__mare-toolbox__run_X(case_dir, sample, ...)   # gateway-native or pass-through
    else:
        call scripts/<fallback>.sh ...                          # local-script path
        # or: run_shell <tool> <args>                           # gateway up but no skill script
    ```

    The skill's `scripts/` directory (`collect_strings.sh`, `collect_imports.sh`, `scan_yara.sh`, `scan_capa.sh`, `init_status_tree.sh`, `update_state.py`, `rank_signals.py`, `build_hypothesis.py`, `resolve_case.sh`) is the canonical local-script path. Scripts are NOT mode-aware — the skill prose carries the mode decision.

    ## Dynamic-Mode Skip Behavior (D-18)

    `CURRENT_STATE.json` carries two dynamic-related fields (see `references/artifact-spec.md` §`CURRENT_STATE.json`):

    - `dynamic_mode_enabled` (bool) — fast-skip gate (gateway started with `--dynamic`?)
    - `dynamic_capabilities` (object) — fine-grained capability map (`ptrace_scope`, `binfmt_misc`, `qemu_archs`, `netns_feasible`)

    When a W-N step requires dynamic mode but `state.dynamic_mode_enabled == false` (or a fine-grained capability is missing, e.g., `mipsel` not in `qemu_archs`):

    1. SKIP the live tool call. Do NOT attempt the dynamic step silently.
    2. Write a placeholder artifact at `<case_dir>/dynamic/<step-slug>-skipped.md` containing:
       - Which step was skipped (W-N reference + slug)
       - The missing capability (e.g., "ptrace_scope=2, kernel restricts attach", "mipsel not in qemu_archs")
       - A remediation hint: "run `./run_docker.sh --dynamic` and re-init the case, OR re-probe via `scripts/update_state.py --status-dir <case_dir> --probe-dynamic`"
    3. Append a row to `<case_dir>/INDEX.md` under a new `## Skipped steps` subsection (create if absent):

       | Step | Reason | Placeholder |
       |------|--------|-------------|
       | W-5/strace-all | dynamic_mode_enabled=false | dynamic/strace-all-skipped.md |
       | W-7/qemu-mipsel-run | mipsel not in qemu_archs | dynamic/qemu-mipsel-run-skipped.md |

    The `<step-slug>` is constrained to a safe basename (closed list per W-N file: `strace-all`, `ltrace-libc`, `gdb-open-bt`, `qemu-<arch>-run`). Do NOT compose arbitrary paths.

    Re-probe path: if the operator later starts the container with `--dynamic`, re-run `scripts/update_state.py --status-dir <case_dir> --probe-dynamic` to refresh `dynamic_mode_enabled` and `dynamic_capabilities` without re-initing the case.
    ```

    **Critical guard — dual-mode invariant compliance:**
    After all six edits, EVERY `mcp__mare-toolbox__*` mention in SKILL.md must have a fallback (`scripts/`, `fallback`, or `else`) within ±3 lines per the D-12 regex window. The Workflow Decision Tree edit (Edit 2) explicitly pairs gateway + scripts forms on each step. The Disassembly section (Edit 3) pairs gateway-mode native call with scripts-mode prefix. The Dual-Mode Operation section (Edit 6) is itself the fallback context. Run the test before commit:
    ```bash
    cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_dual_mode_invariant -v 2>&1 | grep -E 'SKILL\.md|PASSED|FAILED'
    ```
    The `SKILL.md` parametrized case MUST be GREEN.

    **Frontmatter validation:**
    ```bash
    python3 -c "import yaml; doc=next(yaml.safe_load_all(open('workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md'))); assert doc['name']=='malware-analysis-orchestrator'; assert 'IDA' in doc['description']; print('frontmatter OK')"
    ```
  </action>
  <verify>
    <automated>cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_backend_priority_correct tests/test_skill_md_dual_mode.py::test_no_legacy_bn_first_priority tests/test_skill_md_dual_mode.py::test_skill_documents_dynamic_skip_behavior tests/test_skill_md_dual_mode.py::test_dual_mode_invariant -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Backend priority string present: `grep -F 'IDA Pro MCP > Binary Ninja MCP > Ghidra MCP > r2' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns >= 1 line
    - Legacy BN-first phrasing REMOVED: `grep -F 'Binary Ninja MCP server -- primary tool' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns 0 lines
    - get_active_backend reference present: `grep -F 'mcp__mare-toolbox__get_active_backend' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns >= 1 line
    - Workflow Decision Tree routes to W-N: `grep -cE 'W-[1-7]|deep-re-workflows\.md' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns >= 8 (at least one W-N reference per workflow plus the index link)
    - Dual-Mode Operation section present: `grep -F '## Dual-Mode Operation' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns 1 line; `grep -F 'state.mode' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns >= 1 line
    - Dynamic-Mode Skip Behavior section present: `grep -F '## Dynamic-Mode Skip Behavior' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns 1 line; `grep -E 'dynamic/[\w<>.-]+-skipped\.md' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns >= 1 line
    - Legacy-prefix appendix present: `grep -cE 'mcp__ida_mcp__|mcp__binary_ninja_headless_mcp__|mcp__ghidra_headless_mcp__' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns >= 3
    - Frontmatter YAML still parses: `python3 -c "import yaml; doc=next(yaml.safe_load_all(open('workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md'))); assert doc['name']=='malware-analysis-orchestrator'"` exits 0
    - Frontmatter mentions IDA Pro MCP: `grep -F 'IDA Pro MCP' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md | head -1` shows a line within the first 5 lines (frontmatter region)
    - Equal-weight BN/Ghidra Guidance sections removed: `grep -cE '^## Binary Ninja MCP Guidance$|^## Ghidra MCP Guidance$' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns 0
    - Unified guidance section present: `grep -F '## Disassembly Backend Guidance' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns 1 line
    - All 4 SKILL-01/03/04 tests GREEN: `cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_backend_priority_correct tests/test_skill_md_dual_mode.py::test_no_legacy_bn_first_priority tests/test_skill_md_dual_mode.py::test_skill_documents_dynamic_skip_behavior -v 2>&1 | grep -F 'PASSED' | wc -l` returns 3
    - Dual-mode invariant GREEN for SKILL.md: `cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_dual_mode_invariant -v 2>&1 | grep 'SKILL\.md' | grep -F 'PASSED'` returns 1 line
    - No abbreviated prefix: `grep -E 'mcp__mare__(?!toolbox)' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns nothing
    - Preserved sections intact: `grep -cE '^## Quick Start$|^## Required Rules$|^## Artifact Discipline$|^## Completion Criteria$|^## Expected Deliverable Style$' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns 5
  </acceptance_criteria>
  <done>SKILL.md has IDA-first priority, get_active_backend first-call pattern, unified Disassembly Backend Guidance, Dual-Mode Operation section with decision rule, Dynamic-Mode Skip Behavior section with placeholder + INDEX.md row pattern. All 4 SKILL-01/03/04 tests GREEN. Frontmatter YAML still parses. No abbreviated prefix. Existing core sections preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep references/workflow.md (D-01 tool-prefix update + D-04 W-N cross-reference) and references/deep-analysis-checklist.md (D-06 Workflow Selection subsection)</name>
  <files>workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md, workspace/.claude/skills/malware-analysis-orchestrator/references/deep-analysis-checklist.md</files>
  <read_first>
    - workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md (current 97 lines; preserve Phase 0-8 numbered list structure; only inject the W-N cross-reference and the v1.1 tool-prefix sweep)
    - workspace/.claude/skills/malware-analysis-orchestrator/references/deep-analysis-checklist.md (current 73 lines; preserve all existing sections; only ADD the Workflow Selection subsection per D-06)
    - workspace/.claude/skills/malware-analysis-orchestrator/references/deep-re-workflows.md (Plan 02 output — link target)
    - .planning/phases/12-orchestrator-skill-update/12-CONTEXT.md (D-01, D-04, D-06)
  </read_first>
  <action>
    **Edit A — `references/workflow.md`:**

    1. Insert a new section at the top, AFTER the `# Workflow` heading (line 1) and BEFORE `## Phase 0: Intake and Fingerprinting` (line 3):

    ```markdown
    > **Mode-aware execution:** Every step has both a gateway-mode call (`mcp__mare-toolbox__*`) and a scripts-mode fallback (`scripts/<helper>.sh` or `run_shell <tool>`). Read `<case_dir>/CURRENT_STATE.json` field `mode` to pick the path (see SKILL.md §`Dual-Mode Operation`).
    >
    > **Deep-RE workflow routing:** After Phase 0 detects the format, route into one of the W-N deep-RE workflows per [`references/deep-re-workflows.md`](deep-re-workflows.md):
    > - `detected_format == elf` → **W-2** (static) → optionally **W-5** (dynamic)
    > - `detected_format == pe` → **W-3**
    > - packer detected → **W-1**
    > - firmware blob → **W-6**
    > - cross-arch ELF → **W-7**
    > - exploit-dev / shellcode context → **W-4**
    >
    > This file (`workflow.md`) is the v1.0 Phase 0-8 baseline; W-N files override
    > step-3-and-later for the matched format.

    ```

    2. In `## Phase 0: Intake and Fingerprinting` (currently lines 3-25), add one footnote-style line at the bottom (after the existing `Write results to <case_dir>/00_sample_profile.md.`):

    ```markdown

    **v1.1 tool calls (per step decision rule):**
    - `file` → gateway: `mcp__mare-toolbox__run_file(case_dir, sample)`; scripts: `run_shell file <sample>` fallback.
    - `die` / `diec` → gateway: `mcp__mare-toolbox__run_die(case_dir, sample, deep=true)`; scripts: `run_shell die -d <sample>` fallback.
    - `rabin2 -I` → gateway: `mcp__mare-toolbox__run_rabin2(case_dir, sample, command="info", json=true)`; scripts: `run_shell rabin2 -I <sample>` fallback.
    - `r2 /ca` (crypto scan) → gateway: `mcp__mare-toolbox__open_r2_session(...)` + `r2_cmd(session_id, "/ca aes")`; scripts: `run_shell r2 -A -c '/ca aes' <sample>` fallback.
    - `scan_yara.sh` and `scan_capa.sh` → same script in both modes (scripts ARE the canonical path; D-08).
    ```

    3. In `## Phase 1: Raw Strings Collection` (lines 27-32), append:

    ```markdown

    **v1.1 tool calls:** scripts-mode uses `scripts/collect_strings.sh <sample>` (canonical local path); gateway-mode can invoke the same script via `run_shell scripts/collect_strings.sh <sample>` OR use the typed wrappers `mcp__mare-toolbox__run_shell` with `strings`, `rabin2`, `floss` argv per step. Fallback: `run_shell` chain.
    ```

    4. In `## Phase 2: Raw API/Import Collection` (lines 34-41), append:

    ```markdown

    **v1.1 tool calls:** scripts-mode uses `scripts/collect_imports.sh <sample>`; gateway-mode prefers the typed wrappers `mcp__mare-toolbox__run_rabin2(case_dir, sample, command="imports", json=true)`, `mcp__mare-toolbox__run_readelf(case_dir, sample, mode="dyn-symbols")`, `mcp__mare-toolbox__run_nm(case_dir, sample, mode="dyn", demangle=true)`, `mcp__mare-toolbox__run_objdump(case_dir, sample, mode="private-headers")`. Fallback for any missing wrapper: `scripts/collect_imports.sh` OR `run_shell <tool> <args>`.
    ```

    5. In the existing `## Phase 6: Deep Analysis Plan` (lines 75-84), replace with the SAME content plus this prepended sentence:

    ```markdown
    > **At this phase**, branch into the matched W-N file from `references/deep-re-workflows.md`. The W-N file is the concrete recipe; this Phase 6 list is the framework for ordering tasks across multiple W-N workflows.
    ```

    Other phases (3, 4, 5, 7, 8) are tool-agnostic and need NO edits.

    **Edit B — `references/deep-analysis-checklist.md`:**

    Insert a new section AFTER `# Deep Analysis Checklist` (line 1) and BEFORE `## Component Prioritization` (line 5):

    ```markdown

    ## Workflow Selection

    This checklist is the *component-prioritization framework*. Concrete per-format / per-goal recipes live in separate W-N workflow files. Pick the relevant W-N before applying the framework below:

    1. **Read** `references/deep-re-workflows.md` for the workflow catalog and routing decision tree.
    2. **Route** by `detected_format` + signals (packer / firmware / cross-arch / exploit-dev / dynamic-mode availability).
    3. **Apply** this checklist's `Component Prioritization` and per-component template ACROSS multiple W-N workflow outputs (e.g., for a packed PE: W-1 unpack → W-3 static → optionally W-5 dynamic).
    4. **Honor mode + dynamic-mode gates** per SKILL.md §`Dual-Mode Operation` and §`Dynamic-Mode Skip Behavior`. Skipped dynamic steps appear in `<case_dir>/INDEX.md` under `## Skipped steps`.

    ```

    Preserve `## Component Prioritization`, `## Per-Component Analysis Template`, `## Function-Level Tasks`, `## Cross-Component Tasks`, `## IOC and Detection Engineering`, `## Quality Gates` sections verbatim.

    **Verification:**
    ```bash
    cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_dual_mode_invariant -v 2>&1 | grep -E 'workflow\.md|deep-analysis-checklist|PASSED|FAILED'
    ```
    The `workflow.md` parametrized case MUST be GREEN (deep-analysis-checklist.md is NOT in the parametrized file list per the test module but should still not violate the invariant).
  </action>
  <verify>
    <automated>cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_dual_mode_invariant -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - workflow.md has Mode-aware execution preamble: `grep -F 'Mode-aware execution' workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md` returns 1 line
    - workflow.md cross-references deep-re-workflows.md: `grep -F 'deep-re-workflows.md' workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md` returns >= 1 line
    - workflow.md mentions all 7 W-N IDs: `grep -cE 'W-[1-7]' workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md` returns >= 7
    - workflow.md has v1.1 wrapper calls in Phase 0/1/2 footnotes: `grep -cE 'mcp__mare-toolbox__' workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md` returns >= 5
    - Each gateway-tool reference paired with fallback: dual-mode test GREEN on workflow.md (`cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_dual_mode_invariant -v 2>&1 | grep 'workflow\.md\]' | grep -F 'PASSED'` returns 1 line)
    - Original Phase 0-8 headings preserved: `grep -cE '^## Phase [0-8]:' workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md` returns 9
    - deep-analysis-checklist.md has Workflow Selection subsection: `grep -F '## Workflow Selection' workspace/.claude/skills/malware-analysis-orchestrator/references/deep-analysis-checklist.md` returns 1 line
    - deep-analysis-checklist.md references deep-re-workflows.md: `grep -F 'deep-re-workflows.md' workspace/.claude/skills/malware-analysis-orchestrator/references/deep-analysis-checklist.md` returns >= 1 line
    - Original sections preserved: `grep -cE '^## Component Prioritization$|^## Per-Component Analysis Template$|^## Function-Level Tasks$|^## Cross-Component Tasks$|^## IOC and Detection Engineering$|^## Quality Gates$' workspace/.claude/skills/malware-analysis-orchestrator/references/deep-analysis-checklist.md` returns 6
    - No abbreviated prefix in either file: `grep -E 'mcp__mare__(?!toolbox)' workspace/.claude/skills/malware-analysis-orchestrator/references/workflow.md workspace/.claude/skills/malware-analysis-orchestrator/references/deep-analysis-checklist.md` returns nothing
  </acceptance_criteria>
  <done>`workflow.md` has Mode-aware preamble + W-N routing block + v1.1 footnotes in Phase 0/1/2; existing Phase 0-8 structure preserved. `deep-analysis-checklist.md` has new Workflow Selection subsection pointing at `deep-re-workflows.md`; all six existing sections preserved verbatim. Dual-mode test GREEN on `workflow.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the SKILL.md sha256 baseline (D-13) and run the full skill test suite</name>
  <files>mcp-gateway/tests/snapshots/SKILL.md.sha256</files>
  <read_first>
    - mcp-gateway/tests/test_skill_md_dual_mode.py (test_skill_md_snapshot logic — UPDATE_SKILL_SNAPSHOT=1 path)
    - workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md (file the snapshot anchors)
    - mcp-gateway/tests/snapshots/.gitkeep (placeholder created by Plan 01)
  </read_first>
  <action>
    1. Run the snapshot-refresh path:
    ```bash
    cd mcp-gateway && UPDATE_SKILL_SNAPSHOT=1 pytest tests/test_skill_md_dual_mode.py::test_skill_md_snapshot -v
    ```
    This creates `mcp-gateway/tests/snapshots/SKILL.md.sha256` with the current sha256 hex digest of SKILL.md followed by a newline.

    2. Verify the snapshot file was created and matches the live hash:
    ```bash
    expected=$(sha256sum workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md | awk '{print $1}')
    actual=$(cat mcp-gateway/tests/snapshots/SKILL.md.sha256 | tr -d '\n')
    test "$expected" = "$actual" && echo "snapshot matches" || (echo "DRIFT" && exit 1)
    ```

    3. Run the full test module without the UPDATE_SKILL_SNAPSHOT env var. All 12 tests must PASS (or pass-with-warning for the snapshot test if there's any drift — there should be none immediately after step 1):
    ```bash
    cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py -v 2>&1 | tail -30
    ```
    Expected: zero FAILED, zero ERROR. Some parametrized cases may emit UserWarning (the snapshot test, if drift happens between step 1 and step 3) — that's the soft signal, not a fail.

    4. Run the full mcp-gateway test suite (excluding e2e) to confirm no regression:
    ```bash
    cd mcp-gateway && pytest tests/ -q --ignore=tests/e2e 2>&1 | tail -5
    ```
    Expected: the test count INCREASED by ~12 (the new module) vs the pre-Phase-12 baseline. Zero new failures relative to the pre-Phase-12 state.

    5. If any test from `test_skill_md_dual_mode.py` is still RED after step 1-3, STOP and report. Common late-stage failures:
       - `test_dual_mode_invariant` on `workflow.md`: a `mcp__mare-toolbox__*` mention without `scripts/` or `fallback` in ±3 lines — fix the offending block in Task 2 and re-run.
       - `test_no_abbreviated_prefix` on any file: an `mcp__mare__` (without `-toolbox`) slipped through — grep and fix.
       - `test_skill_documents_dynamic_skip_behavior`: the placeholder pattern or `Skipped steps` substring missing from SKILL.md / W-5 / W-6 / W-7.
  </action>
  <verify>
    <automated>cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Snapshot file exists: `test -f mcp-gateway/tests/snapshots/SKILL.md.sha256`
    - Snapshot is a 64-char sha256 hex digest + newline: `wc -c mcp-gateway/tests/snapshots/SKILL.md.sha256` returns 65 (64 hex chars + 1 newline)
    - Snapshot matches live SKILL.md sha256: `expected=$(sha256sum workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md | awk '{print $1}'); actual=$(cat mcp-gateway/tests/snapshots/SKILL.md.sha256 | tr -d '\n'); test "$expected" = "$actual"` exits 0
    - All 12 tests PASS without UPDATE_SKILL_SNAPSHOT: `cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py -q 2>&1 | tail -1` reports ZERO failures
    - No regression in full suite: `cd mcp-gateway && pytest tests/ -q --ignore=tests/e2e 2>&1 | tail -1` shows the new test count delta and ZERO new failures vs the pre-Phase-12 baseline
    - No UserWarning emitted on the snapshot test (because we just refreshed): `cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py::test_skill_md_snapshot -v 2>&1 | grep -F 'warning summary' | wc -l` should be 0 (no warnings registered)
  </acceptance_criteria>
  <done>Snapshot file exists, contains the live SKILL.md sha256. All 12 tests in `test_skill_md_dual_mode.py` PASS. Full mcp-gateway test suite shows zero regressions; the new test module adds ~12 green tests to the baseline. The skill is now v1.1-aligned end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill prose → agent invocations | SKILL.md teaches `mcp__mare-toolbox__*` calls; every example pairs gateway form with a scripts-mode fallback so a single-mode-only error doesn't strand the agent |
| Skill prose → run_shell argv | Every `run_shell` example in SKILL.md uses argv-list form (`run_shell <tool> <arg>`); NEVER `run_shell "tool $sample"` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | SKILL.md `run_shell` examples | mitigate | Every `run_shell` example uses the named-argv form. Verify before commit: `grep -nE 'run_shell.*\$\{?[a-zA-Z_]' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns ZERO matches (no variable interpolation in the run_shell prose — sample paths are passed as literal arg positions). |
| T-12-02 | Information disclosure | SKILL.md gateway-probe references | mitigate | The bearer-token-via-env-var rule (T-12-02 from Plan 03) is reinforced: SKILL.md never embeds a literal token; the only token reference is `MCP_GATEWAY_TOKEN` env var name in the Dual-Mode Operation section. Verify: `grep -E 'Bearer\s+[A-Za-z0-9]{8,}' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns ZERO matches |
| T-12-03 | Information disclosure | Skip-placeholder `<step-slug>` path | mitigate | Dynamic-Mode Skip Behavior section explicitly states "`<step-slug>` is constrained to a safe basename (closed list per W-N file: ...). Do NOT compose arbitrary paths." Verify: `grep -F 'closed list' workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md` returns >= 1 line |
| T-12-08 | Tampering | Snapshot baseline | accept | sha256 is content-addressable; corruption of the baseline file is detected by the soft-warning advisory. Drift is the design intent — drift triggers explicit review, not silent acceptance |
</threat_model>

<verification>
1. `cd mcp-gateway && pytest tests/test_skill_md_dual_mode.py -v` — ALL 12 tests PASS
2. `cd mcp-gateway && pytest tests/ -q --ignore=tests/e2e 2>&1 | tail -3` — zero new failures vs pre-Phase-12 baseline
3. `python3 -c "import yaml; doc=next(yaml.safe_load_all(open('workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md'))); print(doc['name'])"` outputs `malware-analysis-orchestrator`
4. End-to-end skill smoke: `tmp=$(mktemp -d) && touch "$tmp/x.bin" && workspace/.claude/skills/malware-analysis-orchestrator/scripts/init_status_tree.sh "$tmp/x.bin" --new` exits 0 AND the resulting `CURRENT_STATE.json` has all three dynamic-mode keys.
5. Snapshot baseline exists and matches: `test "$(cat mcp-gateway/tests/snapshots/SKILL.md.sha256 | tr -d '\n')" = "$(sha256sum workspace/.claude/skills/malware-analysis-orchestrator/SKILL.md | awk '{print $1}')"` exits 0
6. No regressions in any existing test: `cd mcp-gateway && git diff --stat HEAD~4 | grep tests/` shows only the new test file added; pre-existing test files untouched (or only minor unrelated edits if any).
</verification>

<success_criteria>
- [ ] SKILL.md backend priority corrected (`IDA > BN > Ghidra > r2`); legacy BN-first phrasing removed
- [ ] SKILL.md teaches `get_active_backend()` first-call pattern with backend-native tool names
- [ ] SKILL.md has unified `## Disassembly Backend Guidance` (Binary Ninja MCP Guidance + Ghidra MCP Guidance collapsed per D-02); legacy-prefix appendix preserved
- [ ] SKILL.md has new `## Dual-Mode Operation` section (D-07/D-09 decision rule + run_shell sentinel)
- [ ] SKILL.md has new `## Dynamic-Mode Skip Behavior` section (D-18 placeholder + INDEX.md row + safe slug constraint)
- [ ] SKILL.md Workflow Decision Tree routes by format/signals into W-N files
- [ ] Frontmatter YAML parses; description mentions IDA Pro MCP
- [ ] `workflow.md` has mode-aware preamble + W-N routing block + v1.1 footnotes in Phase 0/1/2; existing structure preserved
- [ ] `deep-analysis-checklist.md` has new Workflow Selection subsection (D-06); existing sections preserved
- [ ] `mcp-gateway/tests/snapshots/SKILL.md.sha256` exists and matches live SKILL.md hash
- [ ] ALL 12 tests in `test_skill_md_dual_mode.py` PASS
- [ ] Full mcp-gateway test suite shows zero new failures
</success_criteria>

<output>
After completion, create `.planning/phases/12-orchestrator-skill-update/12-04-SUMMARY.md` documenting:
- Six SKILL.md edit applications (Edit 1-6) and line-count delta
- workflow.md and deep-analysis-checklist.md sweep details
- Snapshot baseline hash + size
- Final test result: 12/12 GREEN in `test_skill_md_dual_mode.py`
- Full-suite delta vs baseline (test count, failures)
- Any deviations from the planned edits (Claude's Discretion: alternative wording choices, table-vs-prose, etc.)
</output>
