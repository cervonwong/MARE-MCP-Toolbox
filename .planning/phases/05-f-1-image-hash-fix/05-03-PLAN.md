---
phase: 05-f-1-image-hash-fix
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - mcp-gateway/tests/test_image_hash.py
autonomous: true
requirements:
  - FOUND-01
requirements_addressed:
  - FOUND-01

must_haves:
  truths:
    - "`mcp-gateway/tests/test_image_hash.py` exists and is collected by pytest."
    - "Pytest collects at least 11 tests from that file (7 plain test functions + 1 parametrized test with 4 cases = 11 collected nodes)."
    - "`test_sc1_src_edit_changes_hash` passes: touching `mcp-gateway/src/x.py` in the fixture changes the helper's output."
    - "`test_sc2_pyproject_edit_changes_hash` passes: editing `mcp-gateway/pyproject.toml` changes the helper's output."
    - "All four parametrized cases of `test_sc3_pruned_writes_do_not_change_hash` pass: writes under `__pycache__`, `.venv`, `mcp_gateway.egg-info`, `.pytest_cache` do NOT change the helper's output."
    - "`test_binja_toggle_changes_hash` passes: setting `INSTALL_BINARY_NINJA=1` with a stub zip changes the hash relative to the clean-env baseline (D-10)."
    - "`test_baseline_hash_stable`, `test_helper_clean_env_no_binja_inputs`, `test_missing_dockerfile_exits_nonzero`, `test_helper_exists_and_executable` all pass."
    - "The entire `pytest mcp-gateway/tests/test_image_hash.py -x` run completes in under 2 seconds and exits 0."
    - "Tests are hermetic: no network, no docker, no real mcp-gateway dependency mutation. All fixture data lives under `tmp_path`."
    - "Tests pass `env=` dict explicitly to subprocess (only `PATH` and `HOME`) — never `os.environ` directly — preventing dev-shell env bleed (Pitfall 3 from RESEARCH.md)."
  artifacts:
    - path: "mcp-gateway/tests/test_image_hash.py"
      provides: "Regression test for FOUND-01 / Success Criteria 1-4 + D-10 Binja toggle guard"
      min_lines: 100
      contains: "test_sc1_src_edit_changes_hash"
  key_links:
    - from: "mcp-gateway/tests/test_image_hash.py"
      to: "scripts/compute_image_hash.sh"
      via: "subprocess.run with explicit env dict and tmp_path build root"
      pattern: "subprocess\\.run\\(\\[.bash., str\\(HELPER\\)"
    - from: "build_root fixture"
      to: "tmp_path (pytest)"
      via: "fixture creates Dockerfile + docker-bin/ + mcp-gateway/ tree under tmp_path"
      pattern: "@pytest\\.fixture"
---

<objective>
Create `mcp-gateway/tests/test_image_hash.py` by copying the test file skeleton from `.planning/phases/05-f-1-image-hash-fix/05-RESEARCH.md` §"Code Examples" §"Test File Skeleton" verbatim. The file MUST contain all 7 test functions defined in that skeleton, including the 1 parametrized test (4 cases), totaling 11 collected pytest nodes — one test per Success Criterion / D-09 / D-10 obligation per VALIDATION.md's verification map.

Purpose: Lock the FOUND-01 invariant — agent edits to `mcp-gateway/src/` (and `pyproject.toml`) trigger an image rebuild on the next `./run_docker.sh` invocation, while writes to pruned paths do not. The test is the regression-of-record named by Success Criterion 4 in ROADMAP.md.

Output: New hermetic pytest file at `mcp-gateway/tests/test_image_hash.py` that runs in <2s and passes 11/11 tests against the helper from Plan 05-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-f-1-image-hash-fix/05-CONTEXT.md
@.planning/phases/05-f-1-image-hash-fix/05-RESEARCH.md
@.planning/phases/05-f-1-image-hash-fix/05-VALIDATION.md
@CLAUDE.md
@mcp-gateway/tests/test_print_config.py
@mcp-gateway/tests/conftest.py
@mcp-gateway/pyproject.toml
@scripts/compute_image_hash.sh

<interfaces>
<!-- Helper contract this test file exercises (defined in Plan 05-02). -->

From scripts/compute_image_hash.sh:
  Invocation: `bash scripts/compute_image_hash.sh [BUILD_ROOT]`
  Env vars (all optional, default to `0` / empty):
    INSTALL_BINARY_NINJA  ("0" | "1")
    BINARY_NINJA_ZIP      (absolute path to zip; required if toggle=1)
    INSTALL_IDA_PRO       ("0" | "1")
    IDA_PRO_ZIP           (absolute path to zip; required if toggle=1)
  stdout: 64-char lowercase sha256 hex + trailing newline
  exit codes: 0 success | 2 build root / Dockerfile missing | 3 toggle=1 but zip missing

<!-- Existing pytest infrastructure this test integrates with. -->

From mcp-gateway/pyproject.toml [tool.pytest.ini_options]:
  testpaths = ["tests"]
  asyncio_mode = "auto"  # not used by this test

From mcp-gateway/tests/conftest.py:
  # Provides shared fixtures (bearer_token, tmp_upload_dir, tmp_status_dir, fake_backend_mcp).
  # The new test does NOT depend on any of these; it uses tmp_path only.

From mcp-gateway/tests/test_print_config.py:
  REPO_ROOT = Path(__file__).resolve().parents[2]   # exact pattern to reuse
  # Subprocess invocation idiom: subprocess.run([...], capture_output=True, text=True, timeout=10)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create mcp-gateway/tests/test_image_hash.py with 11 collected tests</name>
  <read_first>
    - .planning/phases/05-f-1-image-hash-fix/05-RESEARCH.md §"Code Examples" §"Test File Skeleton" (lines ≈350-484 — this is the verbatim source for the test body)
    - .planning/phases/05-f-1-image-hash-fix/05-RESEARCH.md §"Validation Architecture" §"Test Cases — Detailed Assertion Contracts" (the 11-row table that defines the exact contract per test)
    - .planning/phases/05-f-1-image-hash-fix/05-RESEARCH.md §"Validation Architecture" §"Fixture Contents — Exact Specification (per D-08)" (the byte-spec for the fixture tree)
    - .planning/phases/05-f-1-image-hash-fix/05-RESEARCH.md §"Common Pitfalls" §"Pitfall 3: Environment Bleed Between Tests" (justifies the explicit `env=` dict, never `os.environ`)
    - .planning/phases/05-f-1-image-hash-fix/05-RESEARCH.md §"Common Pitfalls" §"Pitfall 4: sha256sum Output Includes the Filename" (justifies single-fixture-per-test, not cross-tmp_path comparison)
    - .planning/phases/05-f-1-image-hash-fix/05-VALIDATION.md §"Per-Task Verification Map" (the row-by-row contract this file must satisfy)
    - .planning/phases/05-f-1-image-hash-fix/05-CONTEXT.md §"D-07" through §"D-11" (locked decisions on test placement, design, hermeticity)
    - mcp-gateway/tests/test_print_config.py (subprocess pattern of record — REPO_ROOT formula, subprocess.run idiom)
    - mcp-gateway/tests/conftest.py (existing shared fixtures — verify the new file does not collide with anything)
    - scripts/compute_image_hash.sh (the helper being tested, from Plan 05-02 — confirm the contract)
  </read_first>
  <behavior>
    Test file MUST collect exactly 11 pytest nodes:

    1. `test_helper_exists_and_executable` — asserts `HELPER.is_file()` and `os.access(HELPER, os.X_OK)`.
    2. `test_baseline_hash_stable[build_root]` — invokes `_hash(build_root)` twice against the same fixture, asserts equal.
    3. `test_sc1_src_edit_changes_hash[build_root]` — baseline hash; overwrite `build_root/mcp-gateway/src/x.py` with `"x = 2  # changed\n"`; recompute; assert different. This is the SC-1 and SC-4 regression-of-record.
    4. `test_sc2_pyproject_edit_changes_hash[build_root]` — baseline hash; overwrite `build_root/mcp-gateway/pyproject.toml` with `"[project]\nname='mcp-gateway-edited'\n"`; recompute; assert different.
    5-8. `test_sc3_pruned_writes_do_not_change_hash` parametrized over 4 cases:
       - `("__pycache__", "new.pyc")`
       - `(".venv", "new-file")`
       - `("mcp_gateway.egg-info", "new-meta")`
       - `(".pytest_cache", "new-cache")`
       For each: baseline hash; write `b"new content"` to `build_root/mcp-gateway/{subdir}/{filename}` (creating parent dirs as needed); recompute; assert equal.
    9. `test_binja_toggle_changes_hash[build_root, tmp_path]` — write stub `tmp_path/binja.zip` with bytes `b"PK\x03\x04stub"`; baseline `_hash(build_root)` with clean env; toggled `_hash(build_root, env_extra={"INSTALL_BINARY_NINJA": "1", "BINARY_NINJA_ZIP": str(stub_zip)})`; assert different.
    10. `test_helper_clean_env_no_binja_inputs[build_root]` — invoke `_hash(build_root)` with no INSTALL_* env vars; assert returns 64-char hex.
    11. `test_missing_dockerfile_exits_nonzero[tmp_path]` — create `tmp_path/docker-bin/` and `tmp_path/mcp-gateway/` but no Dockerfile; invoke helper; assert `returncode != 0` and `"Dockerfile" in res.stderr`.

    Helper `_hash(build_root, env_extra=None)` MUST construct env from `{"PATH": os.environ["PATH"], "HOME": os.environ.get("HOME", "/tmp")}` and only union in `env_extra` — never pass `env=os.environ` (Pitfall 3).

    Fixture `build_root(tmp_path)` MUST create the exact tree in RESEARCH.md §"Fixture Contents — Exact Specification": Dockerfile (content `"FROM scratch\n"`), `docker-bin/tool.sh` (content `"#!/bin/bash\necho hi\n"`), and the full `mcp-gateway/` subtree including `src/x.py`, `pyproject.toml`, `__pycache__/stale.pyc`, `.venv/lib/marker`, `mcp_gateway.egg-info/PKG-INFO`, `.pytest_cache/CACHEDIR.TAG` — each with the exact content strings/bytes specified.

    All assertions check string equality / inequality on 64-char hex hashes — no Python-side hashlib (RESEARCH.md §"Don't Hand-Roll"). Hash comparison: write fixture → compute baseline → mutate file → recompute → compare; always on the same `tmp_path` (Pitfall 4).
  </behavior>
  <files>mcp-gateway/tests/test_image_hash.py</files>
  <action>
    Create `mcp-gateway/tests/test_image_hash.py` by copying the test body verbatim from RESEARCH.md §"Code Examples" §"Test File Skeleton". The byte-spec is:

    1. **Module docstring** — `"""FOUND-01 / Phase 5: regression test for the mcp-gateway content hash."""` with the Invariants list (SC-1, SC-2, SC-3, SC-4, Bonus) inline as documented in RESEARCH.md.
    2. **Imports** — `from __future__ import annotations`; `os`; `subprocess`; `from pathlib import Path`; `import pytest`.
    3. **Module-level constants**:
       ```
       REPO_ROOT = Path(__file__).resolve().parents[2]
       HELPER = REPO_ROOT / "scripts" / "compute_image_hash.sh"
       ```
    4. **`build_root` fixture** (function-scope; `@pytest.fixture` — no scope arg) — creates the tree per RESEARCH.md §"Fixture Contents — Exact Specification". Every file path and every content string in that spec MUST be present:
       - `tmp_path/Dockerfile` ← `"FROM scratch\n"`
       - `tmp_path/docker-bin/` (mkdir)
       - `tmp_path/docker-bin/tool.sh` ← `"#!/bin/bash\necho hi\n"`
       - `tmp_path/mcp-gateway/src/` (mkdir parents=True)
       - `tmp_path/mcp-gateway/src/x.py` ← `"x = 1\n"`
       - `tmp_path/mcp-gateway/pyproject.toml` ← `"[project]\nname='mcp-gateway'\n"`
       - `tmp_path/mcp-gateway/__pycache__/` (mkdir)
       - `tmp_path/mcp-gateway/__pycache__/stale.pyc` ← bytes `b"\x00\x01"`
       - `tmp_path/mcp-gateway/.venv/lib/` (mkdir parents=True)
       - `tmp_path/mcp-gateway/.venv/lib/marker` ← `"venv\n"`
       - `tmp_path/mcp-gateway/mcp_gateway.egg-info/` (mkdir)
       - `tmp_path/mcp-gateway/mcp_gateway.egg-info/PKG-INFO` ← `"metadata\n"`
       - `tmp_path/mcp-gateway/.pytest_cache/` (mkdir)
       - `tmp_path/mcp-gateway/.pytest_cache/CACHEDIR.TAG` ← `"Signature\n"`
       Return `tmp_path`.
    5. **`_hash(build_root, env_extra=None) -> str` helper** — exactly as in the skeleton: builds base env `{"PATH": os.environ["PATH"], "HOME": os.environ.get("HOME", "/tmp")}`, optionally `.update(env_extra)`, invokes `subprocess.run(["bash", str(HELPER), str(build_root)], capture_output=True, text=True, timeout=10, env=base_env)`, asserts `returncode == 0` with stderr in message, strips output, asserts `len(out) == 64`, returns out.
    6. **Test functions** — all 7, exactly as named below; the parametrize creates 4 nodes from one function (total 11 collected nodes):
       - `test_helper_exists_and_executable()` (no fixture)
       - `test_baseline_hash_stable(build_root)`
       - `test_sc1_src_edit_changes_hash(build_root)`
       - `test_sc2_pyproject_edit_changes_hash(build_root)`
       - `@pytest.mark.parametrize("pruned_subdir,filename", [...])` decorator with the 4 tuples `("__pycache__", "new.pyc")`, `(".venv", "new-file")`, `("mcp_gateway.egg-info", "new-meta")`, `(".pytest_cache", "new-cache")` immediately above `test_sc3_pruned_writes_do_not_change_hash(build_root, pruned_subdir, filename)`
       - `test_binja_toggle_changes_hash(build_root, tmp_path)`
       - `test_helper_clean_env_no_binja_inputs(build_root)`
       - `test_missing_dockerfile_exits_nonzero(tmp_path)`

    Do NOT shorten the file. Do NOT remove any of the 7 test functions. Do NOT remove any of the 4 parametrize cases. Do NOT add hashlib or any Python-side hashing logic (RESEARCH.md §"Don't Hand-Roll"). Do NOT pass `env=os.environ` anywhere — every `subprocess.run` MUST receive an explicit `env=` dict of only `PATH` + `HOME` (+ optional `INSTALL_*` / `*_ZIP` extras for the toggle test).

    All `subprocess.run` calls MUST have `timeout=10`.

    After writing, run `pytest mcp-gateway/tests/test_image_hash.py -x --collect-only -q` to confirm 11 nodes are collected, then run `pytest mcp-gateway/tests/test_image_hash.py -x` to confirm all pass.

    If any test fails because the helper from Plan 05-02 has a defect, STOP and report the failure — do NOT modify the test to make a buggy helper pass. The test is the contract; the helper conforms to it.
  </action>
  <verify>
    <automated>pytest mcp-gateway/tests/test_image_hash.py -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f mcp-gateway/tests/test_image_hash.py` succeeds
    - `python -c "import ast; ast.parse(open('mcp-gateway/tests/test_image_hash.py').read())"` exits 0 (valid Python syntax)
    - `pytest mcp-gateway/tests/test_image_hash.py --collect-only -q | grep -cE '^mcp-gateway/tests/test_image_hash\.py::'` returns at least 11
    - `pytest mcp-gateway/tests/test_image_hash.py -x` exits 0
    - All 11 collected tests pass (no `F`, no `E`, no `s` skips)
    - The run completes in under 2 seconds wall-clock (D-11) — verify with `time pytest mcp-gateway/tests/test_image_hash.py -x`
    - `grep -c 'def test_' mcp-gateway/tests/test_image_hash.py` returns at least 7 (the 7 test functions)
    - `grep -c '@pytest.mark.parametrize' mcp-gateway/tests/test_image_hash.py` returns at least 1
    - `grep -c '"__pycache__"' mcp-gateway/tests/test_image_hash.py` returns at least 1 (parametrize case)
    - `grep -c '"\.venv"' mcp-gateway/tests/test_image_hash.py` returns at least 1
    - `grep -c '"mcp_gateway\.egg-info"' mcp-gateway/tests/test_image_hash.py` returns at least 1
    - `grep -c '"\.pytest_cache"' mcp-gateway/tests/test_image_hash.py` returns at least 1
    - `grep -c 'INSTALL_BINARY_NINJA' mcp-gateway/tests/test_image_hash.py` returns at least 1 (the toggle test)
    - `grep -c 'os.environ' mcp-gateway/tests/test_image_hash.py` returns at least 2 BUT every match is in the context of `os.environ["PATH"]` or `os.environ.get("HOME", ...)` — NEVER `env=os.environ` (verify by `grep -c 'env=os.environ' mcp-gateway/tests/test_image_hash.py` returns 0)
    - `grep -c 'hashlib' mcp-gateway/tests/test_image_hash.py` returns 0 (no Python-side hashing — RESEARCH.md §"Don't Hand-Roll")
    - `grep -c 'timeout=10' mcp-gateway/tests/test_image_hash.py` returns at least 2 (helper invocation + missing-Dockerfile test)
    - The full mcp-gateway test suite (`pytest mcp-gateway/tests/ -x`) still passes (the new test does not break any existing test)
  </acceptance_criteria>
  <done>
    11 tests collected and passing; suite runtime <2s; no existing test broken; hermetic (no real `mcp-gateway/` tree mutation, no network, no docker).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest runner → subprocess (bash + helper) | The test invokes the helper via `subprocess.run` with an explicit, minimal env (`PATH` + `HOME` + optional INSTALL_*/zip). No env bleed from the developer shell (Pitfall 3). |
| test fixture → real filesystem | All fixture writes are scoped to pytest's `tmp_path`. The real `mcp-gateway/` tree is never modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | test fixture writes | mitigate | Every fixture write uses `tmp_path` (pytest function-scope per-test temp dir). No path outside `tmp_path` is written. Verified by `grep -c "tmp_path" mcp-gateway/tests/test_image_hash.py` ≥ several, and absence of any string concatenation onto `REPO_ROOT` for writes. |
| T-05-03-02 | Tampering | env bleed from developer shell | mitigate | `_hash()` builds an explicit env dict with only `PATH` and `HOME` from `os.environ`. Never `env=os.environ`. Per RESEARCH.md §"Pitfall 3". Verified by `grep -c 'env=os.environ' = 0`. |
| T-05-03-03 | Information Disclosure | hash output | accept | INFO — hash is non-secret (it is the image tag). |
| T-05-03-04 | Denial of Service | test runtime | mitigate | All `subprocess.run` calls have `timeout=10`. Per D-11, full suite <2s — bounded workload. |
| T-05-03-05 | Tampering | stub binja.zip in toggle test | accept | LOW — the test writes a 6-byte stub (`b"PK\x03\x04stub"`) into its own `tmp_path`. The helper only `sha256sum`s the file; no execution surface. Stub disappears with `tmp_path` cleanup. |
| T-05-03-06 | Spoofing | helper invocation path | accept | INFO — `HELPER = REPO_ROOT / "scripts" / "compute_image_hash.sh"` resolves through `Path(__file__).resolve()`, anchored to the test file's location. Not influenced by env vars. |

All threats are LOW/INFO/MEDIUM mitigated. No HIGH severity threats. No block-on-severity gates triggered.
</threat_model>

<verification>
After the task runs, manually confirm:

```bash
# File exists and is syntactically valid Python
test -f mcp-gateway/tests/test_image_hash.py
python -c "import ast; ast.parse(open('mcp-gateway/tests/test_image_hash.py').read())"

# 11 tests collected
pytest mcp-gateway/tests/test_image_hash.py --collect-only -q
# Expect: at least 11 lines matching "mcp-gateway/tests/test_image_hash.py::..."

# All tests pass quickly
time pytest mcp-gateway/tests/test_image_hash.py -x
# Expect: "11 passed" (or "11 passed in <2s") and exit 0

# Hermeticity check — no env=os.environ
grep -c 'env=os.environ' mcp-gateway/tests/test_image_hash.py
# Expect: 0

# No Python-side hashing
grep -c 'hashlib' mcp-gateway/tests/test_image_hash.py
# Expect: 0

# Full mcp-gateway suite still green
pytest mcp-gateway/tests/ -x
# Expect: exit 0

# Phase smoke: run_docker.sh --help still exits 0
./run_docker.sh --help >/dev/null
echo "$?"   # 0
```
</verification>

<success_criteria>
- `mcp-gateway/tests/test_image_hash.py` exists, contains 7 test functions + 1 parametrized 4-case test = 11 collected pytest nodes
- All 11 tests pass under `pytest mcp-gateway/tests/test_image_hash.py -x`, with runtime <2s
- The full mcp-gateway test suite (`pytest mcp-gateway/tests/ -x`) is still green after the addition
- Fixture mirrors the exact byte-spec from RESEARCH.md §"Fixture Contents — Exact Specification" (D-08)
- All 4 Success Criteria from ROADMAP §"Phase 5" are now backed by automated tests:
  - SC-1 → `test_sc1_src_edit_changes_hash`
  - SC-2 → `test_sc2_pyproject_edit_changes_hash`
  - SC-3 → `test_sc3_pruned_writes_do_not_change_hash` (4 parametrized cases — one per spec-named pruned dir)
  - SC-4 → same as SC-1 (the regression-of-record named in the success criteria)
- D-10 bonus: `test_binja_toggle_changes_hash` guards the existing Binja/IDA conditional from regressing
- D-11 honored: test is hermetic (no network, no docker, no real mcp-gateway tree mutation, no env=os.environ)
- FOUND-01 is now verifiable end-to-end via `pytest mcp-gateway/tests/test_image_hash.py -x` (the phase exit gate)
</success_criteria>

<output>
After completion, create `.planning/phases/05-f-1-image-hash-fix/05-03-SUMMARY.md`.
</output>
