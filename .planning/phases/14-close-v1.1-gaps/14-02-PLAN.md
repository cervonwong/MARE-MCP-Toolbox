---
phase: 14
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - HARDEN-01
  - HARDEN-02
  - HARDEN-03
  - HARDEN-04
  - HARDEN-05
  - HARDEN-06
  - HARDEN-07
  - SESS-CAP-01
  - JOBS-CAP-01
  - SHELL-03
  - ARTIF-01
  - ARTIF-02
  - ARTIF-03
  - ARTIF-04

must_haves:
  truths:
    - "HARDEN-01..07, SESS-CAP-01, JOBS-CAP-01 are all checked [x] in REQUIREMENTS.md (9 checkboxes flipped from [ ] to [x])"
    - "SHELL-03, ARTIF-01, ARTIF-02, ARTIF-03, ARTIF-04 are all checked [x] in REQUIREMENTS.md (5 stale checkboxes flipped from [ ] to [x])"
    - "The 9 Phase 13 traceability rows show Phase 13 (not Phase 14) in the Phase column, list their actual plan filenames in the Plan column, and show [x] in the Verified column"
    - "The 5 already-satisfied SHELL-03/ARTIF-01..04 traceability rows show [x] (or 'Verified') in the Verified column"
    - "The header coverage line literally reads `**Coverage:** 61/61` (already correct per research; this plan only verifies)"
    - "No requirement body wording is changed (bodies already exist per research finding)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Source of truth for milestone v1.1 requirements + traceability"
      contains: "61/61"
      min_lines: 200
  key_links:
    - from: "REQUIREMENTS.md body checkboxes (lines ~94-102)"
      to: "REQUIREMENTS.md traceability rows (lines ~199-207)"
      via: "Both must agree — checked body checkbox ↔ Verified [x] in table"
      pattern: "\\[x\\] \\*\\*HARDEN-0[1-7]"
    - from: "REQUIREMENTS.md traceability rows for Phase 13 reqs"
      to: ".planning/phases/13-*/13-VERIFICATION.md"
      via: "Plan column points to the 4 Phase 13 plan filenames; Verified column reflects 13-VERIFICATION.md status PASS"
      pattern: "HARDEN-0[1-7].*Phase 13"
---

<objective>
Sync `.planning/REQUIREMENTS.md` to honestly reflect the post-Phase-13 state of v1.1 requirements. **Critical research finding (14-RESEARCH.md):** the requirement BODIES for HARDEN-01..07, SESS-CAP-01, JOBS-CAP-01 already exist (lines 94-102), the traceability ROWS already exist (lines 199-207), and the coverage line already reads `61/61` (line 209). This plan is therefore a series of surgical **checkbox flips and column updates**, NOT body-insertion.

Three discrete edits:

1. **D-05 / D-06 / D-07 (Phase 13 sync):** Flip `[ ]` → `[x]` on the 9 HARDEN/SESS-CAP/JOBS-CAP body checkboxes (lines 94-102). Update the 9 traceability rows (lines 199-207): set Phase column to `Phase 13` (currently shows `Phase 14` incorrectly — the work was done in Phase 13; Phase 14 only syncs traceability), populate the Plan column with the actual `13-NN-PLAN.md` filenames, and flip the Verified column from `Pending` to `[x]`. Verify `61/61` coverage line (line 209) is already present — no change needed.

2. **D-08 (Phase 7 stale checkboxes):** Flip `[ ]` → `[x]` on SHELL-03 (line 22) and ARTIF-01..04 (lines 39-42). Update the 5 corresponding traceability rows (lines 153 for SHELL-03; lines 164-167 for ARTIF-01..04): flip the Verified column from `Pending` to `[x]`. Source-of-truth: `.planning/phases/07-*/07-VERIFICATION.md` already marks these satisfied at the host-runnable layer.

3. **No body wording changes.** The research finding is explicit: bodies already exist with correct wording. Do not rewrite, expand, or polish — checkbox flips and table-column updates only.

Purpose: Bring REQUIREMENTS.md into agreement with the verification artifacts and Phase 13 evidence, so `/gsd-audit-milestone v1.1` finds zero traceability gaps when re-run in Plan 05.

Output: One file modified (`.planning/REQUIREMENTS.md`); 14 checkbox flips total; 14 traceability-row column updates total; zero new lines of requirement-body text. Plan 02 is wave-1 parallel with Plans 01 and 03 (different files, no overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-close-v1.1-gaps/14-CONTEXT.md
@.planning/phases/14-close-v1.1-gaps/14-RESEARCH.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-run-shell-typed-static-wrappers-re-artifacts/07-VERIFICATION.md
@.planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VERIFICATION.md

<interfaces>
**Current REQUIREMENTS.md state (verbatim from grep, with line numbers):**

```
22: - [ ] **SHELL-03**: `run_shell` docstring explicitly documents that confinement is structural posture ...
39: - [ ] **ARTIF-01**: Each case directory supports lazily-created subdirs: `tool-logs/`, `extracted/`, ...
40: - [ ] **ARTIF-02**: Agent can write/append artifacts via `write_artifact(...)` and `append_artifact(...)`...
41: - [ ] **ARTIF-03**: Agent can enumerate artifacts via `list_artifacts(...)` and `get_artifact_tree(...)`
42: - [ ] **ARTIF-04**: Agent can range-read large tool logs via `get_tool_log(...)` ...
94: - [ ] **HARDEN-01**: SessionRegistry cap is enforced atomically — N+1 concurrent ...
95: - [ ] **HARDEN-02**: BackgroundJobRegistry cap is enforced atomically — N+1 concurrent ...
96: - [ ] **HARDEN-03**: r2 sessions are spawned with `[-e, cfg.sandbox=true]` BEFORE ...
97: - [ ] **HARDEN-04**: No `cfg.sandbox.grain` argv flag is emitted ...
98: - [ ] **HARDEN-05**: The `_DANGEROUS_R2_CMD_RE` pattern remains byte-identical ...
99: - [ ] **HARDEN-06**: `open_r2_session_unsafe` is registered iff `MCP_GATEWAY_R2_UNSAFE_ALLOWED=1` ...
100:- [ ] **HARDEN-07**: `SessionCapReached.to_dict()` and `JobCapReached.to_dict()` are byte-identical ...
101:- [ ] **SESS-CAP-01**: Session slot lifecycle holds end-to-end ...
102:- [ ] **JOBS-CAP-01**: Job slot lifecycle holds end-to-end ...
```

**Traceability table — current rows (verbatim):**

```
153: | SHELL-03  | Phase 7  | TBD  | Pending  |
164: | ARTIF-01  | Phase 7  | TBD  | Pending  |
165: | ARTIF-02  | Phase 7  | TBD  | Pending  |
166: | ARTIF-03  | Phase 7  | TBD  | Pending  |
167: | ARTIF-04  | Phase 7  | TBD  | Pending  |
199: | HARDEN-01 | Phase 14 | TBD  | Pending  |
200: | HARDEN-02 | Phase 14 | TBD  | Pending  |
201: | HARDEN-03 | Phase 14 | TBD  | Pending  |
202: | HARDEN-04 | Phase 14 | TBD  | Pending  |
203: | HARDEN-05 | Phase 14 | TBD  | Pending  |
204: | HARDEN-06 | Phase 14 | TBD  | Pending  |
205: | HARDEN-07 | Phase 14 | TBD  | Pending  |
206: | SESS-CAP-01 | Phase 14 | TBD | Pending |
207: | JOBS-CAP-01 | Phase 14 | TBD | Pending |
209: **Coverage:** 61/61 v1.1 requirements mapped (100%). Phase-13 hardening rows ...
```

**Phase 13 plan-filename ↔ requirement mapping (from 13-VERIFICATION.md status PASS):**

```
HARDEN-01   → 13-01-PLAN.md      (SessionRegistry BoundedSemaphore)
HARDEN-02   → 13-02-PLAN.md      (BackgroundJobRegistry BoundedSemaphore)
HARDEN-03   → 13-03-PLAN.md      (r2 sandbox argv) + Phase 14 D-12 live arm
HARDEN-04   → 13-03-PLAN.md      (no grain flag invariant)
HARDEN-05   → 13-03-PLAN.md      (frozen regex docstring reframe)
HARDEN-06   → 13-04-PLAN.md      (open_r2_session_unsafe)
HARDEN-07   → 13-01-PLAN.md, 13-02-PLAN.md  (cap-reject dict shape snapshots — both registries)
SESS-CAP-01 → 13-01-PLAN.md      (sessions lifecycle)
JOBS-CAP-01 → 13-02-PLAN.md      (jobs lifecycle)
```

**Phase 7 plan-filename ↔ requirement mapping (from 07-VERIFICATION.md):**

```
SHELL-03 → 07-07-PLAN.md         (run_shell docstring posture wording)
ARTIF-01 → 07-01-PLAN.md, 07-05-PLAN.md    (EXPANDED_CASE_SUBDIRS + lazy creation)
ARTIF-02 → 07-05-PLAN.md          (write_artifact / append_artifact)
ARTIF-03 → 07-05-PLAN.md, 07-04-PLAN.md    (list_artifacts / get_artifact_tree / resources walker)
ARTIF-04 → 07-05-PLAN.md          (get_tool_log range-read)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip 9 Phase 13 body checkboxes AND update 9 Phase 13 traceability rows (D-05, D-06, D-07)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 90-105 for body checkboxes; lines 195-210 for traceability rows; line 209 for coverage)
    - .planning/phases/13-harden-concurrency-caps-and-r2-sandboxing/13-VERIFICATION.md (confirms all 9 reqs are PASS at the code/test level)
    - .planning/phases/14-close-v1.1-gaps/14-CONTEXT.md (D-05, D-06, D-07 locked decisions)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Make these exact substitutions in `.planning/REQUIREMENTS.md`. Each is a one-character (or one-column) edit — do NOT change any other characters on the affected lines.

    **A. Body checkbox flips (lines 94-102):**

    For EACH of the 9 lines below, change leading `- [ ] **HARDEN-` (and `SESS-CAP`, `JOBS-CAP`) to `- [x] **HARDEN-` (and equivalents). The pattern is: leading checkbox `[ ]` → `[x]`, nothing else.

    Specifically, these 9 substring replacements (use single-line edits, NOT global find/replace — each appears only once with this exact prefix):
    1. `- [ ] **HARDEN-01**` → `- [x] **HARDEN-01**`
    2. `- [ ] **HARDEN-02**` → `- [x] **HARDEN-02**`
    3. `- [ ] **HARDEN-03**` → `- [x] **HARDEN-03**`
    4. `- [ ] **HARDEN-04**` → `- [x] **HARDEN-04**`
    5. `- [ ] **HARDEN-05**` → `- [x] **HARDEN-05**`
    6. `- [ ] **HARDEN-06**` → `- [x] **HARDEN-06**`
    7. `- [ ] **HARDEN-07**` → `- [x] **HARDEN-07**`
    8. `- [ ] **SESS-CAP-01**` → `- [x] **SESS-CAP-01**`
    9. `- [ ] **JOBS-CAP-01**` → `- [x] **JOBS-CAP-01**`

    The body wording AFTER `**HARDEN-NN**:` is unchanged. Do not touch it.

    **B. Traceability row updates (lines 199-207):**

    Replace the 9 traceability rows with the following exact rows. Each row updates THREE columns: Phase (14 → 13), Plan (TBD → actual filenames), Verified (Pending → [x]). Use a regex/textual replace per row.

    Replace:
    ```
    | HARDEN-01 | Phase 14 | TBD  | Pending  |
    | HARDEN-02 | Phase 14 | TBD  | Pending  |
    | HARDEN-03 | Phase 14 | TBD  | Pending  |
    | HARDEN-04 | Phase 14 | TBD  | Pending  |
    | HARDEN-05 | Phase 14 | TBD  | Pending  |
    | HARDEN-06 | Phase 14 | TBD  | Pending  |
    | HARDEN-07 | Phase 14 | TBD  | Pending  |
    | SESS-CAP-01 | Phase 14 | TBD | Pending |
    | JOBS-CAP-01 | Phase 14 | TBD | Pending |
    ```
    With:
    ```
    | HARDEN-01 | Phase 13 | 13-01-PLAN.md             | [x]      |
    | HARDEN-02 | Phase 13 | 13-02-PLAN.md             | [x]      |
    | HARDEN-03 | Phase 13 | 13-03-PLAN.md             | [x]      |
    | HARDEN-04 | Phase 13 | 13-03-PLAN.md             | [x]      |
    | HARDEN-05 | Phase 13 | 13-03-PLAN.md             | [x]      |
    | HARDEN-06 | Phase 13 | 13-04-PLAN.md             | [x]      |
    | HARDEN-07 | Phase 13 | 13-01-PLAN.md, 13-02-PLAN.md | [x]   |
    | SESS-CAP-01 | Phase 13 | 13-01-PLAN.md           | [x]      |
    | JOBS-CAP-01 | Phase 13 | 13-02-PLAN.md           | [x]      |
    ```

    Preserve the markdown table column alignment as best as practical (the trailing spaces in the table cells are decorative; readers and audits parse the table cells, not the whitespace).

    **C. Coverage line (D-07) — verify only:**

    Run `grep -n "Coverage.*61/61" .planning/REQUIREMENTS.md`. It MUST return at least 1 hit on line 209. If the grep returns 0, the research finding was wrong — STOP and report. Per research, this line is already correct; no edit needed.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*HARDEN-0[1-7]\*\*\|^- \[x\] \*\*SESS-CAP-01\*\*\|^- \[x\] \*\*JOBS-CAP-01\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*HARDEN-0[1-7]\*\*" .planning/REQUIREMENTS.md` returns exactly 7
    - `grep -c "^- \[x\] \*\*SESS-CAP-01\*\*" .planning/REQUIREMENTS.md` returns exactly 1
    - `grep -c "^- \[x\] \*\*JOBS-CAP-01\*\*" .planning/REQUIREMENTS.md` returns exactly 1
    - `grep -c "^- \[ \] \*\*HARDEN-0[1-7]\*\*\|^- \[ \] \*\*SESS-CAP-01\*\*\|^- \[ \] \*\*JOBS-CAP-01\*\*" .planning/REQUIREMENTS.md` returns exactly 0 (no unchecked Phase 13 boxes remain)
    - `grep -cE "\| HARDEN-0[1-7] \| Phase 13 \|" .planning/REQUIREMENTS.md` returns exactly 7
    - `grep -cE "\| (SESS|JOBS)-CAP-01 \| Phase 13 \|" .planning/REQUIREMENTS.md` returns exactly 2
    - `grep -cE "\| HARDEN-0[1-7] \| Phase 14 \|" .planning/REQUIREMENTS.md` returns exactly 0 (no Phase-14-labelled HARDEN rows remain)
    - `grep -c "13-01-PLAN.md\|13-02-PLAN.md\|13-03-PLAN.md\|13-04-PLAN.md" .planning/REQUIREMENTS.md` returns at least 9 (every Phase 13 row has a plan filename)
    - `grep -c "Coverage.*61/61" .planning/REQUIREMENTS.md` returns at least 1 (D-07 invariant verified)
    - `grep -cE "\| HARDEN-0[1-7] \| Phase 13 \| 13-[0-9]{2}-PLAN\.md.* \| \[x\]" .planning/REQUIREMENTS.md` returns exactly 7 (every HARDEN row has Plan filename AND Verified [x])
  </acceptance_criteria>
  <done>
    9 Phase 13 body checkboxes flipped to [x]; 9 traceability rows updated (Phase column → 13, Plan column → real filenames, Verified column → [x]); coverage line still reads `61/61` (no change needed).
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip 5 stale Phase 7 checkboxes AND update 5 Phase 7 traceability rows (D-08)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 20-25 for SHELL-03; lines 39-43 for ARTIF-01..04; lines 150-170 for the traceability rows)
    - .planning/phases/07-run-shell-typed-static-wrappers-re-artifacts/07-VERIFICATION.md (confirms SHELL-03 + ARTIF-01..04 SATISFIED at host-runnable layer — search for "SATISFIED" near each ID)
    - .planning/phases/14-close-v1.1-gaps/14-CONTEXT.md (D-08 locked decision)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    **A. Body checkbox flips (lines 22, 39-42):**

    Make these 5 single-line substring replacements (each unique by `**ID**` suffix):

    1. `- [ ] **SHELL-03**` → `- [x] **SHELL-03**`
    2. `- [ ] **ARTIF-01**` → `- [x] **ARTIF-01**`
    3. `- [ ] **ARTIF-02**` → `- [x] **ARTIF-02**`
    4. `- [ ] **ARTIF-03**` → `- [x] **ARTIF-03**`
    5. `- [ ] **ARTIF-04**` → `- [x] **ARTIF-04**`

    The body wording after `**ID**:` is unchanged.

    **B. Traceability row updates (lines 153, 164-167):**

    Replace these 5 rows (preserve alignment as practical):
    ```
    | SHELL-03  | Phase 7  | TBD  | Pending  |
    | ARTIF-01  | Phase 7  | TBD  | Pending  |
    | ARTIF-02  | Phase 7  | TBD  | Pending  |
    | ARTIF-03  | Phase 7  | TBD  | Pending  |
    | ARTIF-04  | Phase 7  | TBD  | Pending  |
    ```
    With:
    ```
    | SHELL-03  | Phase 7  | 07-07-PLAN.md                | [x]      |
    | ARTIF-01  | Phase 7  | 07-01-PLAN.md, 07-05-PLAN.md | [x]      |
    | ARTIF-02  | Phase 7  | 07-05-PLAN.md                | [x]      |
    | ARTIF-03  | Phase 7  | 07-04-PLAN.md, 07-05-PLAN.md | [x]      |
    | ARTIF-04  | Phase 7  | 07-05-PLAN.md                | [x]      |
    ```

    **C. Pre-flip sanity check (cheap defence against drift):**

    Before flipping a body checkbox, grep 07-VERIFICATION.md for each ID and confirm SATISFIED:
    ```
    grep -E "SHELL-03|ARTIF-0[1-4]" .planning/phases/07-run-shell-typed-static-wrappers-re-artifacts/07-VERIFICATION.md
    ```
    If any of the 5 IDs is NOT marked SATISFIED in 07-VERIFICATION.md, STOP — do not flip that checkbox; record the discrepancy in the SUMMARY and leave the body checkbox unchanged. Per CONTEXT.md D-08, all 5 are SATISFIED per Phase 7 verification — this check is belt-and-braces against research drift.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*SHELL-03\*\*\|^- \[x\] \*\*ARTIF-0[1-4]\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*SHELL-03\*\*" .planning/REQUIREMENTS.md` returns exactly 1
    - `grep -c "^- \[x\] \*\*ARTIF-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns exactly 4
    - `grep -c "^- \[ \] \*\*SHELL-03\*\*\|^- \[ \] \*\*ARTIF-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns exactly 0
    - `grep -cE "\| SHELL-03 .*\| 07-07-PLAN\.md .*\| \[x\]" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -cE "\| ARTIF-0[1-4] .*\| 07-.*PLAN\.md.*\| \[x\]" .planning/REQUIREMENTS.md` returns exactly 4
    - `grep -cE "\| (SHELL-03|ARTIF-0[1-4])\s+\| Phase 7\s+\| TBD\s+\| Pending" .planning/REQUIREMENTS.md` returns exactly 0 (no stale Pending/TBD rows remain for these IDs)
  </acceptance_criteria>
  <done>
    5 stale Phase 7 body checkboxes flipped to [x]; 5 traceability rows updated (Plan column → real filenames, Verified column → [x]); Phase 7 verification trail consistent with REQUIREMENTS.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cross-file consistency check + commit</name>
  <read_first>
    - .planning/REQUIREMENTS.md (final state after Tasks 1 + 2)
    - .planning/v1.1-MILESTONE-AUDIT.md (the audit that demanded this sync — confirms 61/61 + traceability_gap closed)
  </read_first>
  <files>(no source edits — verification + commit only)</files>
  <action>
    1. Final pass-count: `grep -c "^- \[x\]" .planning/REQUIREMENTS.md` MUST equal at least 61 (all v1.1 requirements checked at body level). If less, identify the discrepancy.

    2. Run `grep -c "^- \[ \]" .planning/REQUIREMENTS.md` — this should return 0 for v1.1 requirement bodies (lines 1-105). Note: the "Future Requirements" section at the bottom may still have legitimately-unchecked items — those are out of scope. Use the more specific:
    ```
    grep -nE "^- \[ \] \*\*[A-Z]+-(0[0-9])" .planning/REQUIREMENTS.md
    ```
    This MUST return 0 lines (no remaining unchecked v1.1 requirement IDs).

    3. Verify the traceability table integrity:
    ```
    grep -cE "\| [A-Z]+-(0[0-9]|CAP-0[0-9])\s+\| Phase [0-9]+\s+\|" .planning/REQUIREMENTS.md
    ```
    Returns at least 61 (every requirement has a traceability row).

    4. Verify NO `Pending` markers remain in the traceability table for v1.1 IDs:
    ```
    grep -cE "\| (FOUND|SHELL|STATIC|ARTIF|SESS|EXTR|JOBS|DYN|SKILL|HARDEN|SESS-CAP|JOBS-CAP)-.+\| Pending" .planning/REQUIREMENTS.md
    ```
    Returns 0.

    5. Commit the file with message: `Sync REQUIREMENTS.md traceability for Phase 13 + Phase 7 stale checkboxes`.
  </action>
  <verify>
    <automated>bash -c "grep -nE '^- \[ \] \*\*[A-Z]+-(0[0-9])' .planning/REQUIREMENTS.md | wc -l | awk '{exit (\$1==0)?0:1}'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^- \[ \] \*\*[A-Z]+-(0[0-9])" .planning/REQUIREMENTS.md` returns 0 lines (no unchecked v1.1 requirements)
    - `grep -cE "\| (FOUND|SHELL|STATIC|ARTIF|SESS|EXTR|JOBS|DYN|SKILL|HARDEN|SESS-CAP|JOBS-CAP)-.+\| Pending" .planning/REQUIREMENTS.md` returns 0 (no Pending rows in traceability)
    - `grep -c "Coverage.*61/61" .planning/REQUIREMENTS.md` returns at least 1
    - `git log --oneline -1 -- .planning/REQUIREMENTS.md` shows the closure commit
    - `git diff HEAD~1 -- .planning/REQUIREMENTS.md | grep -E "^[+-]" | wc -l` is small (<= ~50 changed lines — this is a surgical sync, not a rewrite)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md fully synced: 14 checkboxes flipped (9 Phase 13 + 5 Phase 7); 14 traceability rows updated; coverage line `61/61` verified; commit landed; the diff is small and surgical.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact ↔ truth | The audit success oracle reads REQUIREMENTS.md as the source of truth for milestone state. False claims here would mislead future audits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02-01 | Repudiation (false claim of completion) | REQUIREMENTS.md checkbox flips | medium | mitigate | Task 2 step C pre-flip sanity check greps `07-VERIFICATION.md` for SATISFIED before flipping each ARTIF-0X / SHELL-03 checkbox. Task 1 anchors HARDEN/SESS-CAP/JOBS-CAP flips to `13-VERIFICATION.md` status PASS (already greppable). If verification source disagrees, the task halts and records the discrepancy in SUMMARY rather than silently flipping. Plan 03's ROADMAP sync uses the same verification artifacts as the second source of truth. |
| T-14-02-02 | Information Disclosure (none) | n/a | n/a | accept | No PII, secrets, or tokens in REQUIREMENTS.md. |
| T-14-02-03 | Tampering (corruption of unrelated requirements) | REQUIREMENTS.md surgical edits | low | mitigate | Per-substring replacement (each `[ ] **ID**` prefix is unique per requirement) and small expected diff (≤50 lines) make accidental corruption obvious in `git diff`. Task 3 acceptance criterion bounds the diff size. |
</threat_model>

<verification>
Plan-02 verification chain:

1. **Phase 13 body flips:** `grep -c "^- \[x\] \*\*HARDEN-0[1-7]\*\*" .planning/REQUIREMENTS.md` == 7; SESS-CAP-01 + JOBS-CAP-01 each 1.

2. **Phase 7 body flips:** `grep -c "^- \[x\] \*\*SHELL-03\*\*\|^- \[x\] \*\*ARTIF-0[1-4]\*\*" .planning/REQUIREMENTS.md` == 5.

3. **Traceability rows synced:** `grep -cE "\| HARDEN-0[1-7] \| Phase 13 \|" .planning/REQUIREMENTS.md` == 7; no rows show `Pending` for v1.1 IDs.

4. **Coverage line preserved:** `grep -c "Coverage.*61/61" .planning/REQUIREMENTS.md` >= 1.

5. **Total v1.1 unchecked count:** `grep -nE "^- \[ \] \*\*[A-Z]+-(0[0-9])" .planning/REQUIREMENTS.md` returns 0 lines.

6. **Pre-flip-sanity grep:** `grep -E "SHELL-03|ARTIF-0[1-4]" .planning/phases/07-*/07-VERIFICATION.md` confirms SATISFIED markers (output captured in SUMMARY).

7. **Commit landed:** `git log --oneline -1 -- .planning/REQUIREMENTS.md` shows the closure commit.
</verification>

<success_criteria>
- All 9 HARDEN/SESS-CAP/JOBS-CAP body checkboxes show `[x]`
- All 5 stale SHELL-03 + ARTIF-01..04 body checkboxes show `[x]`
- All 9 Phase 13 traceability rows show `Phase 13` (not 14), real plan filenames, and `[x]` in Verified column
- All 5 Phase 7 traceability rows for the flipped IDs show real plan filenames and `[x]` in Verified column
- Coverage line still reads `61/61`
- No `[ ]` v1.1 requirement bodies remain (Future Requirements section excluded — that's not v1.1)
- No `Pending` traceability rows remain for v1.1 IDs
- `git diff` for the commit is small and surgical (no body wording changed)
</success_criteria>

<output>
After completion, create `.planning/phases/14-close-v1.1-gaps/14-02-SUMMARY.md` documenting:
- 14 checkbox flips landed (9 Phase 13 + 5 Phase 7)
- 14 traceability rows updated (column-only changes; no body changes)
- Verbatim copy of the pre-flip sanity grep output from `07-VERIFICATION.md` (proves SHELL-03 / ARTIF-0X SATISFIED before flipping)
- Final `git diff` line count to demonstrate surgical scope
- Any deviation from the plan (e.g., research-finding drift) with Rule-1/2/3 markers
</output>
